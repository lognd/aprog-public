# {{ assignment_name }}

<!-- FILL IN: write the problem statement here. -->

## Submission

Submit a single file named `{{ artifact_name }}.cpp`.

Your file must contain a `main` function and compile cleanly with:

```bash
g++ -std=c++17 -Wall -Wextra -fsanitize=address,undefined -g \
    -o {{ artifact_name }} {{ artifact_name }}.cpp
```

## Grading

| Component         | Points |
|------------------|--------|
| Compilation       | 10     |
| Correctness       | varies |
| Memory Safety     | varies |

Your program is compiled with AddressSanitizer (ASan) and
UndefinedBehaviorSanitizer (UBSan). Any memory error (heap overflow,
use-after-free, memory leak, undefined behavior) will be flagged and will
reduce your score.
