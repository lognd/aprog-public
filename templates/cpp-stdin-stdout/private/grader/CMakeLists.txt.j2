cmake_minimum_required(VERSION 3.16)
project({{ artifact_name }}Grader CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# After InjectStudentIntoStaff runs, {{ artifact_name }}.cpp lives alongside
# this CMakeLists.txt. The student binary is built from the injected file.
add_executable({{ artifact_name }}
    "${CMAKE_SOURCE_DIR}/{{ artifact_name }}.cpp"
)
target_compile_options({{ artifact_name }} PRIVATE -Wall -Wextra -g)
