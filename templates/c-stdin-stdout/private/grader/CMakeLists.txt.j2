cmake_minimum_required(VERSION 3.16)
project({{ artifact_name }}Grader C)

set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)

# After InjectStudentIntoStaff runs, {{ artifact_name }}.c lives here.
add_executable({{ artifact_name }}
    "${CMAKE_SOURCE_DIR}/{{ artifact_name }}.c"
)
target_compile_options({{ artifact_name }} PRIVATE -Wall -Wextra -g)
