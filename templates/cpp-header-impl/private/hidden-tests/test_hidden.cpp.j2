// Hidden Catch2 test suite for {{ assignment_name }}.
// Not shown to students until after the due date.

#include <catch2/catch_test_macros.hpp>
#include "{{ artifact_name }}.hpp"

// FILL IN: thorough hidden test cases.

TEST_CASE("hidden example test", "[hidden]") {
    // TODO: replace with a real hidden test.
    REQUIRE(true);
}
