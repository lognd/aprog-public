// Visible Catch2 test suite for {{ assignment_name }}.
//
// Students can compile and run this locally:
//   mkdir build && cd build
//   cmake .. -DSUBMISSION_DIR=<path-to-your-submission>
//   cmake --build .
//   ./{{ artifact_name }}_tests
//
// The grader runs all of these cases and shows results immediately.

#include <catch2/catch_test_macros.hpp>
#include "{{ artifact_name }}.hpp"

// FILL IN: add visible test cases that match the examples in README.md.

TEST_CASE("example test", "[basic]") {
    // TODO: replace with a real test.
    REQUIRE(true);
}
