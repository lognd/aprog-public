// Grader driver for {{ assignment_name }}.
// FILL IN: exercise the student's {{ artifact_name }}.hpp interface.

#include "{{ artifact_name }}.hpp"
#include <iostream>

int main(int argc, char** argv) {
    // FILL IN: create objects, call methods, print results.
    // This binary is used by OutputCompareTest to check runtime behavior.

    // Example:
    // {{ artifact_name | capitalize }} obj;
    // std::cout << obj.get_value() << "\n";

    return 0;
}
