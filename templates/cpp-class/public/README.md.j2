# {{ assignment_name }}

{{ description }}

## Problem Statement

TODO: Describe the class students must implement.

## Interface

The header file `{{ artifact_name }}.hpp` is provided. Implement the class in
`{{ artifact_name }}.cpp`:

```cpp
// FILL IN: show key methods here.
```

## Requirements

- TODO: list requirements (e.g., no STL containers, manual memory management).

## Grading

| Component              | Points |
|------------------------|--------|
| Compilation            | 0 (required) |
| Visible tests (Catch2) | 60     |
| Hidden tests (Catch2)  | 40     |

## Submission

Submit `{{ artifact_name }}.hpp` and `{{ artifact_name }}.cpp`. Do not change the
class name or method signatures in the header.
