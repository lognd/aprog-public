cmake_minimum_required(VERSION 3.16)
project({{ artifact_name }}VisibleTests CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

if(NOT DEFINED SUBMISSION_DIR)
    set(SUBMISSION_DIR "${CMAKE_SOURCE_DIR}/..")
endif()

include(FetchContent)
FetchContent_Declare(
    Catch2
    GIT_REPOSITORY https://github.com/catchorg/Catch2.git
    GIT_TAG        v3.5.2
)
FetchContent_MakeAvailable(Catch2)

add_executable({{ artifact_name }}_tests
    test_catch.cpp
    "${SUBMISSION_DIR}/{{ artifact_name }}.cpp"
)
target_include_directories({{ artifact_name }}_tests PRIVATE "${SUBMISSION_DIR}")
target_link_libraries({{ artifact_name }}_tests PRIVATE Catch2::Catch2WithMain)
target_compile_options({{ artifact_name }}_tests PRIVATE -Wall -Wextra -g)
