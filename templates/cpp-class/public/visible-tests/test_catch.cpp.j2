// Visible Catch2 tests for {{ assignment_name }}.
#include <catch2/catch_test_macros.hpp>
#include "{{ artifact_name }}.hpp"

// FILL IN: add visible test cases.
TEST_CASE("example test", "[basic]") {
    REQUIRE(true);
}
