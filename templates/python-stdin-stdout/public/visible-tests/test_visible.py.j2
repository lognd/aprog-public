"""
Visible tests for {{ assignment_name }}.

Run locally:
    python -m pytest test_visible.py -v

These tests call your program as a subprocess and compare its stdout
against the expected output for each sample case.
"""

import subprocess
import sys
from pathlib import Path

SUBMISSION = Path(__file__).parent.parent / "{{ artifact_name }}.py"


def run(stdin: str, args: list[str] | None = None) -> str:
    result = subprocess.run(
        [sys.executable, str(SUBMISSION)] + (args or []),
        input=stdin,
        capture_output=True,
        text=True,
        timeout=10,
    )
    return result.stdout


# FILL IN: add visible test cases matching the sample examples in README.md.
def test_example_1():
    output = run("TODO\n")
    assert output.strip() == "TODO"
