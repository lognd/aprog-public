# Hidden Tests

`test_hidden.cpp` -- Catch2 hidden test suite. Compiled by the grader's
CMakeLists.txt against the student's submitted header.

`{{ artifact_name }}.hpp` -- Staff reference implementation. Compiled into the
`reference` binary for DifferentialTest extra-credit cases.
