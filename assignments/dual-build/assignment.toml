[assignment]
slug = "dual-build"
name = "Dual Build"
author = "lognd"
description = "Write a Makefile and a CMakeLists.txt for the same C++ library project."
version = "0.1.0"
status = "draft"

[classification]
language = "cpp"
difficulty = "medium"
topics = ["systems", "build-systems"]
concepts = ["makefile", "cmake", "static-libraries"]
labels = ["starter-code", "multi-file"]

[template]
slug = "cpp-cmake"
version = "0.1"

[grader]
visibility = "visible"
stdout_visibility = "visible"

[grader.dependencies]
system = ["g++", "cmake", "make"]
