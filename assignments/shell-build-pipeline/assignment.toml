[assignment]
slug = "shell-build-pipeline"
name = "Shell Build Pipeline"
author = "Logan Dapp"
description = "Write a shell script that replicates g++ compilation in stages with incremental rebuilds."
version = "0.1.0"
status = "draft"

[classification]
language = "cpp"
difficulty = "medium"
topics = ["systems", "build-systems", "io"]
concepts = ["iteration", "shell-scripting", "file-io"]
labels = ["starter-code", "requires-filesystem"]

[template]
slug = "shell-script"
version = "0.1"

[grader]
visibility = "visible"
stdout_visibility = "visible"

[grader.dependencies]
system = ["g++"]
