name: Verify Assignments

on:
  workflow_dispatch:
    inputs:
      slug:
        description: "Assignment slug (leave empty to verify all)"
        required: false
        default: ""
  pull_request:
    types: [labeled]

jobs:
  verify-full:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && github.event.label.name == 'verify')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: public

      - uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/aprog-private
          ssh-key: ${{ secrets.APROG_PRIVATE_DEPLOY_KEY }}
          path: private

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          python-version: "3.10"

      - name: Install AProg
        run: uv sync --project public

      - name: Verify
        run: |
          SLUG="${{ github.event.inputs.slug }}"
          if [ -n "$SLUG" ]; then
            uv run --project public aprog verify "$SLUG" --public public --private private
          else
            uv run --project public aprog verify --all --public public --private private
          fi
