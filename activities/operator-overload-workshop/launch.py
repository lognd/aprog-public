#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/operator-overload-workshop.json and re-run the generator.
"""Operator Overload Workshop

Nine compilable C++ programs that teach operator overloading from the ground up: operator== compares contents instead of addresses, operator+ builds a new object, operator<< prints through a free function, operator[] reads and writes through a reference, pre/post ++ semantics, and explicit operator bool. Predict the exact stdout of each to build a precise mental model.
"""
import json, os, subprocess, sys, tempfile

# -- Crypto / obfuscation --
_KEY_A = "bfed509d9b503bb9eb60d48608242ccfe7dff842cdb2ba024bcd5f7a107f7cbe"
_KEY_B = "640b9cc951eb90a64eac9f565e859e042cfb4c84de2b0f1c3fad010228527914"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "693490e0597a9988d5d0cb794f6b21ad1519dd48dd331c12a3ce46b075eaede6a44f57e4d13a2964377c8adb1390fcf61727369bdf0eb37d3b997ecc"

# -- Public snippets and encrypted answer/explanation material --
SNIPPETS = json.loads(r"""
[
  {
    "id": 1,
    "title": "operator== can compare contents instead of addresses",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <cstring>\nstruct Str {\n    char buf[16];\n    Str(const char* s) { strcpy(buf, s); }\n    bool operator==(const Str& other) const {\n        return strcmp(buf, other.buf) == 0;\n    }\n};\nint main() {\n    Str a(\"hello\");\n    Str b(\"hello\");\n    printf(\"%s\\n\", (a == b) ? \"true\" : \"false\");\n\n    char arr1[] = \"hello\";\n    char arr2[] = \"hello\";\n    char* p1 = arr1;\n    char* p2 = arr2;\n    printf(\"%s\\n\", (p1 == p2) ? \"true\" : \"false\");\n}\n",
    "prompt": "What does this program print? (two lines)"
  },
  {
    "id": 2,
    "title": "operator+ builds a new object and leaves both operands alone",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\nstruct Vec2 {\n    int x, y;\n    Vec2(int x_, int y_) : x(x_), y(y_) {}\n    Vec2 operator+(const Vec2& other) const {\n        return Vec2(x + other.x, y + other.y);\n    }\n};\nint main() {\n    Vec2 a(1, 2);\n    Vec2 b(3, 4);\n    Vec2 c = a + b;\n    printf(\"%d %d\\n\", c.x, c.y);\n    printf(\"%d %d\\n\", a.x, a.y);\n    printf(\"%d %d\\n\", b.x, b.y);\n}\n",
    "prompt": "What does this program print? (three lines: c, then a, then b)"
  },
  {
    "id": 3,
    "title": "operator!= implemented in terms of operator==",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\nstruct Point {\n    int x, y;\n    Point(int x_, int y_) : x(x_), y(y_) {}\n    bool operator==(const Point& o) const { return x == o.x && y == o.y; }\n    bool operator!=(const Point& o) const { return !(*this == o); }\n};\nint main() {\n    Point a(1, 2);\n    Point b(1, 2);\n    Point c(3, 4);\n    printf(\"%s\\n\", (a == b) ? \"true\" : \"false\");\n    printf(\"%s\\n\", (a != c) ? \"true\" : \"false\");\n}\n",
    "prompt": "What does this program print? (two lines)"
  },
  {
    "id": 4,
    "title": "operator<< as a free function, not a member",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <iostream>\nstruct Point {\n    int x, y;\n    Point(int x_, int y_) : x(x_), y(y_) {}\n};\nstd::ostream& operator<<(std::ostream& os, const Point& p) {\n    os << \"(\" << p.x << \", \" << p.y << \")\";\n    return os;\n}\nint main() {\n    Point p(3, 4);\n    std::cout << p << \"\\n\";\n}\n",
    "prompt": "What does this program print?"
  },
  {
    "id": 5,
    "title": "operator[] returning a reference lets you write through it",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\nstruct Trio {\n    int data[3];\n    Trio(int a, int b, int c) : data{a, b, c} {}\n    int& operator[](int i) { return data[i]; }\n};\nint main() {\n    Trio t(1, 2, 3);\n    t[1] = 99;\n    printf(\"%d %d %d\\n\", t[0], t[1], t[2]);\n}\n",
    "prompt": "What does this program print?"
  },
  {
    "id": 6,
    "title": "the const overload of operator[] is selected by the object's constness",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\nstruct Trio {\n    int data[3];\n    Trio(int a, int b, int c) : data{a, b, c} {}\n    int& operator[](int i) { return data[i]; }\n    int operator[](int i) const { return data[i]; }\n};\nvoid printFirst(const Trio& t) {\n    printf(\"%d\\n\", t[0]);\n}\nint main() {\n    Trio t(5, 6, 7);\n    t[0] = 50;\n    printf(\"%d\\n\", t[0]);\n    printFirst(t);\n}\n",
    "prompt": "What does this program print? (two lines)"
  },
  {
    "id": 7,
    "title": "pre-increment returns the updated object; post-increment returns the old value",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\nstruct Counter {\n    int n;\n    Counter(int n_) : n(n_) {}\n    Counter& operator++() { ++n; return *this; }\n    Counter operator++(int) { Counter old = *this; ++n; return old; }\n};\nint main() {\n    Counter a(5);\n    Counter& refA = ++a;\n    printf(\"%d %d\\n\", a.n, refA.n);\n\n    Counter b(5);\n    Counter old = b++;\n    printf(\"%d %d\\n\", b.n, old.n);\n}\n",
    "prompt": "What does this program print? (two lines)"
  },
  {
    "id": 8,
    "title": "explicit operator bool lets an object be used directly in an if condition",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\nstruct Handle {\n    bool valid;\n    Handle(bool v) : valid(v) {}\n    explicit operator bool() const { return valid; }\n};\nint main() {\n    Handle h(true);\n    if (h) {\n        printf(\"valid\\n\");\n    } else {\n        printf(\"invalid\\n\");\n    }\n    Handle h2(false);\n    if (h2) {\n        printf(\"valid\\n\");\n    } else {\n        printf(\"invalid\\n\");\n    }\n}\n",
    "prompt": "What does this program print? (two lines)"
  },
  {
    "id": 9,
    "title": "operator< enables ordering comparisons",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\nstruct Money {\n    int cents;\n    Money(int c) : cents(c) {}\n    bool operator<(const Money& o) const { return cents < o.cents; }\n};\nint main() {\n    Money a(150);\n    Money b(200);\n    printf(\"%s\\n\", (a < b) ? \"true\" : \"false\");\n    printf(\"%s\\n\", (b < a) ? \"true\" : \"false\");\n}\n",
    "prompt": "What does this program print? (two lines)"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "8sGZc(8SffpQF?dOD5&Zi|rS<eM%<e@gPTbnGOzwOTQzY49+5_4y-p4`!8P5+gmfTdXXceXv>IfwITw^Q58vaedDZQ;&`u>s(Lo=3m6;$BTBws4xTQhx<s$kc~x-ZiLHcKcvQT!*nG3@y8HpND0Zx<zLCW-=QV`?W!`LrcQU&DD4}TWDj{%E?msym#&({U5O6p1lw=DgA;j9(63_bWl=)1losYHSdT+xQbYJqx;cUK=SHzsi9CWfZtpv8~0#%Kd`1$3qZIoPMp@#xC$T{a_DZA^9<iaO5I8b47(5UFAKUHIfBWGq3MjhV$K=0;ZUf)YC=tT|7g3)mQ`a=p&0HmS8OZjojA)?)IIgRShfeJ$80BM|eREjg03FXV02Zrt;UraSMLs0^Xp4QHssr!FdtOcMY`qa!PF<9e~m_e5LLvr{8TPNCltFJZb>4VLNE%+~lZ2=mxqK<G=_qy4UAlk{Bk+zb$=0(6$e`zz+!O4qSFp{9@q98Ml9J|ThLiZc}H`P%8m|{Jfvr#zaHXt|Jn$f?QEe8I5RBdzh<J>dno30;%nLjJC2i;k8LNAlmH#mY1^NI$L>lTR9ogYp`;y`rL!7PF`LR13H?b!+FN717C1vcF(XdSd*M1NWa^?47N52?5v)sj0mXgCt5MCkb79a7iRq%~d7XHu8U3G6V39$U&t0?64aCDV?Od04P3_P23&-qatELk`PzHKd>w#kjKb`pRQLL58CAI9(7f+UJEh;&uC?;J0dUj>zA!!KfR1W*L}?Y!KiV`*bOnjPWJ;6+HZ;#DQ_Z{i!sPZHuz+`vP|$J*@GM5reF)Q+)F5HK5q6O)^AgeWF1|Om1<WP^<bWvIk&RFG~aTRw(XUjs3)fksxegYU4U5Md#VHPXJV1xihlj@;l})yeS2h`3A}NEd_LSb~Pi|2h3N;@Et;-eJ}Cvd4dMOzFIrP5fKc7h<S~-RR79N@x$C`xaLhF{M1oVgo=W%sU@n6pqd~85&",
  "q-(YC$SfI4T|b-R3Yt7kgrqafA6v3~8%>6YaZtg4-Szu@=Oe;7A1+|kVvAo!5a=%@Ne`rkNlRd!Ex%+N?L{c2FR;v08JP*%5^8(_=68|?q?Cq_)q^|>-c^qD=j!Zda*cEGmlmnEOI4DV(bYGJz>1@S_X_}Rp|;s~xq~JwD=kRvD`MSI7(kBC{*VhGU$M^(`WR^k-e(9$a8tRi^@z(-U9?Vq{V&R2Ij9rr@-&co5+51V#`m+#KZQ)~uR7kMN@_9fE;|%ooChMV<KdC2;((ul<uK)z)dVM?H^h^s!;pM~K-5&a7vvkoAk{;dKl?E-J-S1^JhAq)^#qtTF#4QeWxNsIkuHVsh{J?+dQ+lvZfo!$3FZLBoZTp~<-OL=Htamm5t=nkQXFAXOX2gD;P54k0zK>R5y!vDIg{%|=&SDsI0pjEBTG#VfHD2k7tX`y?qt-b&;RCervY7#j>n9`R|@Js9c7X^3r^bQO>~}f7O#@0hyUEC;Z$~?m_O2%RPp12gDm*J4`&$lJutM@;vNNLr71^=blDI8P;0-_@@y@J##DuzV@!D3@_Ndwx*Fo~scu_1`A#q!&oWKY4?sXz?l#+-Mux0@01Fwb$Eo?=p_;AQs_A|D@Ly6(OZc+iPlRJiChc>$-_A5(me!sj>FgmiCvZlSN{kK`hmrEoUFVzq=?T3+WgU|1R0b0p!Y5&vM*qCCQ#f-<A3s7JdAV8X+u-9k6k^jog{K!*dO^raT6F+Zk?TMw9`G%8D?%>m!38VF3UeLch2j)gFT&Ae|Hs+zkjrGK9E0+&tNu!?Pr8{9Mtr-ZHqQ*j0PKE2_}wI+0Gm)DpG;+Oi?tB%o2#;y69",
  "!?NuB^zAZ;bun(3vBBi`s*u-lfdT_G<}%A?wnY%tFu7bi7MnKNa81lf;hbUdZ68y_lKza>%N`q8QsJSh=#Ji&7_k`Q@A*c%@har?cY^;)`NA*Fc`pULC>B94Sxx!p90FU~GdFE9{C*Kuf*afXT2Otwf`4En#j|?5Z~<;VPQ*vp&J3Of$L;F>e`b^B=M7)XG5H3}@1D16l}i0$V5S9^)a7C~jnXHA3jhU6p{|bTK_}gq`!cF2P&NwazvEl$KIiz)jA0oh3AiKUs}mYu4}0!$(<i;_P2NN`4<@@~X)7+f*mWMPSX@7N%$WvHQqjwPDD&U%v9)uRm}=G1jfxBD@macVECzGIQOU*3jKKt>h!ojU4Z5Y|E-4PU5h*i+vO=<P=pcxdw+z+bTK>CTwK!D$GVHajWAN3d%;G}D1G}WH&pw&oT!lD<z$`8q)9L-+87*JL)iy_i>cGTj7%0zPkZ9q@!!{2)K8LSyjadhR@|eY^WO<%~-a?b$K`~+luGX3qd05Q*xPjzzxVi@qGn-k@gZL3iW(&Byi@i-M$R8s*i*{sTGuHBnW{XgwFWgeoVo;hL18rFuen>eH8Et+xA4oIoP-*_=&Rk|?^HIuS!Y0u3gJrt1*X4l|i$L<~bm+M$>NlO@vrs}@5EQr*B7I$#696bu@@1u;{XnK_wHx5vlIqY!%m",
  "`VDex+Yy_%gr*2iT|}_z^Q?64cbqCbaQ*5u@a0fa4y{&J;-Oip%WDVNw;?t>kQ0@}fWyw#M9Q$m2(HyRtlqF$<k4%dP89`~KS{&C)a6vz<m=wM!=V>HY2w~Pd-;e0P>?QO&MOspm`)1nhWHJ8PgWAY3RjL-$@|4x(A}AE)Jl+MGIH<eh%9mJSm9(#>RXGi6|}C6xw?{3kOe3&q;k!W(UtQ*UJv5i8i;v@+%9h&C~dS9F8%%V820I^$Kknvh3@;^{``Jo(#>EsW-p<T0y`p+=E3jN;MG8ekCX1f1@Fk;_3;dFFx$$!-Asw*Xe>bqhH7F<H8--}ahU7H*t~i{VaMN~%G60{EUkJ>P;KtCpm_P?lu=ydCCg;DI2~-nc2UpiW<>sRN$FEzDr?<mchrtJH5D}G+1n@JA03lD>KF|1%f;&{Am?;R0i^S5LFaupw09U*FLjmJ3iR6XIzFm*_F{sN%7J+im60tL5aSUBNZh5s<~S6^qBNB0`CXgJYMeLDdD5K%6oF8;FXxfQX`7Ze&-TCZ=bnMn8si-k)mCfT(ST`|OB`Zg`T^*!O8e$G?)q{f9UZs>d+4Ja|As_qe0p;!-)W26)X734D*_w)?JpGVG3$*~YBss?*J;efnr@W6{tpb~CVBltt$Qh1xHMDz+=uF%YIug{ui$63N1r}094YCCI~J$>ZR6-#F#{B9VBG4%0uiFvz@9HeyqqHn{h9J1yay~X_ZL3JB8~ucvnP6rkr)=iPb8oG66zF8piONBRbRbGU-^KOnstZRmXU=gO`>7@<}Ts!YJdhL)UYrtPK04AVnS~i",
  "iuqqW5PTCfC_B3<6ZSbx#@m{r&I4*CmNA8J0}5|W5+Nxxv1OyI7Vz#-vQ&lHY(yS!VW>8=4wqaQr2{kAUr2xv<p3!Ds&?ZIFzm&j*oAF~NfhFPj@WT^n|v+=^zf*o6SydtVGa-rxLqT=M-65@f%k&J`rqrpa806#cWZ03ZI*;2X*FgU_%LS6+nYQh9@c4(K#q38y1aU5J^(Gx&^SW)4L-3QH;0jn8CLR1IrdsWuv1zp(C?nPX>V=&oQ>0xE^@F`dN^%ljL-BYn^-r;NE(+r%lGs;>9f~9)e#Hi{(CZ7dSLpz*z<KL<{Ib<-Y6OA5TaLN?e|Z>a7r3w_<GhuUcd})Y+|#M%sV@DNtAeWKX+7eQ~aVjoKk<)rHX7%`=&WG+ieFcSx&TMOR8QU(+JJa9IKvFW>BpjfAe<XAS3zE^bmrNB_@7_<i_0Zei}&0s4*fijgLH==OJ<p2u<4I2qeB{#>-7c4viSR#o(M`FYyg?VqVE?)n)e8R-dxjixRZ>jNFwK-ySsvBz~T&o5mq>K1!3K1ufouP`Rph`ni&1hefzxhcice;O%MGZm>hmO#$`&t5Vq_Hk~$EU}Y0*G{C#h3I`_S=%LA!g?LX;a10SI;YtFlCzQ@5(~MUESK}d0$%LVoA5W1$utSNDhv<XB^(~gye)4Y",
  "tZbk(&h#>Xmwm=RxuHAEJSFy;Fr|khOh7y9sb$5n4G<zKVO;k6?{_<1z+nT}7lW4=?*B@eUF4A-F{BwFEhW}-Y(O3TkJR5HV=++=NXBjGjgl4YfY&0-aM=xHX;Ldl(3FTn{AU6#iPZzJE%G4_K<kh$8YN!nbWfO7x2Bdl-3koWg$Ci@KQs|dk>GfKdG$-4){giqp0{ZWSX;A5v?t^lC3|Tf17qhRl)$EFNk=&d(ZAv)3$DBi)MjqxXAf3R*%Go!koekgR%Nyop$EOn&S0ZAj-{HCi!rZ+Y;H#G;DT1Gv+4{gKy3&Sp#i8LgC3kIBuouai5&B;-x10nHXrWivJhj#>SkTf6u28muNZod0%vcA{@I}FCnZ#gDoSG+*N0Ts>)g1bTz|c_Vx2vN@gkF&?ARz=+q!K=Q!xL>%TL%^r=h{68C*m>6<#<u6h|9VvR?gCOn`p`5+KhiX!@r7OPuMtZ0TgE&xn0h>3hTo(Sn~C6t;B=42!@iAJ72b(7F%EiRS2cYB(I-5X~~4+)wv^l^v!o_Ja0vUA35p_JljVe>?DqY`)Z?GS;gpI8PU8{(saCb2Oji&bMt0+A2}mmf=A+FhLMGmUkW^1{vEgHV8*CXHx}AJ9*O8ub?usm^vzm=VNJPBTzmOR2<NO$~B*#aVYs4(h4QMS9Bekl-2#xf!v@euD%?L+l$txYE&h`2NM3{?`iGk-kUwH>)q5g#HEk}5A)FgwlfXk9@vab2hrQzHym2aO$fG4$j;UdX`KBQp1OtlwXIpzM)pNZT$Xe!z)9vQ5DxtSO$0|>MYWg5o4BdWBo#NKp>H;|l&UU8r&;6eI#`@jmq$9v#Q<p=DCnX0?EG-N;%{H*dHA%pq))=o$8c61ANyADp;?j",
  "$GK_OTy+u++^Qice~ci05v-R<Wr@<@SkvN(=UA1XPt#MMep_meOi}59h_0Yz;ERa`DQ$&`N}Lb8mUtE`snqAPm83GnnY^ZlESKX%8@dc5aX_<9^uk+!3lR8eB|(%q_p7>g<KcB}ybgkwye2vPG&JodyCajoY%bGn+ZgY+Q?NKc(kq<~7YHmrfkVOegg7t&1&UR&noG+4LgP*iRx-g~wx#`Ho;ho?n|xbbm0jozaX+*mvSh^O(js*F2?i3i#wbGqWNpN5d=c5)^NQ!gxN1i)$n-1GTv5Fa)55k3S4Xd%v?p;|Ce@;UR?@~`ySo<k#8PZ14<zP<5rHIa6;7Jh29hhFiXL1~77iRH0q9&p0{J{#__%oqN;DD`P+%yuQ9r=bv=nwGnq!3mv^`bMDhKwqh4mNoGXMTOyaY2Ja}VZ5kmso|)VNbBEO3x3-GdVVhXHy}wVs1<7<1EBq7Yb04P-tN+JlE!*dvfUqjB)1U`{a2dzu=vc99H!Q9qJccbPQn-;g_Hw}PP-9)J@%TXwyZ2VPBXX7hyMzQg|ZcOLhoFg<oe&wpKSSJ^We*rwy##oj_b(OsuZAnzdPLRXrJWDhe)RMK`GAM#r>_c(6c2NfDd3h*RhH$DFQHo&o9nr{miZAW+G_A3mu<x_c~qTAApDbJ|(OZ^z!ENgxf&Z}gWvKOZscbkI~$|>uti!x2ZM%aRu%Us{Yo|gSnZD<u%`tS3NM27_O9B3eFosxbE(5GFZi#>fei~^@nV1b!00qVKjYkQjJX%5B^#*I4Y>WHh}pl6-MhN;ttOLqUgYUf`NT=rTK)<Zgf<Es-i`<{{*aTDj!Qj$5(Ru`^FY*#868ccQ=1oyOGXK(T6IA`STg92gxt-qYJ&1e`vOvXidkQZt#5nQiU%trdf^a^AS`Q<iQ",
  "4>0J(?qS%qAsKa%GW4&T`O0RY8w}r7EFCaCdvhf{@#IvDs|!!3^%6K(y{{tXD>*J`zep!4a!u|xX=6uW=>OMIyB8nAd8-a#gSS_T@e36qs>d5*Ooq{~Y&ItX*y$7<dqisDqMG@dvZ6qXCz8iUCFr8uEP#ThXQHBs!h5Ms(pKKwQB_Ch71t?jnCDh9b(;Liy3>vi0W)onIy(2MKGZ+ea1B0Y%9(LHmk_tR|G;>w4?m%KYK{{bEqjx8h7Z3G_~<TZ<~TBLH{#TbYco{vf~3n6iSlZWZzJu!X;-FTX5O&V7oqucvBnSd9$OZR<)==!^IXbv<Dig-b%)Un$CK1YzH<9@2~uu+HmR%RqP6qU{Dw!yjoY799_VT(4Q6^*%&UN_N$&#i+d>TuD{<=7x^Zl@-uh^bWz<@N5+1p@*@yh(ndIubnnIfMGHh@akyrcqgn3A2Iv2aq7gtsDDnUmU#?W}E|9=S#?D?W*T#c(_NSC7XtoDWLf^#HcB8gBvXUhESG;tUj_>TzI5#C~G&SqBu8kC{l%vrakJOMzZyUX&0-ZD#{v?Kiln;2n||8P_98*xocQHVX?XG(vJ+lZK_g0dDd7*?1}EGpII_W$c7*2}14UMKf$^4UO!c|E7%AG&V(Y>y|u>-_2NvDd_lhM=lP+pRNP4gzPKgFq2PHKSKKU+S0p8DP}9len34c<k4P80fYl`}YyMGLnS8d&GT&b7Pm%e6MynS(uroBl}?<@YL&4ya_ix&~?vV1)K}1{s9BozRGD*ngEMe7VE67r_mSQ(ll7a",
  "qd6A=jZ?r%nQ<sczd)K>aSPqJ&aNwzl@V7II<*&zp@Hzy47#(RtQ+{&dPe~JL}3O|<NhDmL0D<y)AgGE0Q{^WrB<RE*RaS5xq27sK-@2tLd51tE#APgKH|LJIaaw~IH3f%0`S&V0^5aZTjjAwBtDs+;a1Fq?|AwZYqcc)QjLc8#h4=6B&<ZJOqLlwSXN8Ok1^ahnY4p8H$%q`*c`ZK%WDFtw~4Z9#&ln{`^akHgsE>&#`9*9uv6edy?{Z+n$FcsN#p8EId}<VR04VxoBqenxv4$lqbL46@AVe%jRa6{WUYC}O#yNe3|oAMWurU-vbcX3g20E1YfVA0Glec0*%=eb=T^Qs7+8I`jb;M3x>LqB(_rQ9&PSc-xZbWDO-Mn-9%PsJw7_}LyV6MHxdl*{6kqFzOvK@QUt_wrSZ1*ro$ovS{R+-ZSX8B2lTNq3Rw?SOt8-d|0y6<(DmEbM`x@z#w94aUV~razzctuO!twFZFdHPcRj$EfUl9#{R<C*gej!+xjOE2hLsh_HF(?i5$`}?M{Ryq-TpXc?N$e4SFe$(t=qf<~D~7y3mrY&<5v$lHX@8H_AQS0Lq&WUa?^uM24}m8g%J?ec&Mqcx"
]
""")

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    secret = _open_secret(ITEM_SECRETS[index - 1], "operator-overload-workshop:snippet:" + str(index))
    if secret is None:
        print("  [error] Could not open snippet data -- contact your instructor.")
        sys.exit(1)
    return secret

def _find_compiler():
    for c in ("g++", "clang++"):
        if subprocess.run(["which", c], capture_output=True).returncode == 0:
            return c
    return None


def _compile_snippet(code, compiler, tmpdir, idx):
    src = os.path.join(tmpdir, f"s{idx:02}.cpp")
    exe = os.path.join(tmpdir, f"s{idx:02}")
    with open(src, "w") as f:
        f.write(code)
    r = subprocess.run([compiler, "-std=c++17", "-o", exe, src],
                       capture_output=True, text=True)
    if r.returncode != 0:
        return None, r.stderr
    r = subprocess.run([exe], capture_output=True, text=True, timeout=5)
    return r.stdout.strip(), None


class ActivityEngine:
    """Core check/passphrase logic for Operator Overload Workshop; importable without a TTY."""

    slug = "operator-overload-workshop"
    title = "Operator Overload Workshop"
    description = 'Nine compilable C++ programs that teach operator overloading from the ground up: operator== compares contents instead of addresses, operator+ builds a new object, operator<< prints through a free function, operator[] reads and writes through a reference, pre/post ++ semantics, and explicit operator bool. Predict the exact stdout of each to build a precise mental model.'

    def __init__(self):
        self._actuals = None

    def items(self):
        """Return the public snippet dicts, in display order."""
        return SNIPPETS

    def compile_all(self, compiler, tmpdir):
        """Compile and run every snippet once; cache actual stdout per snippet id."""
        actuals = []
        for s in SNIPPETS:
            out, err = _compile_snippet(s["code"], compiler, tmpdir, s["id"])
            if out is None:
                return None, s["id"], err
            actuals.append(out)
        self._actuals = actuals
        return actuals, None, None

    def check(self, index, raw):
        """Check a 1-based snippet's predicted output against its actual compiled output."""
        secret = _secret_for_item(index)
        actual = self._actuals[index - 1]
        correct = raw == actual
        return {
            "correct": correct,
            "actual": actual,
            "feedback": None if correct else (secret.get("wrong") or {}).get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all predictions are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(s, index, total):
    print(f"\n{' ':=<70}")
    print(f"  Snippet {index:02}/{total:02}: {s['title']}")
    print(f"{' ':=<70}")
    if s.get("platform_note"):
        print(f"  Platform: {s['platform_note']}")
    print()
    for line in s["code"].splitlines():
        print(f"    {line}")
    print()
    print(f"  {s['prompt']}")
    result = None
    while True:
        n_lines = ENGINE._actuals[index - 1].count("\n") + 1
        if n_lines > 1:
            print(f"  (Output has {n_lines} lines -- enter each line, then press Enter.)")
            parts = []
            for ln_i in range(1, n_lines + 1):
                parts.append(input(f"  Line {ln_i}: ").rstrip("\r"))
            raw = "\n".join(parts)
        else:
            raw = input("  Your prediction: ").strip()
            if not raw:
                continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return result.get("canonical", raw)
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print(f"  Actual output: {result['actual']!r}   Type it exactly to continue.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    compiler = _find_compiler()
    if not compiler:
        print("error: no C++ compiler found (need g++ or clang++)", file=sys.stderr)
        sys.exit(1)

    print(f"\n  Compiling snippets with {compiler}...", end=" ", flush=True)
    with __import__("tempfile").TemporaryDirectory(prefix="operator-overload-workshop-") as tmpdir:
        actuals, failed_id, err = ENGINE.compile_all(compiler, tmpdir)
        if actuals is None:
            print(f"FAILED\nerror: snippet {failed_id} compile error:\n{err}",
                  file=sys.stderr)
            sys.exit(1)
    print("done")

    answers = []
    items = ENGINE.items()
    for i, s in enumerate(items, 1):
        answers.append(_ask(s, i, len(items)))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
