#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/bit-manipulation-re.json and re-run the generator.
"""Activity 3: Bit Manipulation Reverse Engineering


"""
import json, sys

# -- Crypto / obfuscation --
_KEY_A = "872add5bfb97cc9dcca2cc6d4c9d8c4c27e0e6f5449ae752c8a06032a569cddb"
_KEY_B = "c239cb4f76dc22f4fecbe550ad4ed962e8772580c996ae075cf67e7d94596ff2"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "daa16a7c638cd673486c7c84dd9d1f7e4d768e134d1c3b5b669feea1bd7d0f04c07e0f538c2977e8c7b29f5baa03c85469261dd15ebc7560ea364916d168"

# -- Public questions and encrypted answer/explanation material --
QUESTIONS = json.loads(r"""
[
  {
    "fn": "f1",
    "prompt": "What does f1(24) return?",
    "hint": "24 = 0b00011000. Start by computing -24 in two's-complement."
  },
  {
    "fn": "f2",
    "prompt": "What does f2(24) return?",
    "hint": "24 = 0b00011000. Compute 24 - 1 = 23 in binary, then AND."
  },
  {
    "fn": "f3",
    "prompt": "What does f3(0b10110101) return?",
    "hint": "0b10110101 = 181. Trace the loop: each iteration clears one set bit. How many iterations until x = 0?"
  },
  {
    "fn": "f4",
    "prompt": "What does f4(0x0506) return? (give the decimal value)",
    "hint": "0x0506 = 1286. Trace each mask+shift: what does (x & 0x00FF) << 8 produce? What does (x & 0xFF00) >> 8 produce?"
  },
  {
    "fn": "f5",
    "prompt": "What does f5(6) return?",
    "hint": "6 = 0b110. Compute 6 >> 1 first, then XOR that with the original 6."
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "f6l_=<Ill6)`PlW*p37kDV2S=Dgi|o82UxOD~{y8NX~$tSM?U;MGC1nW==p)DYnH@zuROFbPy0lrkSNT9EGG#RUw3vERYu6?swYXqcM{NfNT;d;eo!^IxJCI(oxDV78ZvEZnm;bIw13>M0~7hlWGF+ii)#LSHOtWG8KhHfSg8?FTKkh`{7A|YP7;9FRuPpbVSZxkD=J|TCU&(14FQo(l!EDAk)#$E*;^*Ji0|@m^UPt>T+UaS(31^n7Ow~w8FX9R5$(~mGT3*ML!2a?D^I!iledcKGcNmovn@W>Qglt294y|jo){WINN&fu|TrNRfkA1f>?wG*xfffCu0=p-zb)GP3B_2q<a(9Rsy9FGL3>`fS3f@)@NTKd^!VI8lekUC^Tn1f&V=rZRfnd9U5wmAJfgb$W7dH7sCfwH#oRnc|lgFQzmLh6bK9VhnH0}yFP+aq^N*!FuE_DHwdiSA95<T!)$CPZR^=I=MD>GA=BW#wl+;TL?uwYt!4dhKFrtPRRDJj+nxwk=}llt)M##W3$z_VUWqQn+qrTT5ZD}&7o_69txNay*2Vf7E=giOp_yNtH+cY+sKDno;Aj&Vn?#3&O}12D>^vwDQWIFM&wjAv>iuL9MmKfN#g$Vs=sCr@1S7DlaR",
  "vLw(IB`6bB;Yc{v-OxGUpJJYmLlZxO$#C=jNrmw;k(QqK|51#vM~~Y9e*+pMjNA@f=R<Jw>y1C)AAD|)Y8N3A`JVmBk6eU|Z1O_c#wsl!+#8+j2)|OjqO3uqslY4Zno5&_M+QT|`SYxByS^SKp_%9#ULO^z7TfqbX}10@K@3!`C=B|5uOhj3Lr$7-QLW{)i%WaxsdIf7$U4c@n>7{z$o?m~ly}vTv>@g*S|O<#Mi-`#gZLu=KWcuG2Qi0@=&|=<=lFAx;~7|I-mFct#7Jp7FltqlOWrEVMli0f$sz!S6MVO4^YHwqUA#Zen-Y~jL8feKHC?jlGRDV5sEK<P)r`ZR)Lfn9sKZOZ5l;^+vq;yW5a{LPk6w;W3Xr$=;$~-06KiX|9x`le=(g~-y|%XNl|9WwxP;6DK=h16Wy42j1_=AiN=AoVOrSdEOT?5{3%K{C{qwq2JfSD(BGsi$ME;<+6ckw0%bziNskjmUz)a?@MhIo|CMv+b2!o8!%mMy(tI;#ClMYO1{QMK3gJ`U~KVjd~W9gYoqN*=M?scueK4>d_(%DJS",
  "$!y7?R!%iVU&z40f-%&%0q?7JPbM|LiN@qK4wyM@9#jj`n3E_ibkZj_$*w~JE!Tw+z{7G2uT-pF8Sb)}SnSOJ$yI)xA7+s6i}Jo1y-cgO<6B!Vca)T8E7LMn1E-h*y1I`n5)qiuDA#704OlQW6a^-j<xQJwt+HuiJK!kN&XYBSI4dBX8-$HCw-_@0_%aYn^;aZ_pffpf^jR+rHqa9U``C5yb@~yynBFt{FP1D9@F>&RM*^_4aXV$uP)szUq9Wts;h$p_D!>6R>23Tegf7JwiJKR2bJzNdlMm&qc3Vw+d*cT^Jh=@V>5^%*nX&}2as}M7{%}5!85arZB@Ql%jSzyKCm;@Aw+0A&6scyh+aEiW+;XPcf8@#%44WN9guSX|4`BrQt}|A|kd&0ch>R>wdJ!q?OBy3DX67H2=_xv*B$K*-1v^|jWp3t{HhTmH>+3&ao+fGcKuVF5L_rvgxH|k@!v3=F9T)iN(pa#T7_A$EMo6I{<o_sspxLN$ME{+#aTK2d<(0|cmhi;bXBWa|)Ae<tQ~WHOmUiYR&IKRAb@2VFtQwh8h`;o0<EEOnHnDoaTp#IskJEa702~-WOv*d$0I=N{tO4W6$-_yW<$?",
  "G4RvOL=6x{Qm4PH@PET`d7+2bG=UDy4qXA@9P{|j^Czll;tW~GAy~XM0_(ll_kJS&<wy}oz%%`p1OZ)_aI*3rhw3eoaWe`5chae9za;9YTA=rQwuZ(d2aJ-1$v%5$$qYf^?+|cr93-k_Ti|^*hK%3M2I2*a6wO;jzXH4T_7?MrfcS~n?`iisg{;$92{RHggiJL<3NUD}Gp*g66_8p~)^tDA0J>g2taP~yOEVv6$z-F0x6(a3uJCx&OqcY<f5=tb%!*n7$xe*6v1)1GA_V=9UAOH{U<w$>H>_6wHe}=`2_G&F5%>%UCL(g0k~!LV$ocxFfN~ruwnAn+wY39bF?prK<5G{p$@`;^ko{uXc(%K_+cz0IdLdZc>Utq0Jm^kw@cDYG>p-ZUVRgDE6@gE*D}@twx<E_s$~J&bZWmP1OHck|MicOZWz-4WZz+8#4@3tq{m+8-WUy{7DE-voj0`74t4r7-j_pSR0hFm!t8H3}+Y%|yw$76f*-_MRFDU4hOQ?`z0P@BOZ38{J-VLwY^$4iqqdCgt-;Tr",
  "WSA+lFeROI(y5D+K4O6LYbc^u)odiT+Q)awFVL)kiGWNgvU$B`IoeMA|1XIJstd1XPfB~7=8y%<h(*kN>RpcqF@-b)*|U2$z<;ez2F<}Zn=lX^MLaH2V+Z=>l}Iv!0w>-BXtqbSfxMVFYIGyCU>{h5KG&{whhq|Ie5P`#g0?HtZ}|Wzx1^3~fk?`SCnUmd?_f<&X;cY;yBU2>DiSlYDg8RnP2iM>kxHKUdp&H~YhC4_i^f&Ml6B%hT$l|Bb7$>B?0lwRn6R!hGL{$iWspu&ZY9g-rXg+Wh&F^b)fTMCO7;vuuh9W&OR@6)yeneMrwQfxaI5z04~Dzv#U7jlHMDXDn8w-4^m`G9+jv|uC6E#$=REUmBYa7^z(17sgr5*TewPrPx)&75eGB)Km-eI)@%kE`{XiWm{JDR9B1;3+cprb9MqZSW6Um-`Pr}uz80Lm3)HXLo4>zt0MT3Kzn{O-sv7hi%78dHl>#&&Wl0!(PIiQ<xbw&N=LWD>%W~DZok$>Jx5u2ke(&AVn?fMEBZ!%a-8h=oyGI;"
]
""")

FUNCTIONS_TEXT = """\

    int f1(int x) { return x & -x; }

    int f2(int x) { return x & (x - 1); }

    int f3(int x) {
        int count = 0;
        while (x) { x &= (x - 1); count++; }
        return count;
    }

    int f4(int x) {
        return ((x & 0x00FF) << 8) | ((x & 0xFF00) >> 8);
    }

    int f5(int x) { return x ^ (x >> 1); }
"""

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    """Open the encrypted answer/explanation payload for a 1-based item index."""
    secret = _open_secret(ITEM_SECRETS[index - 1], "bit-manipulation-re:question:" + str(index))
    if secret is None:
        print("  [error] Could not open item data -- contact your instructor.")
        sys.exit(1)
    return secret


class ActivityEngine:
    """Core check/passphrase logic for Activity 3: Bit Manipulation Reverse Engineering; importable without a TTY."""

    slug = "bit-manipulation-re"
    title = "Activity 3: Bit Manipulation Reverse Engineering"
    description = ''

    def items(self):
        """Return the public (non-secret) item dicts, in display order."""
        return QUESTIONS

    def _options(self, index):
        """Shuffled display options (answer + distractors) for a choices item.

        Order is deterministic per item so the correct answer is not always in
        the same position, but is stable across runs.
        """
        secret = _secret_for_item(index)
        opts = [secret["answer"]] + list((secret.get("wrong") or {}).keys())
        _random.Random(self.slug + ":" + str(index)).shuffle(opts)
        return opts

    def check(self, index, raw):
        """Check a 1-based item's raw answer; return {correct, feedback, explanation, canonical}.

        For a concept "choices" item the raw input is matched against the
        displayed options with small-typo tolerance, and `canonical` carries the
        exact answer string to decrypt with; for other items an exact match is
        required and `canonical` is the raw input.
        """
        secret = _secret_for_item(index)
        answer = secret["answer"]
        wrong = secret.get("wrong") or {}
        item = self.items()[index - 1]
        if item.get("choices"):
            matched = _best_match(raw, self._options(index))
            if matched is None:
                return {"correct": False, "feedback": None, "explanation": None, "canonical": raw}
            correct = matched == answer
            return {
                "correct": correct,
                "feedback": None if correct else wrong.get(matched),
                "explanation": secret.get("explanation", "") if correct else None,
                "canonical": answer if correct else matched,
            }
        correct = raw == answer
        return {
            "correct": correct,
            "feedback": None if correct else wrong.get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
            "canonical": raw,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all answers are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(item, index, total):
    print(f"\n  Q{index}/{total}  {item['prompt']}")
    print(f"           Hint: {item['hint']}")
    while True:
        raw = input("  Your answer: ").strip()
        if not raw:
            continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return result.get("canonical", raw)
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print("           Trace through the function step by step and try again.")


def main():
    _banner(ENGINE.title)
    print()

    print("  The five functions:")
    print(FUNCTIONS_TEXT)
    print("  Rules")
    print("    - Do NOT compile or run the code.")
    print("    - Trace by hand using binary arithmetic.")
    print("    - Wrong answers are explained and re-prompted.")
    print("    - All five must be correct to receive the passphrase.")
    print()
    input("  Write your one-line descriptions for each function, then press Enter...")

    answers = []
    items = ENGINE.items()
    total = len(items)
    for i, item in enumerate(items, 1):
        answers.append(_ask(item, i, total))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
