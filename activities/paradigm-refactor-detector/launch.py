#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/paradigm-refactor-detector.json and re-run the generator.
"""Activity: Paradigm Refactor Detector

The same small problem, solved two different ways side by side. Compare the consequences of each style, not just its label.
"""
import json, sys

# -- Crypto / obfuscation --
_KEY_A = "12ddc0572038f089e5ac178139ff6a2fc4b49ef7c2b8714e8e635c0acfe06a3a"
_KEY_B = "5a76c78a1fb2c7e9d4977b498c89f6d43b691afbd95a43dc894e8471a35a892b"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "25feb1cee85eba62d4fafca91a10040c7c3c01c7d3cdfd83afa2f6ca870f4417afb7e445e5b1c5137a1059168845978908479c69f23ea4310fef0e"

# -- Public questions and encrypted answer/explanation material --
QUESTIONS = json.loads(r"""
[
  {
    "prompt": "Two versions compute the sum of squares of a list. Which version can the caller run without any variable ever mutating? Type exactly one of: version a / version b / both / neither",
    "code": "// version a\nint total = 0;\nfor (int x : nums) {\n    total += x * x;\n}\nreturn total;\n\n// version b (pseudocode pipeline)\nreturn nums.map(x => x * x).reduce((a, b) => a + b, 0);",
    "hint": "In version a, does 'total' ever get reassigned? In version b, is any existing variable overwritten, or are new intermediate values just produced and combined?"
  },
  {
    "prompt": "Two versions build a list of even numbers from an input list. Which version's correctness depends on running its steps in a specific top-to-bottom order? Type exactly one of: version a / version b / both / neither",
    "code": "// version a\nvector<int> evens;\nfor (int x : nums) {\n    if (x % 2 == 0) {\n        evens.push_back(x);\n    }\n}\n\n// version b (pseudocode)\nevens = filter(nums, x => x % 2 == 0)",
    "hint": "In version a, if you ran the push_back calls in a different order, would 'evens' end up different or wrong? In version b, is there a sequence of statements whose order matters at all?"
  },
  {
    "prompt": "Two versions compute a running average. Version a is a Stats object with an update(x) method that mutates internal running totals. Version b is a pure function average_of(values) that takes a full list and returns a number. Called twice with the exact same input, which version is GUARANTEED to return the same output both times (referential transparency)? Type exactly one of: version a / version b / both / neither",
    "code": "// version a\nclass Stats {\npublic:\n    void update(double x) { sum_ += x; count_++; }\n    double average() const { return sum_ / count_; }\nprivate:\n    double sum_ = 0;\n    int count_ = 0;\n};\n\n// version b\ndouble average_of(const vector<double>& values) {\n    double sum = 0;\n    for (double v : values) sum += v;\n    return sum / values.size();\n}",
    "hint": "If you call stats.update(5.0) twice in a row, does average() give the same answer both times? If you call average_of({1,2,3}) twice in a row, does it give the same answer both times?"
  },
  {
    "prompt": "Two versions build a greeting string. Which version most directly describes WHAT the final string should look like, rather than HOW to build it piece by piece? Type exactly one of: version a / version b / both / neither",
    "code": "// version a\nstring greeting = \"\";\ngreeting += \"Hello, \";\ngreeting += name;\ngreeting += \"! You have \";\ngreeting += to_string(count);\ngreeting += \" new messages.\";\n\n// version b\nstring greeting = std::format(\"Hello, {}! You have {} new messages.\", name, count);",
    "hint": "In version a, do you see a template of the final sentence anywhere, or only fragments being appended one by one? In version b, can you read the final shape of the sentence directly from the code?"
  },
  {
    "prompt": "Two versions configure a Widget. Version a calls setWidth(), setHeight(), then setColor() on one mutable Widget object. Version b calls with_width(), with_height(), then with_color(), each returning a brand-new Widget rather than modifying the original. Which version allows two callers to safely share the SAME starting widget without their configuration steps interfering with each other? Type exactly one of: version a / version b / both / neither",
    "code": "// version a\nWidget w;\nw.setWidth(100);\nw.setHeight(50);\nw.setColor(Color::Red);\n\n// version b\nWidget base;\nWidget w = base.with_width(100).with_height(50).with_color(Color::Red);\n// base itself is never modified -- each with_* call returns a new Widget",
    "hint": "In version a, if a second caller also holds a reference to the same starting Widget and calls setWidth() on it, does that affect the first caller's widget too? In version b, does calling with_width() on 'base' ever change 'base' itself?"
  },
  {
    "prompt": "Two versions compute a factorial. Version a is recursive with no reassigned variables. Version b is an iterative loop. Which version needs a mutable loop variable that gets reassigned on every pass? Type exactly one of: version a / version b / both / neither",
    "code": "// version a (recursive)\nint factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\n// version b (iterative)\nint factorial(int n) {\n    int result = 1;\n    for (int i = 2; i <= n; i++) {\n        result *= i;\n    }\n    return result;\n}",
    "hint": "In version a, is any local variable ever reassigned after it is first given a value? In version b, look at 'result' and 'i' across loop iterations."
  },
  {
    "prompt": "Two versions compute the maximum, sum, and count of a list in one pass. Version a uses a single loop that updates three separate variables together. Version b chains three separate library calls (max_of, sum_of, count_of), each over the same input list. Which version makes it easiest to test each computation (max, sum, count) completely in isolation from the other two? Type exactly one of: version a / version b / both / neither",
    "code": "// version a\nint maxv = INT_MIN, sum = 0, count = 0;\nfor (int x : nums) {\n    if (x > maxv) maxv = x;\n    sum += x;\n    count++;\n}\n\n// version b\nint maxv = max_of(nums);\nint sum   = sum_of(nums);\nint count = count_of(nums);",
    "hint": "In version a, if you wanted to unit-test only the max-finding logic, could you extract it without touching sum and count? In version b, is max_of a completely separate, independently callable function?"
  },
  {
    "prompt": "Two versions look up a user by id. Version a returns a raw User* and sets a global error flag (g_last_error) if not found. Version b returns an optional<User> that is empty if not found. Which version forces the caller to explicitly check a return value rather than relying on separately-checked hidden state? Type exactly one of: version a / version b / both / neither",
    "code": "// version a\nUser* find_user(int id) {\n    for (auto& u : users) {\n        if (u.id == id) return &u;\n    }\n    g_last_error = ErrorCode::NotFound;\n    return nullptr;\n}\n\n// version b\noptional<User> find_user(int id) {\n    for (auto& u : users) {\n        if (u.id == id) return u;\n    }\n    return nullopt;\n}",
    "hint": "In version a, could a caller forget to check g_last_error entirely and just dereference a null pointer? In version b, can you even get a User out without checking whether the optional holds a value?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "$;nVN<g<%IBT=5<FPC=Ml+KDLUhsNKSe1O_r%NI0+L2rL->k6zCLiBI1}^l$w|YY;Cl*Gjp1UV5XsGol?|jA&)hOvzg(6jOBaUJwNoRXLRp0X0<A_c#IJ2OyN(+ka$flQIZH&<nokr^u(Th^S*kU1<kMBC2m;YuM5wj$O1TH&-ypo#w%z{I=E$L01sk`a|H*qF#eGdV#*mC>z70|I%)GOg#f-safv6}Jb?CK=dzDT~03C^xP5l|LSqftv2?LFqp3r=Pj*w>JB1%%-CzVbcEi~#}jJ$1E~xyl0}fRAQslom-m6eg#xlPIqma)Wsd6D*=ePD=F5ol?mfP}M*ZJX%AhRJbKGY5O^Lb1sQlyp>Z#^p~zg*XdFgZmw(@_=+rQZL1&8I!}7?-a5pzP4vcb)Pn`CVeo^+C5UiiKdJHoV535QyF6msYqeO&fXqWXp~jThQxRiLe#IJPpe$o4?N3tRk&V9HvAWFEvMH2AaF2}^hjGUIjn&_sAg@G+ou#s(dG)rh4B!aq?O1)4Q_P4`Z5f{t+#%_mDfg)|`xZ<`odAZDRIss`X;SL?ob>Q>0<d#0Y9^y96T(&ZR~DwDtdW-E->$RkpLQw`dY+mp6TH$7hy",
  "lu_a%P}U?kq)f_12n{x%Db#pV;KR>yTeY(d@z(_x`#sNEN$F8TyO0lhG}df|Cm|AO*l*^O)G(#oko$$Fe?_mYnzpg=gG200TEb}>cXR)gMO!Q9Kq3q%Y+Qgdbjv9YSTz_h8P@n_Rt$XHPR&9te&%oZM9d^IiUIh(kIswf%DlkH;DnBNI48)%PMg71iHqyBIIsa|IsK5Q53AJ)<aq%KmET8JJUPqa4=mahyR47<Of1%wtPm>H>4PPzB<KbObAJ9E`@{W9>vNPBs-5soQFUdeb5@ZFp;-Ym=EA06$sB7d(!~_Z<6e!-B}Zfcj%N&ofHrfhyg=Q?4aUx+hIke7)J*>zC9030(O<S=GTcsUipuBeAujv<bOk^5$pB$&tj;NxnLinte8e_E+RDyJ&Cp5id#P={sB23nI2DP1!t)M{aXNT28z~fom)*#|cGAULOK?XtS-6MN_QLEjoaoVwArSTlmxsr#U;R3U(gVU{=&Cu2<^3T9Q#VK=2vnXccF*jx#p)Q2$Ba7xcX|=`ydGgbYG;_D7C@uWZ0y{66V4jCOMKmCHgw`BS;emC7*xoC%ta0_pb22*Dcjgl&$VqRn~LV7OuqD*AM0$JC@UI#Glc_F>GX-Y2B(h;pVjEM2mw^RFBepk4thvECejd(RXV>nueUaELqQMTd$kZ+s{",
  "?Aq0@#v1;Gj_;s$lxBl}n;j4W&X@v-SLlFW3}HOJu<46PXkRLLst83N9sxOnq-&C7ruSqQ+<hERxXXdDhaOAi&Dm{(KRy<K_{79HYyJN4@te3Qh@9o|;J+IOErPrx-8(AMTBBT){pR~Mgr0U?@sox2Wz#GFTFL(M0UAA#J>d{+gN+5usPiwYg%Y?QODP@u_ttAR*~)IDB$hMrDGNsCB;oZRE^RPJX(oGH6p<76CO`1Wo+o`+n)j+)sU{cJN%uZB3KnUW$MtQ8$+S1^=dLjX?7F%O4iEs~D_>A?K~k`H%jLT)6K6Dg9R%{yJ?;4;HZ7=;tycr_Ccr^}(RpOd6<g2gaRGY{EHnG^%CwkvV{mtIA}<W8T867A*mIyj4)9N0m8?Cz0B7!>RI3KSvpZKV&^~aG-oVZt>S!AP<k^uZTS7cGB}e&xj0^&9y(93AhY;`M(XbvIVYP!$DgXDm*vGTJ^rmplti+@E1knu*zbK!l47V~^a!a+$Jq7G;7NjWNWC^PRuUa-yFQve6dgVBstV?s>m-5ka`2oy%3JEbuDh`MMP6KMXtvM6|4!k8g7%q9CQdTa?_)AuR0jofFyh&!(o!E%q*YYiT8A|s~g^HA10i9yM)xwIo8+aJcXiK(ws5s_B=|0~#iwK<|EGwwkICdl}*_B?0^YrEbkqyN;?XT17|2+%3KaAA51pMI-yu%q-+Z|#bo1w-2gAtkN$qUk;?;3UZif1&1(cas%s5$T306VFcaCL7",
  "fz=txD-BpY8s0B1aj!~E>}&=p9_{lRh!Ed>I9hejIo!)e3cwT(q1ygGE_5+2iDNlDxE>J9C!CkK9GAx671r~`Y<f_mEqU4e#ChOU#5N&@(>J=TpO4nMN|3YM1fEYa;_*SN2m8#7HC{s0Ask1e{RkV{4<Y}>c*Odsbk5N9;!X2U*m!CEl>RX4WvPmK?%hCItkq6etQ<8^*3{D4-G#RYJ4L_RGyet}vQyf*-Z2dPIdUm=+=y6A54=+}xGmL!cG-Cdh}S!DEibwxG?3>+7}bY9tI=p`VRrSGy%)H`{|U^jzV_KJ?%B*;PX3XGxmt@(TL2)dOf5<@O(=2cN{mD`k1+b1{+6Q0|8(SqD2{K|I3REmSakZW*y^VYn|b@i-qQ)ZGaiadh*fhzj~?~39s>;V&y7u-jSJJ~ZI=^{MXp7&wiZd0?3UXS0^wXwn+p|PiOso-%u^u~tqGN|7$-OYM6^$;D-<Wq!)nfJKs<kq`}Qd8J{^NPDz?e}x!0(Zx{*$N=Xfzi1)=v{2*WLP@GD`T0^DVHmbMytgcTnYJ#$l;TRE95n)$`r!l~2f^`Sqn!Y4IBrM-AV2+_KgtXOxQ6Mjq~0@Ms{=b>V2c_#m`)+akYcHEBhVR6)_Fa#*K9N;E<w{^4JML48pad(>`-4n*=)tniIhyp<bZ>LG{ENb``L_W!n#1;tRWGXj8gKQ>;Tm",
  "EkUS6Uuucw!Ozbt5dz1y6%>;p?4(n0o!~4L(S6tHRsOB3{y_Zry3;Qfww`AB@eRy@cMXyt@qgoZHyhDj?m_2BbnCo5z;hC)6upx2VF*6~F)%fXwbiXFCYrP|t@5TJnG*9M#r9k<N!Ze*9(RNZd3Z#vt*i{(+X9gn+Ceh($HX94!muW3yMPa4QxdXpJ$1RU8)iR1%Mo4_fa}pXF;?PaAGd$`IO3G_2H<``GmsCFmMo(oP!?$Y#Q1OeWrd^3(k#ol1PIB3cxsV9szoDX4f7)BPEBr{yRYiz@4XXQl`^<zVGZ=fUcDI}*eyNH__3kg!5+ef{-o4F@jo_wq0Ml=3E7#+Q1p5f<84(ke-iWTz;f0vCi~oAXRQnoe{bHPqn&Vi(-LnJorV6w)=<Zzpgcpl5N<}V#W<^xs4DnXCG9{fw5<(>Lk1{f&KkM-&_uUP4YALyl0?N@+1;7@o;WpUU0e;q&T)TX29x<jx+z@4=5RMRNf9@UZxxUMcak;+PD!H36{j!oWyZYLl}6?@fYRX1w&&ucUN4y2V-wkv?GCt&{5T*uu@}-wlBk^1AuQ18Yq}4mp|B>rfG!)MDM`DB+l1#Fqmsobh&_&A0!lunnI_drWXWv9Mri~xcF$5a?{7H%{pm$Td_@_mXB841JNvq^rF7=`Hc#ZfvtqBPoD3}_583hSbqgfGra*LolAcLn+Yl(TsvG|~^<$^`RnV{9##|t=AtKQhuIMNjD+qs<+u&x8h%zbYmj",
  "@x8#_;;2)&{ec+k&sHCk%^yCvk2MUUpJ&U?&FA7_PIeoKpPZ$&>EP!)!K^<jAIp0-el%9n7|;MRqG=!Y`Nv=Neb+yE{7W<ZhHawXyvE`F1tftI!hW~6LA;#=(=nN}Rpw+IDuaCYOa9s<PN~?lFO()!m*e|b6$7++g?~Lc1x4n^JLxXB4&Uruuk=Ie3xH~B1b%#ebTLCZ#P{xHT>f|9>@cic_0{}=X<W<&VWb)?!fe$VMwPK-DwZY&aDGo*-uQ+`Wpr&FNp9NLc*8Nl`>kq#x@QN}zPS3D3G^cYX-MLn+AKW1HL%x&R1ir|P%MAvRbtQr_nV040|QS86Y+}`5rOFH>qVeACkCHXEYD0NTQHvgIAqC%%#4amYm?f}g(IdKCE*;QY5h`(H7Y3W;d{(YENIg{Iw9M<E<bN*wtNqS=M5y{85J6j@<I#Q!@PK}Nz5<1M=rDzYVWdyx%sd#f61*Iujhgr_}G>6vtII$4-3(5UOe(?V8<XYD3y{MCDA`%Iaov<dZ2#uRgJ{Mj|Uj76jqN)cP_QdSLCmJ>9XD3+S6F4*QVYNQ*4Kv$Ck`IWwTANPgoxifQ)?GY3qTN6_44b-se0Jzxfh|*y(8`=JR&ZWkyHVXaxT7_f*L",
  "AYW9vGjK;9?o!SF>a`W7W5j?KOZXf%E$Xz$l4LBGNXh&94)!NbLS%Iu-uf^1d496wXS4ie%1Ba4d;$i)7LRp*CY(##a%M-}wEcCLiRen=TdVS##M9Ook&!Ci#Z^B2+qS?;u!u18fsL&7z6DeZ0-SApd)?B*2g&V|b9d24o};wbGefS(8v{l(U9~ALgL?*YM85OEPj}2fiLOi;A$blQCW!8y8k?gfOU@|KrtGffG+MY4?EwNu1T3MyKg`o=HsuRHQ+5wAg{mf1+;937daub`JRplQn{%uXT~6SlOEQ+cTX6=iVM|!Yeib(}N8NT%e`g-Mdd7jK9K8IDqZPy~wN`(aYj!)DwSu#XuE^a6y|bhAVjI91>T`KAn5IX`<R=i!Q1n*JNCR4T#w^8`<4;tHg+(QK>)j(<=nRqI#+5qi$v^~R!jS5tB(qEQyQE<dmoHI0)yb{_J0qd7@^w^Y8MOg0^qO|MN=bTJxGkckeMm#AzRh~(0H+UKtV~Mw;@?g&FJ1$89NoViE<@5F<ky05&F;O~Z;_Fk0~FpDaldmhuPW*ZGVPt~rI9vJM%yL$rs)2Rgd^(`X|Ez{&c^$(k(3qHkwK#`0f?0aW3qUGjd~AAR6?S{QI6*7iw3zxvzd%O-@*S~bQZ~K#|AR4?|3WH2s)(53#zW^#pb{~JdhE|0sIbuzD-06dxgfTWnxRVsMPq6VF49EyO|sYP)|%V%-*7l$ZfDM8ffJ2H?ZG|KpivMiT%T*H#^|-4e`*ww9R<H?Ah$iAp{XVlK",
  "oaQexaw5Cmt1|<^^C83^c`k7M$-8o?Z*t@q0g^}|TXI(L$6Wr{OCcgG+CXQv53ksI0L`$n<U7q(MT<HGpW$igcv*<W_dS3IIhxxUT~7pd0wOe|ZrCcfwzy(Le;jb_+12=6%a|Rj3d{7SU#f=b@brw#|13Vr9nZOmm3h7*`Md45e>6{U=7vB>R8(Pp-x<MX_77_4AiqwTZZ|qteG}l<3ey3J=;eoxx;sKn46kxWj=z(LRKqqViD)KA%sUa^38i7K040PBBhs&3+=Dfg1C+Nmr^oPP&;9$O3O*ckOK8S<4U-atEx~|sQsa}MZ8rc-Xk1(QFNmzP-%F!7*}%Uf``WZyu`i9=k!)NiOGCziL|_2&QC_cs({73)pe-emI*?mL62VV2->l6?<z2Cjj<df2z|gcET-N!~OWIj&lz(^W+iu2LU;APPJ;z87A!)h6ZCP(gpd>Y7sdyKA5l-@s4*T+6x)9|fKXe}#3m>UOd}#+gaYNb$nUI?P(KCp@GnlhkQt4SICf(YI`gbcfSFJ|g9?5zVp=+$a)}y`#q5Bj>Ci&vO;znEa-v)WNfG&ltAEBV@;Tjc=kPkr+612uZsWNtf`;D~@&rT5jpR!U@C1QN3(VTN)lsG}04v`b)Z|F1E-^2g@G9(>X`ehAM%jgre%AwVJ2xNH2vRp1M*E}h+!Z|`xpHGtE%o&w&t5n7qxA7mensPS4dYu5e9H^A?f3FO&l<di+MPMOwNi4)8q|%61g+NxZl&(xwt7ufu_0p3E27EiR=Xq9ZovazKP5fV4VWXTOE<&ZA|JrhZwJ)f<cN&2%kg1I+l?_<}dC~"
]
""")

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    """Open the encrypted answer/explanation payload for a 1-based item index."""
    secret = _open_secret(ITEM_SECRETS[index - 1], "paradigm-refactor-detector:question:" + str(index))
    if secret is None:
        print("  [error] Could not open item data -- contact your instructor.")
        sys.exit(1)
    return secret


class ActivityEngine:
    """Core check/passphrase logic for Activity: Paradigm Refactor Detector; importable without a TTY."""

    slug = "paradigm-refactor-detector"
    title = "Activity: Paradigm Refactor Detector"
    description = 'The same small problem, solved two different ways side by side. Compare the consequences of each style, not just its label.'

    def items(self):
        """Return the public (non-secret) item dicts, in display order."""
        return QUESTIONS

    def _options(self, index):
        """Shuffled display options (answer + distractors) for a choices item.

        Order is deterministic per item so the correct answer is not always in
        the same position, but is stable across runs.
        """
        secret = _secret_for_item(index)
        opts = [secret["answer"]] + list((secret.get("wrong") or {}).keys())
        _random.Random(self.slug + ":" + str(index)).shuffle(opts)
        return opts

    def check(self, index, raw):
        """Check a 1-based item's raw answer; return {correct, feedback, explanation, canonical}.

        For a concept "choices" item the raw input is matched against the
        displayed options with small-typo tolerance, and `canonical` carries the
        exact answer string to decrypt with; for other items an exact match is
        required and `canonical` is the raw input.
        """
        secret = _secret_for_item(index)
        answer = secret["answer"]
        wrong = secret.get("wrong") or {}
        item = self.items()[index - 1]
        if item.get("choices"):
            matched = _best_match(raw, self._options(index))
            if matched is None:
                return {"correct": False, "feedback": None, "explanation": None, "canonical": raw}
            correct = matched == answer
            return {
                "correct": correct,
                "feedback": None if correct else wrong.get(matched),
                "explanation": secret.get("explanation", "") if correct else None,
                "canonical": answer if correct else matched,
            }
        correct = raw == answer
        return {
            "correct": correct,
            "feedback": None if correct else wrong.get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
            "canonical": raw,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all answers are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(item, index, total):
    _hr()
    print(f"\n  Q{index:02}/{total:02}  {item['prompt']}")
    print()
    for line in item.get("code", "").splitlines():
        print(f"    {line}")
    print(f"\n  Hint: {item['hint']}")
    while True:
        raw = input("\n  Your answer: ").strip()
        if not raw:
            continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            print()
            for ln in _wrap(("Correct.  " if True else "") + result.get("explanation", "")):
                print(ln)
            return raw
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print("  Think about each call separately and try again.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    answers = []
    items = ENGINE.items()
    total = len(items)
    for i, item in enumerate(items, 1):
        answers.append(_ask(item, i, total))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
