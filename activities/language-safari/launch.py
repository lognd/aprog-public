#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/language-safari.json and re-run the generator.
"""Activity: Language Safari

The capstone recognition tour. Each of these scenarios describes a feature from a DIFFERENT programming language -- Rust, Java, JavaScript, Go, Haskell, C#, TypeScript -- and asks you to name the concept from THIS course that it maps to. The point is not to learn any of these languages: it is to recognize that languages differ in syntax and surface features, but the underlying concepts -- resource ownership, error handling, concurrency models, static typing -- transfer. Every scenario is honestly explained, including where the mapping is not a perfect one-to-one match.
"""
import json, sys


# -- Crypto / obfuscation --
_KEY_A = "0d581c562ab592607b08cf3f85ddfc59a79cd9ae6e6a9e92919c2d20547d1ada"
_KEY_B = "1da09fc549b01ef156141cd41786b30263a000280375c338629080fba13abc87"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None
_BLOB = "9b604364ba14a4b958e16e34f4ba4651f44ceb978b56ac82a94bf14d1648d7dc52c92cd5e74134b3240b96e440ef809840df664bcf1cb242822950f2265aee71"

# -- Public questions and encrypted answer/explanation material --
QUESTIONS = json.loads(r"""
[
  {
    "prompt": "Rust has a feature called OWNERSHIP and BORROWING: every value has exactly one owner, and when that owner goes out of scope, the value is automatically cleaned up; other code can temporarily 'borrow' a reference to it, but the compiler enforces rules about how many borrows can exist at once. Which course concept does this map to?\n\n  Type exactly one of: RAII and the Big 5 / Python's garbage collector / C++ raw pointers",
    "hint": "Think about the C++ idiom where a resource is tied to an object's lifetime, and cleanup happens automatically when that object goes out of scope -- no manual 'free' call needed."
  },
  {
    "prompt": "Rust does not use exceptions for recoverable errors. Instead, a fallible function returns a value of type `Result<T, E>` (either `Ok(value)` or `Err(error)`), and a function that might return 'nothing' returns `Option<T>` (either `Some(value)` or `None`). Which course concepts do these map to?\n\n  Type exactly one of: std::optional and errors-as-values / C++ exceptions / a null raw pointer",
    "hint": "This course covered a spectrum of error-handling approaches, including a C++ standard-library type for 'a value or nothing' and a general discussion of when to use error codes/return values versus exceptions."
  },
  {
    "prompt": "Java (and many other languages) uses GARBAGE COLLECTION: a background process periodically scans memory, finds objects nothing references anymore, and frees them, so the programmer never explicitly calls anything like `delete`. What does C++ do INSTEAD to achieve automatic cleanup, without a garbage collector?\n\n  Type exactly one of: smart pointers and RAII / nothing; C++ always needs manual delete / std::vector garbage-collects",
    "hint": "This course spent real time on a family of C++ types specifically built to make manual delete calls unnecessary, using the same scope-based cleanup idea as RAII."
  },
  {
    "prompt": "JavaScript runs on a single-threaded EVENT LOOP: async operations (like a network fetch) are started, and while waiting, the event loop is free to run OTHER code; when the operation finishes, a `.then()` callback or an `await`-ed result resumes the waiting code. Which Python course concept does this map to?\n\n  Type exactly one of: asyncio / threading / multiprocessing",
    "hint": "This course covered a Python library, built around async/await syntax, whose whole design is a single-threaded event loop cooperatively switching between tasks during I/O waits."
  },
  {
    "prompt": "Go has GOROUTINES (lightweight, cheap concurrent functions, many of which can run on a small number of OS threads) that communicate over CHANNELS (typed queues used to pass data between goroutines safely, instead of sharing memory directly). Which course concepts does this design relate to, and what is the general tradeoff it echoes?\n\n  Type exactly one of: threading/multiprocessing with message passing / decorators / a Python list",
    "hint": "This course compared multiple Python concurrency tools for different workload shapes; goroutines are a lighter-weight take on the general 'multiple things running concurrently' problem those tools also address, and channels are a structured alternative to letting multiple threads touch the same shared variable directly."
  },
  {
    "prompt": "Haskell strongly favors PURE FUNCTIONS: a pure function's output depends only on its inputs, with no side effects (no modifying anything outside itself, no reading or writing external state) -- calling it twice with the same arguments always gives the same result. Which course activity's core idea does this connect to?\n\n  Type exactly one of: pure functions and referential transparency / C++ inline functions / staticmethod",
    "hint": "This course had an activity specifically about detecting and refactoring functions that quietly mutate state they should not, in the name of a property closely related to 'same inputs always give the same result.'"
  },
  {
    "prompt": "C# has PROPERTIES: a class field accessed with plain-looking syntax (`obj.Name`) that is actually backed by `get`/`set` methods running custom logic (validation, computed values) behind the scenes, invisible to the caller. Which Python feature does this map to?\n\n  Type exactly one of: Python properties / __init__ / __repr__",
    "hint": "This course covered a Python decorator that lets a method be accessed using attribute syntax (no parentheses) while still running real code underneath."
  },
  {
    "prompt": "Java has INTERFACES: a class explicitly declares `implements SomeInterface`, committing at compile time to providing every method that interface requires. Python instead commonly relies on DUCK TYPING (if an object has the right methods, it can be used, with no explicit declaration required) or, for a more structured version, Abstract Base Classes (ABCs). Contrast Java's interfaces with Python's approach, and name the two relevant Python concepts.\n\n  Type exactly one of: duck typing and ABCs / global functions / *args and **kwargs",
    "hint": "One Python concept requires no explicit relationship declaration at all ('if it walks like a duck...'); the other (ABCs) is closer to Java's explicit contract, but still Python's own take on it."
  },
  {
    "prompt": "TypeScript adds a static type system ON TOP of JavaScript, but that type system is GRADUAL: types can be added incrementally, file by file, and untyped JavaScript code still runs even where types are missing. Which course concepts does this map to?\n\n  Type exactly one of: type annotations plus a type checker / C++ auto / duck typing",
    "hint": "This course covered Python's own optional way of writing down types, plus a specific tool that checks those written-down types are being used consistently."
  },
  {
    "prompt": "Across every scenario in this activity, state the one general lesson this whole module is trying to teach, in your own words.\n\n  Type exactly one of: syntax differs but the concepts transfer / every language converges on identical syntax / concepts are unique per language and do not transfer",
    "hint": "Every single question mapped some unfamiliar language's syntax back to a concept this course already taught. What does that pattern, repeated across seven very different languages, tell you about learning a new language in the future?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "z^*hBV1=X)YsyWDn90#>!3YKrb#wwcSv1$dXFy%7#e9LQ_7ACqqgE)|cqLy)SJNAah~gJ8Mq`MpW^GnNt^(n%dr5+8>G&ep$i6#h>@(?S@=gk6XPt3BTY|3z*{XB(M|_1i4p7c2Nw3VmUKSqUpN4iUXI<<<y{?_-=POL6-#aJqdwf_7uMbGK?2S|#${5~AdiuQNErC)Zet-RMbunil;}4H!tN7-i0z+BQ!?AEeWon<6ji&z9x9p<_7|umvi|0}EZLOHenHC_{7b~#P1*=gk2R2WdS1GO%JZMG~C@_4Llg++9NMUItzgTFuW1Upgk9$RevZs$$K96z+!=7-uSm$&I67XQQE{{%rUE#4U=`Wwh(H-9N(v<<@p;3W}!ySyk*gnTVreB@NU}W5Uo^ak<0{68YCM3r>O!iLEb`JRbDQp~3#v-$2$atk5L<$e8rI|#S>?fN}b(J)Yg7hcb2TfXPW~TY>-Bx3%1G@p@#Ixx#Tsk(Fhw13L;G9E#^dL0y&0pKLGf2#<+;pn!xGzK(Gox(sE7~tD)1%HC8S1XeN&>c1Abxc}Ft(6unkEkhbttBph%Nlle#}X#^nTj1pdm*WBvbH>jC<RDh)zVI++z(i3k96^8@Xwqj8W4j$Xw$>u-sz2hj<;iPLK<Bb&@kit=mkd8y@%?@3%+5Sz7QManku^qdoY22MBt0&;zPDT4j%LN5MvbKHxUvozGoB!l%~d)=`9=avKUZ9qMeki%Zi=35iNpcC04XiBJu0AT^B*Otmm}9e$f1uvZSiD!`4x7pPvQKjiV<uXfIHxcB_-StQscKM|(Z1T^2BhAk@BqvpxR_fX?k$34@q{9jmoF4?9mF$0suR$KyW3Ogmy)_^FdK8B8L_jXxqI{n@*RPC<OcNfd3&9BOJ8mfgE40EH3|Msh3Rto",
  "LgPz-e95_1a!p{ImrXP=a6pdYa&+5(D-~X!vwS&qFPhDDB3Oq~*+t(R5d(sKnc+D6U2VbV9TiyXb~B&^D7PNe@K|S2DV|~LK_H(TolS<*lhn8)J^usfebaaq1V19YDw*SemVe7zMnXP=ZJ-hTH^OV%2&_~Jy>Xc=;kX8?H8l`3M7<VO=`&H@W!SFDDlHZ7ZBy%{H<8_-ecUn#Q+4jXjL^MA)F@ndtZ@w$7H|PfmY~j!WFWZmUXR0Am9%}?10!6CYJ5QIWYbTNf5dv22cEvG48we4OLRU}9TCnXpfM&v@X=Qsf7Aj$LjmNYdhiVFev>vY==doe@UN;}lOjL|_wG;>B`NqunqBuqECVd7DGG2>f6Um4>c#*`Q<Cb;Be3P->lpPjr;!RE*fRQYK5fIqp^H{VG6MRR_r;UuHC#AIzk&nqtcpBkS6|wq1Fl12<64!Hg(ILl;krrCF#K7hBFeNhzDkLp%`Z}pXQQKa{}WBKztCaVmJNpcOURjJMd6XSbX|1!wu><0q=^t|_HY;7Ji0P+fJ=c+;sC~~w(w@%s4$Vg3?-L;ewTLZmMBS=5Ta#BGvgT`auPL>JDYC$t^v^PBxF)r{`U@%bp0f(#N?v$bCvY|Dao#3o@JP5=UuH0!*KfBj5{-I1Zms>I>4Y=mMGfDL*N0*Zrtb#(zOY%6Iu>*=B^pr<8pSpM@S5}x{qDr#xdkOsr5IxNZb{Tb@ypQqg-|c%xQZdVv-Pdpb33CD#(mA#)ixgQ23M@rXws!C1%TGDINQeE-*DE>J|CKmTRCUpdAp?hWWx!r($t)!EKI{KZqc3VY4S!*4KOpEKPoMJSE)kKrti@_Yys@;=<YqFuc$k$`k",
  "VRJ6x4kX?#udgVBDy+ZL?z+5O{?dOSCCNeve&;W+dO2vfc@pG)O=uVUxbo8hQlc-r|E%lCILAP`z|BRM<{tURZ*PVzmq5nP=f=RM{CSQ*_(q+$-#WpgpHhYJWJcjo$SNRFA><X*tAp@}TmcvJIrSbf$@7GtlU>K=66D1quFFyvw`e}m1xPPQ{uM9MtF+D3MAp%nD<yUQ;txxYwXeM?I(acHBQ5li2B>m&r%^9QK(v21n`TJdn?&&5OGr?BtCGTzJ=ykgglWaV*X4?O9bjjr`Ci6{HCrtHX^Jb;77pB7NjKINlUKJ@GLpCzwrmr&(9tX4Eo#zcwDfiDr)jg!k#bCagVS^*hS4{W6%(O@y&6Si%K2Qwmy8!l>AP)X!^o{-Rb7ks+)_{dY1?a6tKv937_hrSI@JD4ObJP1#B&nwsJUzMe|@3WnfIw~**)0TZBgkM)a2hptl_eYy6qi7^Hf^0nogUGDv0MQJ!ey>?~qn?>v|uUwfEge2o(Ez2vDmFv^M{v_pFF+&Ca-R@>h~D<<F|k{bxK{#K>myz^FfOr%7<Ei^}cbs1P>t%%89h5L-{>vmym~Rp_9JI{K{U&rX^PB_m4#?~GeOG)Ex4Ta{X2cj+>pjhYT?_V7J6I~*wkt$oOiATU-O>&Lk;0^-55XPD?mCN7np9baw&+EZlvV1!|B2f`sX&SUD3kl}Gall-$u#C=UdqaZO$CAWNq%A|X>iIo{rJVlSbu$Ab!nLZTgDm%JDVUYbAhSYy-Kz_(s7LFDI$Cw}8vkXg2wy5V;T5F_Zq4}s%ee{^c2fI2#d!&(rIRsjXDJw%qs@a)+",
  "#%Yy4gH*iMcEh3I)RJ@V-dIOG;W2ZV$nm7qq#n6hQ5pNPpja^u8Yngv`~%5KW-3U`GfO?ACl)`l1*^JoLPrILx1I5wIrpet#FwERQVxTn;GGo!ZGFSD!+vUq`u$0+M=f~1f70y@Gq-Xy9CX`b2Y!UGvWYQf<LrrfzzvVCvh5Jey^~#PCcXv}Iok)u)U;fE==WVlp`(7jh2TSufT^Qp{~a%Ov-kaHV=JxpAqVDGJFE}AU@+mwz4A1)?0BZnD)Gmwa6+@t)Qx}D2%T)o>?Hp%7bHljO1qZm9{zC!bFIXwVctlGeKi9g1Hlsao;Lj95?%fTp_ybbCz20{|8=t0H&`;<Jt`aP`1Vk|3%EkJhxf72lbwZAPDyiu#3~6Qb4n2b8&#o`04X(#nyIODHkoHP06@wfUm?q_@eq7#0$;)ZvVB)RCH8(K<a^!;&H&DEi&=8VlrxvY_MtQDW~nxh0=|sW9M1Xq=MC8nHNlfH?28&)bX;lO>z)S8me^XH%EQ{5i%lQf66T2-Yo)25?PhX`uFry!WiHAqNmloiEkwJl`ln`-_GL;)uTKO;E4of;TcWN0C)uNrX^l1-%WkE7y6SuvaHF-OgxZ0DXO4D;>{pbKN~EWpln_h&h&-~bd84)i+0h_`*m#H)0a}0&0M9k_thy*KR`a$cLK0P(3g$Hx)hA<g<)N?q43R@%f*H$Sh((j@L=i(t^K#nHhVgDU0Lr!CJm&C0E$sUd97{x`)1AYa&giLqhN_BL`LaO2vW!CmNtC@z?)zLb{l>fD{H~(JI7XA>kd$=0>gNGfdD@bex9#jWVUwkRS+gG>D1L`=$0LE6NAB88alow",
  "J%&^~Le;lR>Q7S;es`-VL%<n$+u{Uy$up?zM@KiPCL1zgjBwkr&0s+rcCNK>?zIav6&t)Wj!D~WAobALDLLY_H2Fc?i)1pO<mtS|2PQ&vpSh8~uP3&*mc=?D!8yI4OS?cpY(TqwdzWmIT=$CP8oAc7;jRrIbL?rRQII<zWP?dHf<^w*)4DZq53+*kxjI-^HaA2llwta1iSUDoLVovY&DZ&srQTgei7Gg&>nf-Wzfy7Anf}bx8*$LX>?oG+u#OpfiqE#AaO4K*?*Y9xg+7dX^-23Cu=ZBKM#3wB?ay9Q`3z?yrsAq`okW<$M`(XB(Axf1WVr~OfmjXr>pDn&&;oakW0kUqAs{$uwQCUR>@|5dH7L0s-svy~oEp>*B#sZ7pd~`FJKLnhk<v>`!#-BVjq#FG;k@i_tVnh2(G!=&#iz@`@k5aN$J%7KJIby+S4#)<yBp{+N=j*Gnun~6h%iy3{Y@<82a>2NXfDEiOpRVKI^dLF((g9;sJQC^GNU*G^M(}ki=e}gxX7EBnbwlf<l(_cq-^A#y97FLYZz_sYFuqTR(XM867b!*kfForK927NiMI2~U)(%kQ9dT%c&~Io<Rk<?WR}=WVt#@FbHYKRbWYO@4u$9pcbCGbx{iwtG1%-|@AlR%F$^<nSP&=Z-)b~KzvVm4HVgHQzCsTf#`0IC&|%0cFa3a;RvNMZm%zakdAw_Ojb)XG@brNUhkToQG@%KO{Blfh(45VAiB8g-Kz~iT{?{7pWCpC1Z|xxZ@v*zIpQOK@(!#N~{ZZW&b9QKN#$X|p(T&|}b1L|BzifZtDzjhNY~Q{0(UsERZWW53m!`<{HATG<0ao!EpBp3qBq3~2",
  "Mu2_TztH!425C@?8J`yX-Miw%ZxXPTIR!5lvf7gTw@EL&DPE+#41Q{Cq;g?`!~5q;9bFbj_rSKpcv?p(S|TRpe_CeFg2aW&2urL1T$dAq@7?m)A8ffsTna}LXJh%nhg>9-g61%0l6-lOrFMuk^=8jILmdbTwPPd=Hf#8X#~0=GQ{6e>^x;#?j(yh=D88y^#*FLURc=Lc-ykGao}gFrYmdcOij_BRDV)S?3-})iO@pO9=CHBNqmL#GcZW!ndCtj0z}uwj!^;Yx<13JjqOwD1U+T)f*w(4Me4z3xmnKZjRBp>4C?y@i?<D~eV_^;5FS*1P=3lj10!@^Xd8d7hgCd5zIG&&V4TaA5qxbr4yqwPdoBG&xvIo?R2EbfyxYP-xCJ@vr@>O7AA!Bxm6y@FGp*8GLzrewnDG2jqHa+$kkDn>$oRS`&WsN+O*XE?dkVwj`OyTwQe;b18MVMxH(CZq5O7VoO)_?d?ei*nF4*9X{yECBzgHtl%lwEu)Yoo$HV?8r@a3Y{4U`Nwxb!;6_*Nuw<G#sv<GQvC&1x$lxE?U5F`R#L#Dy3Hk8TCq&i(w<G5;p2#Rj81-5y;D&zAWk;*$^qe+_xi1Ax$qfsa@6nh)24-1Bd58i9gmTO0SA1>2~3D{KfQfJGcUz;3HC>JF@t9av>{Sy0569h?C(SYNxL)Tg$t~SHIub=p0LXB$-cMoiI4%Cas6*wCSZ?nGB^dVto9!^R5Iwh{Ig$U$$S1cm-VgG6uH~I%qz_pAb9nMN)FRrhsbMS*8X;+IwfK)oLq+aoWA70>7m=b9zs@2r{U{ck^x3{YBte_vsw1KGhKx>V`6x5usSVqE-@_jk#Ur{s5|6u>qc;-p*`>GJ#i1L+O<H6AF1d3huBP0z&*!_)wY?@f*7YFoR5BHv",
  "XuaQ6*$nQhg@g9v;A{}5g3Q+t$TfLUuG(vKWuJ5ha7bjRbASCmKgCe)T}LGmXSiK`&1F1bP*_#Mt@P|YMNl9{yJPokbe8_cqb3&vFL`SV9)oM%U^2*k`FF`2a&-n|q+A#|5bmi~l_En&hq5NK^JtY<(j!STv>@R-+5f?j6Ll+~9`mIc9pu&wqv65*S1qU9vXIG`AyLw@pLGthG}<P_MT9WPrS`B4{CgMefeX_}u<J@aE_4zT>+{fvvc@fYZ{0i|vS6f}70^?5+{DlFO!T=?E=CfJh)!s<ZAPO@rMY|YO~D*Ia87+^9{wGQ$VzHH%w_)kIFC)ff$<J(KE(GFU}g`<pokYdmtewFOb}iYEBZE)F$g^AmYO5HIWZyk_<RZ@pb~MioqS<>x3gKt<6FLuUtIaMqx*%50qK71=@&9VMug@uZHbR5CJU_8L2>RDs|E~F5ClB4EgqopoSWN+i{=__Q)s`Hp7BXXX!k()pST?H5WHNPdLsfnkO~Z%fGWA`dX*-zT|lsLGkE>srR$LcG`@*HGjGqS-qYK}My-yUQ6J_VQzwQB21sqEi)!aP%*a}Il8RSJUk9cf7^AAuix)>nnNCRpCH{9xF(TGDDqo9#-TsG)dP~)acm=N5Wj-OtvvP?qfmExs4)PnJpIDt2<IUMZ2&A-9w)HCsV69NUE10cX$7BC*Ah?KR#bOCH^}Y(f@ge",
  "PpOkDitL)8gaE_^e~mlV=N97|G;d;oO|0M4VR)EaGz=Y1q=rUMWBpl0nbsL`5oM)A`mSAs<a2KU4>tMfZK5VYHP~as;K?ptN*bbXKPgAh0lIB1ftOVL<ag3(^r{(3yA$BH?3l+(v554c{3Rc-Feo<JI?Q`}6MDtX<V8B|bXY%Nu7(Q>3wiTEoZS$-Y&OF2ePIRAOZkE*%F4#|h{vqWeEcm02%<kwTQC65DeY#iKpD}~#!KuIg!Oezb*@}#qsr2@eVcC&QZn;|OeG6#3niAL@q?;Y*LCKh7-wmy*l}Sbo_hv-4anm`ht7`G5y$)D8XMzsv~%yqe#?Oj1MWgJZmTCw^3kNmL7a(l-2Vqo_=B^L>8RWrVtflgG7TOS?5cM#BWct?F+K)c(dq$VVO<+oQ;@HfZp@2x?z3PuBZH75YmN3BWMN|gqx`ng9G5r!J_%?Tf17xiq~8mhz@nZ=8yPC<(T(!#AolknO+-t5>oyV7>V^soy=4q6t@mr((^ozDalFek)-0@U1b4~4_=3_zuXo^A49b_H^5<p>E0CHdiILa&9fN`?HCV`2=3a`}nJh4R__144lx*t)q4(8Ugi{TbTb$1s3&eYg760>p&)|?+b(k&YY06tnMu8mdT^*{~QVgNugZ{U0*7uZQQCtdHp_azo0m9`6fbtSF%yU}PH^~5wQpE>eP|jpS#HMauI7>^`^vkK?0+{>l>{u-vIe$S=Vod+?8I);YO5y|;R0I9x!_Q0j7b=rD1L#O?u26n^Sgeobv|6wV5`0I~uRWk5tDPtyk^~H~NOK~}41>ko_qfrpZ<N%5oP^oE4hpASGWYSTm-)bHcN;x|Ls)fF|7d4GU0Qa+6og58Bpq?uByS_&|7DoM5I5FFUK<L_9o<knmALTeO8",
  "qCo$VrYpSBBzVu}r`DeP=$Iy^I&!M<6J}~Cwc%bT|5#FMRNyhPm7Q3xHfh4|-RX@GS6;BXd2X^Cw;X(pkXXU^4gKu?mcbqosPCw-u$}2OuH{)gR&`+-NR>HhVp?IJ_Q*F=shWKQY)t`3>$X2V&|!+flO34yZ>Il94EQ2qw#VT}H7vg6@egt0c>uu%Yj6xx1|Y8HI<5SfEu#WWp|M3x*fM8LOm$>7D!!a0+F!{Qy)qgfS*=V{v25tkW%82Cw!TSxKi5nA0yl32PFHrYPjTyLCnMpOqrtHucN|1Mh=`+wx(z~QjF=yw1bv(PS}%-tH%yW&np1md5?6_#+2s50GE6mF<2XT0ax)=4g#T+G=A);?g^oB3b75o2$cDmZ!K{Y&7cF-8Kk;SooIweA+QLS*;!9G8Z-N?O=u+o3paYpswU4CUVFY)bsz;Y8_rmA2_W|5U!ukgiiFM|*9ujBT%y)wnEQuIr=V^a=G)VHB7J`WQ**(jJo7uSeL3D~58DOH}T)wyv>5sc`Ymq9)-&Mqe+vBI~<Q5Q8a2Orx+^;%X^4OnICy(=n>bUV*33Gzk80*4vs?BM|Dp^$gPSg;b3Ct6uT(K!Jm`_Zx-KFbS8x+n#ykkZ!n6~#s4)u4&mf{Mi3433F*C|IQ^`CN^Oa=H+Ps}cqktw*7W>7GT3Nh)>Zu^_Vx-3dBtd4sQcH0(8xE>f8orKJ{L3)5j`+P$2zhSsP8d3F&xtQUF;ew);e{%",
  "XRTEd?<uyldLj|b`}z@y4J)=QCUQMSNJc#vR3@Gad_P78^|(H{PBwPWkhpOA%*C4|-|_2R{;zh~;k46ZhA?)e_voekp&;GkVQQXR7vd-huWb^O9riDY7e3Fa11hXL%<u>_Ksawqr?F7}cD*Q<rmk^+9{%$!&~3~nuYt68=E;k)h|$~x!g@1=9nPSQ;CSZK-;)WP`sjHV9sTZSCCz4(ntyMgA_Ibf=_BXiH^TS5t*@q$9SKE0S1$VrE&xm)U-aFW#S6IQ$2m3tgAvC#_|Zob2jFm!8W<mRCAWmituqFW{-ZV(U?!fNC@`yhQ7FB(S+RVa>V<^<?%!#o6#b8WE8y4$)GK~sg7~P$IU@HPlRx$XLndSGh}Ij^p#)P1DZ}$Q@MJ8MMVTqe&B(#`zpgN~0c|yin@T-7D3FJW#$qD)upx<_4K;!?JF{`zb(e%h@9DOGBa+g2$XEg(;J@dyX)^5uDvu&}SuLxP70n4LEaqJ$w(V`a<XtJ%Ov$rjd(pM{n};TLc^ioJcln1~Y>0^F;0q6kD($%+!ax>lBRYH0WFHaQ=tl!qd_PD2XglG<ipArY0=$*M<3RN3k5jkZ2sn3|yJ&L|{HYhqHnQrFPg&_f4&BYkq2t^BAy4nugLK+8=Umju`Il@jJo2u42zTYaFfCM$?|>kKs#$thx?Z=9kKmqzGL|2=zF(*~%S;X->T6}?-$o6tUOE7gSLH$;Jr_<}yoby}#Obtc(1FXv8`Vr^o~P0f{Cyh|$+|U@C_<P(8jWx3UtjlZK4ZR|0&v<ARGH1r8uHF=7K4{xe;x*$o#7vw#wc~ITT7!qE|Dv}@o7zp(WKbdsR{Zzndd(C2GW2<T9<X5)dCX(z!-x"
]
""")

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)

def _secret_for_item(index):
    """Open the encrypted answer/explanation payload for a 1-based item index."""
    secret = _open_secret(ITEM_SECRETS[index - 1], "language-safari:question:" + str(index))
    if secret is None:
        print("  [error] Could not open item data -- contact your instructor.")
        sys.exit(1)
    return secret


class ActivityEngine:
    """Core check/passphrase logic for Activity: Language Safari; importable without a TTY."""

    slug = "language-safari"
    title = "Activity: Language Safari"
    description = 'The capstone recognition tour. Each of these scenarios describes a feature from a DIFFERENT programming language -- Rust, Java, JavaScript, Go, Haskell, C#, TypeScript -- and asks you to name the concept from THIS course that it maps to. The point is not to learn any of these languages: it is to recognize that languages differ in syntax and surface features, but the underlying concepts -- resource ownership, error handling, concurrency models, static typing -- transfer. Every scenario is honestly explained, including where the mapping is not a perfect one-to-one match.'

    def items(self):
        """Return the public (non-secret) item dicts, in display order."""
        return QUESTIONS

    def _options(self, index):
        """Shuffled display options (answer + distractors) for a choices item.

        Order is deterministic per item so the correct answer is not always in
        the same position, but is stable across runs.
        """
        secret = _secret_for_item(index)
        opts = [secret["answer"]] + list((secret.get("wrong") or {}).keys())
        _random.Random(self.slug + ":" + str(index)).shuffle(opts)
        return opts

    def check(self, index, raw):
        """Check a 1-based item's raw answer; return {correct, feedback, explanation, canonical}.

        For a concept "choices" item the raw input is matched against the
        displayed options with small-typo tolerance, and `canonical` carries the
        exact answer string to decrypt with; for other items an exact match is
        required and `canonical` is the raw input.
        """
        secret = _secret_for_item(index)
        answer = secret["answer"]
        wrong = secret.get("wrong") or {}
        item = self.items()[index - 1]
        if item.get("choices"):
            matched = _best_match(raw, self._options(index))
            if matched is None:
                return {"correct": False, "feedback": None, "explanation": None, "canonical": raw}
            correct = matched == answer
            return {
                "correct": correct,
                "feedback": None if correct else wrong.get(matched),
                "explanation": secret.get("explanation", "") if correct else None,
                "canonical": answer if correct else matched,
            }
        correct = raw == answer
        return {
            "correct": correct,
            "feedback": None if correct else wrong.get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
            "canonical": raw,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all answers are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()
def _ask(item, index, total):
    print(f"\n  Q{index:02}/{total:02}  {item['prompt']}")
    print(f"           Hint: {item['hint']}")
    if item.get("choices"):
        _show_choices(ENGINE._options(index))
    while True:
        raw = input("  Your answer: ").strip()
        if not raw:
            continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return result.get("canonical", raw)
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print("           Trace through the function step by step and try again.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    answers = []
    items = ENGINE.items()
    total = len(items)
    for i, item in enumerate(items, 1):
        answers.append(_ask(item, i, total))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
