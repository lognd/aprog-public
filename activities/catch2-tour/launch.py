#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/catch2-tour.json and re-run the generator.
"""Activity: Catch2 Tour

A guided walkthrough of Catch2 v3 -- from FetchContent setup through TEST_CASE, SECTION, and assertion macros.
"""
import json, sys

# -- Crypto / obfuscation --
_KEY_A = "33554324fef05b9b2cdf5c420424d5962c835dc6ba60e49ca5b894c49610c779"
_KEY_B = "246bf1e9d11ac959c8e52d1ad0cb81c8daff2ffc6def209bc9fbd8374fe518e7"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "27d21e46661e92d1c3c244d2234b4758f8a6c84695cb65836bd749b4b486300fbf63843a879b5618db7f4526c1c1be609709eae068d402d62c30"

# -- Public questions and encrypted answer/explanation material --
QUESTIONS = json.loads(r"""
[
  {
    "prompt": "What target fills the blank to link Catch2 and provide its own main()?",
    "hint": "The target includes 'WithMain' in its name.",
    "code": "FetchContent_Declare(\n    Catch2\n    GIT_REPOSITORY https://github.com/catchorg/Catch2.git\n    GIT_TAG        v3.5.2\n)\nFetchContent_MakeAvailable(Catch2)\n\nadd_executable(my_tests tests.cpp src.cpp)\ntarget_link_libraries(my_tests PRIVATE ___)"
  },
  {
    "prompt": "How many test cases does this file define?",
    "hint": "Count the TEST_CASE(...) calls.",
    "code": "#include <catch2/catch_test_macros.hpp>\n#include \"stats.hpp\"\n\nTEST_CASE(\"mean of an empty vector\") {\n    std::vector<double> v;\n    REQUIRE(mean(v) == 0.0);\n}"
  },
  {
    "prompt": "If denominator is 0: which line stops the test immediately so line C never runs?",
    "hint": "One macro aborts; the other records a failure and continues.",
    "code": "TEST_CASE(\"division\") {\n    int denominator = 0;\n    CHECK(denominator != 0);   // line A\n    REQUIRE(denominator != 0); // line B\n    int result = 10 / denominator;\n    CHECK(result == 5);        // line C\n}"
  },
  {
    "prompt": "How many times is the line `std::vector<int> v;` executed when this test runs?",
    "hint": "Each SECTION runs with a fresh copy of everything defined before it.",
    "code": "TEST_CASE(\"vector operations\") {\n    std::vector<int> v;\n\n    SECTION(\"push_back increases size\") {\n        v.push_back(1);\n        REQUIRE(v.size() == 1);\n    }\n\n    SECTION(\"pop_back decreases size\") {\n        v.push_back(1);\n        v.pop_back();\n        REQUIRE(v.empty());\n    }\n}"
  },
  {
    "prompt": "What command-line argument runs ONLY the two sort tests and skips the search test?",
    "hint": "Catch2 uses square brackets to filter tests by tag.",
    "code": "TEST_CASE(\"insertion sort correctness\", \"[sort]\") { ... }\nTEST_CASE(\"insertion sort performance\", \"[sort][perf]\") { ... }\nTEST_CASE(\"binary search correctness\", \"[search]\") { ... }"
  },
  {
    "prompt": "What flag produces one line of output per assertion instead of a summary?",
    "hint": "The flag starts with -v. Check ./my_tests --help.",
    "code": "$ ./my_tests\nRandomness seeded to: 12345678\n===============================================================================\nAll tests passed (7 assertions in 3 test cases)"
  },
  {
    "prompt": "You will use Catch2 in the Data Structures course for every assignment. What kind of tests does Catch2 excel at compared to a main()-based test harness?",
    "hint": "Think about what happens when one test crashes or fails -- does it affect the others?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "BC==Rj2fDBE1d{uw03+4!`6cYyOPhzt?nPLU+dA30D79X@}CNCgT`3bs;XY0v)s#BYDKIR2t1!^?$kyg{T-koUOw`=GEQaA-fM#QK`Z-)9aZG+7#%y19-J)!b2tud(={q4*e+xBEo?i%QCO!-a=#DuI<?O!+RbNdF8eN}IyKe@Vsms;F4chk&o*ni1gcDJ{;4_E0gtDD$CY>a#N0cAQqKb4r-jMJe74k_SGYWjyY6h*QI!E#8@vyxBb^Bu{6#s~EzF=+x()~ZOAP=r#(z4tIUz~Oz+R_rmDsI(ngf`6$JJ{XZHP>q{pFcP!>wh+%DXuO+T8S_(1!uoNfyW^wn0-W*sjoai-q7k8;R>znzc+2yC@r+8=g9{`3BTi#H&w8FQfk7jdgxAD3>)bgA{f51ml$rNkxDry}H4eSh=6|0ojq;7J5JlCmZyAv;",
  "0;~VQMjgDzfyZ#j+#3EKEpcW8R9MUob_ETZ&%Oa~h7JS?H6nP`yGch)7zadq8`Ka>kEH+hiZ!Y)_!?y7QoYB2SWIZ@--B_OOB_Y?er8qU2ht5(Uv*1%w^YoU0F^fFqjn6GUo%V<xxmr8V5xd7pP@ipPhZ?{GM4C+%lr<T`r(T0T$;IL@d@ZUq-!x=5}7{o)*`Iid1q_)QhLOM{8ytODZKPcc_<N<Rrp=kz4NGi7f`Z?X_sW2;cw_M>D~RxkRPbh2iHgFKC=W^D$hJAErhqnmXJ*6>%L)&Io%<G4B^F0P3H7UD0dk$MJ8xQUg6p>-%5i",
  "y6RwHJd?;u6!!rym5Ts>6N$MbLO<qyOV4LHN!CJpeD*FTXB#P{+TV)YV%P?YAM9qacrwNR!vf%(TJ!`7=2R-PBK_P>BqPl0bZI~N<K;x~EoWGlKfDg*ij$svh5Gv^#LB$l?Rs<|HMy=DUiqM5{nwa?8Chz(;?<QeaG%J%-a!6Y`ygMf$jRIO1Q>>WrQ2HUOB5(6Ocf|sr;MG0NxYn)p4PYAofRwZrwI-VIAc&Ebc5x@$Z|WPpD~WLeoeygQilvudvTm1Sz1JZG)>ZZ4xiA9Y_K{7aQr6JK-z<P-Y4RJ2595B&Kh|(-k=x5`e7hWNmt<At%MQ$yY6kds9|b?i)#JJ0&N`y@tVl19VSQdHoaTTzV5}3luVC`K@@P9Sy%8wyhn=@;}J&nnaNUv8E~ygxdr$sVphEQmR|Imge0X_y_AZVc<K>u7O5apW6*~F@#X%b^WSpkq_L{t?#DmFr#mA!d?Jq",
  "s&ZIaFu(V0?%}wV=@onN!dKUfq%B(nze`@tonBzBOeoE?QeSShcoisTy2MyW>2GW(1|{A&Hs&$;teH2FDC8NqwK_3~L<fDrce*xcEEuu$#YIWZXf*#zgqPxXZ$0d)5O`<#4Pgbu0>_lyEwD4`0mDPPl!e4%kOZZB!yz${K%uLT(xU&^W2wogbpv<ISN4j~jZV&;snrU+B}_z_XmhPsz+bSdyC`M1x6RT3p&rOev7Vd>=l7?skucTsB89&oDD9EE#Lvneo%#x;UhA+yObND?vv&PuKb2jwJvOw{lsw`NXKF-UM$@eL+Z6S2<YhSZo84#kcA&vBNSiPwp(~|kqD3S?)-%>eGsnCU*Eznb!cuIk#g-r6",
  "CI(gAH^if7B6J(YQu6%&^@ejt$%k?F4<{qt=ewzU$~k)!?(kCe>^^DgBP~Kq9F>;2J<7R0>dor*#%_)IjV!F@@S(OSvggvRyeg@-D0OveC|76<pL@Z+P|<HAMs&sMM7KKbZ;wv$56exwIpF8mkX-SlZndcnMy%(k<RCJf)}Wn^DYxVz;1@YdDPeWleEj~UYg@tf?Qibc{0uAk;ErN@41QKVK*wn_COITV&H)T`%=3W8^3CM6L`e2o5?cE>sWSpD>=%qi_uaqvF(MgtSm+~FF9Pqi0n5Ea{+lv&?>hNI6CDG+6OGp&Lw?)fpU?&+xnS<PJbJ~hEs2j14B2hpn3w9Vo<6dIWL(=o`+%mV^(A~33Yplr^<WH1AW*5#3U_i{QPK2Ed4x5Bi+TwhoL`Yit-5?2S)MqjHlk#$%d*D3g;@SOtkgF!Qo?lRb?La?<Cq^FnT<of9m4|1Okst>U0^vYPeAh",
  "bOv*eZ_U$hAVU&$07JQz*TPNfYLFE9A9!ogQ6AnzPMvu7V3!T|{`_br5j32OGW$=>8y4kPP4DctA2VDZ@HWVy{6iGF;RA=HT)WlrIt-RFZLO7W8douXQ;Wd^CmyQLGqkK@^!IB1b%$0JVT|&O)>}ogLqW#bM2KuJ1~1j23T5ZeV=SJ|_A+K#tE^^3_B)ks@BCA(;?Ihqw+~Ic%)xs~gP_W5d3l3tA!sg+44%1*+Dy|sT8^~>lp;cZT|HBn^P)K1w1B4gJ~?eu|J0mLR_uoqs$>aC7@@qSzC~j2Xp*MelQ7nj?=`2kj-ImC9sP8Fl}bP*oDw0BQV*}N<@%NENv>U}X0M0z1SZ4Do}jQP9&CNFbemS^9`*+r1=a4~ZGb;x^OmbQ21wlAd2nWB2>xeps*ZFzXWb%g?;74x01pw)URF{cY*Zfv",
  "i+Ykd!AMMj+$0raui&2VLRs~kRz#|X*3#ZRtQVKX9UoAlf&x_bseCBzEU#`R>Yd~3qM<nx*p@RCI<z6lr$AiqqUYzL7@4L~k)*7~iD&^Km|5L$VHkQu`ih{fTK3$8hm0|@6EB3=wo3A|zbYgDzfG>VdFIWX1bn0wh3C%JC0p}>+Alucj;Re?EMv>-J4lZj+?e4zxCeNpn0M`a>5jaYY0?*hD^iyGkBA=unt<|DRC(4#bM1t-I=<NlQl*<ISB9onNZrI(x1XNfKTwj%`q?{2E-=JKh+)4+(}O;?IDKpTOy!@S0hESBh!cDYRstW<Krv<pUNbh_wf4Q6rm$@xk$#>+FSUb=sX`jgqm#7`6~_CXRzZ0rZ!cl2RpbO*wx-OEeH?~?yedwKcQ)#hfaLTQC!~KQ`648mP206kho0O#;o=_a7q<l{M!atH>f($c@N3TU_sbjZx)d59tF%OxO^-MZTev<KD<I|*w#}JJzm#}btd_*|thsFsS|)$yEFqulb0eT&1WoG`EvEW^qt@phIC$0Y#DM"
]
""")

# -- Helpers --
import textwrap as _tw

_LINE_WIDTH = 70

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    """Open the encrypted answer/explanation payload for a 1-based item index."""
    secret = _open_secret(ITEM_SECRETS[index - 1], "catch2-tour:question:" + str(index))
    if secret is None:
        print("  [error] Could not open item data -- contact your instructor.")
        sys.exit(1)
    return secret


class ActivityEngine:
    """Core check/passphrase logic for Activity: Catch2 Tour; importable without a TTY."""

    slug = "catch2-tour"
    title = "Activity: Catch2 Tour"
    description = 'A guided walkthrough of Catch2 v3 -- from FetchContent setup through TEST_CASE, SECTION, and assertion macros.'

    def items(self):
        """Return the public (non-secret) item dicts, in display order."""
        return QUESTIONS

    def check(self, index, raw):
        """Check a 1-based item's raw answer; return {correct, feedback, explanation}."""
        secret = _secret_for_item(index)
        correct = raw == secret["answer"]
        return {
            "correct": correct,
            "feedback": None if correct else (secret.get("wrong") or {}).get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all answers are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(item, index, total):
    _hr()
    print(f"\n  Q{index:02}/{total:02}  {item['prompt']}")
    print()
    for line in item.get("code", "").splitlines():
        print(f"    {line}")
    print(f"\n  Hint: {item['hint']}")
    while True:
        raw = input("\n  Your answer: ").strip()
        if not raw:
            continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            print()
            for ln in _wrap(("Correct.  " if True else "") + result.get("explanation", "")):
                print(ln)
            return raw
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print("  Think about each call separately and try again.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    answers = []
    items = ENGINE.items()
    total = len(items)
    for i, item in enumerate(items, 1):
        answers.append(_ask(item, i, total))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
