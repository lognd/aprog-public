#!/usr/bin/env python3
# Generated by aprog-private/activities/generate_activities.py
# To update snippets: edit quiz-data/cstring-vs-stdstring.json and re-run the generator.
"""Activity: C-String vs std::string


"""
import json, os, subprocess, sys, tempfile

# -- Crypto / obfuscation --
_KEY_A = "cea995f0aed5ebab959b08a40376831593252f42f7615df5f0daca5c7ee7b5e4"
_KEY_B = "45a6a110b143ee54774cdbc075f9f4028766f17d5f557a94b036ede213a88f0c"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "45af572d834ebf06ee70392a9a5e405c8536a09fd1e9c206148c6c0a96ca72f7fbc9886bef5fd55bdec3a2ecd3bee4516f"

# -- Public snippets and encrypted feedback/explanation material --
SNIPPETS = json.loads(r"""
[
  {
    "id": 1,
    "title": "getting the length -- two ways",
    "platform_note": "any platform, strlen from <cstring>, std::string from <string>",
    "code": "#include <cstring>\n#include <iostream>\n#include <string>\nint main() {\n    const char* cs = \"hello\";\n    std::string ss = \"hello\";\n    std::cout << strlen(cs) << \"\\n\";\n    std::cout << ss.size() << \"\\n\";\n}",
    "prompt": "What does this print? (two numbers, one per line)"
  },
  {
    "id": 2,
    "title": "concatenation -- the operator+ gap",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <string>\nint main() {\n    std::string a = \"hello\";\n    std::string b = \" world\";\n    std::string c = a + b;\n    std::cout << c << \"\\n\";\n    std::cout << c.size() << \"\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  },
  {
    "id": 3,
    "title": "comparison -- == works on std::string",
    "platform_note": "any platform",
    "code": "#include <cstring>\n#include <iostream>\n#include <string>\nint main() {\n    const char* cs = \"cat\";\n    std::string ss = \"cat\";\n    std::cout << (strcmp(cs, \"cat\") == 0 ? \"yes\" : \"no\") << \"\\n\";\n    std::cout << (ss == \"cat\" ? \"yes\" : \"no\") << \"\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  },
  {
    "id": 4,
    "title": "mutation safety -- who can be modified?",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <string>\nint main() {\n    std::string ss = \"hello\";\n    ss[0] = 'H';\n    std::cout << ss << \"\\n\";\n    char arr[] = \"hello\";\n    arr[0] = 'H';\n    std::cout << arr << \"\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  },
  {
    "id": 5,
    "title": "out-of-bounds -- one is silent, one throws",
    "platform_note": "std::string::at() throws std::out_of_range; [] is undefined behavior",
    "code": "#include <iostream>\n#include <string>\nint main() {\n    std::string ss = \"hi\";\n    std::cout << ss[0] << \"\\n\";\n    std::cout << ss.at(1) << \"\\n\";\n    std::cout << ss.size() << \"\\n\";\n}",
    "prompt": "What does this print? (three lines)"
  },
  {
    "id": 6,
    "title": "the c_str() bridge",
    "platform_note": "any platform -- c_str() returns a const char* view",
    "code": "#include <cstring>\n#include <iostream>\n#include <string>\nint main() {\n    std::string ss = \"hello world\";\n    const char* p = ss.c_str();\n    std::cout << strlen(p) << \"\\n\";\n    std::cout << (ss == p ? \"same\" : \"diff\") << \"\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  }
]
""")
SNIPPET_SECRETS = json.loads(r"""
[
  "6S_S_x)i)GC!Q_18(Hjrwp#jhR9Xr?d>c9Nx;{}89jN{ct$2F}(m{4rbsjvAjqwgt0K+Jwr?<d%csMh4wDTgtq_#|Z^lOHfE>gI`%=!?H7Y`~s>_-@LD*48-^0H7u*7cG)Y1~6=J!=ew3asML>Q-j!Y}OT@x1;T!%{0n%c}7Mwjpt)ZIicAiAHP3>F}>Ruc)s4Ra;St?YcYA<jH(0rB_#B}E#;gHw7w2f#o^fCIul?}6lVMJSb=Rs_oMuYEiB+O)<Zpr=B#Q0FjyLnVO9up5lWF?Py_}{Q>~B+M-5J$3@?}N)dPU!(IGUD(pif}h$ny{T*)yej+e{_H1?t3uwWR9+bNdU04AsnabYFQR%sCHA=<}m`EhSqzH(hVJwisPWyc6lL51uvfwAVttk)hGwD*$Pm6RMHT6T3ZEkv)LM|M!V(z0Ri&nX6ytalLO#_r%Q^D_5h|4lN>85j$N$_lD|",
  "at5p<IzCxguet|f=!9}LOynt?@G+L|@2|Ppr9yh7(73jsL1ot{zxoes#k_)RTwB@JI;a|Kjnzl`u)W|0(2c?rv5W@3ox1L%_s-0-4IzgM4v)|rUBPJVOednbravs~9$i8F7#;qC`Ugq=xEEQxKjIH3c4h-4F+9vLUr3c6cP@|I6r1zFomhvUH-Ryu`mAJ8iy1xY$Kwv9R$!!3;32t<@<e6iqWBQHS#^qejx?`niC$5ZuJTrdRqIqbYHcpYTy>TE2t}ZGCYhG0g*2`52Z*`vtJpr>HEF$S_D<B*XK78H^yhcD(y8km^=!-h>*KpAY+5>G8>~c#DnGtFRASAE{^OAS6;;^X?9I}`Usx-d_TiLp9V>aiJsq~<S=98mM)DJC$ug*BB`eja9(!o{Yh;p=X367_95%v<ycj$4O@gV1=bgQ4zOf(}v^FUH!w|m@Y^&ryOrWve)%T=o@)L{",
  "0obslH`Wdy>x=ameM9yHx-l%SG9`_<iPXA4%JGf`L4=hPZXH@kb;2<k<i_c?>d$ga;0MP1gsAS?e23Q6>h@RnC3%h;x?V@AN$lv_Fp&^&AoQC)xq!)|CHN);$@vyAM3a1cq&8TG0qX1SaGsCy#m=?lV~&+FcTZj`1_E3@`?ce<eef`8KwctCrF;egVh~sc6C(QZ7_Bnc@&gBHfppSWjB-+CN35A_%;`eUVBUQWO(<w&vUD<T;o`!i3E{ps4V`kJnt}Cf6?^A((lG&Zk|Krc4Dy35a@sQ@$<Kb;(QRGB!Bnmjs&pdT*s{@k>Z8#hk&$0=@3wKFmqE$10W|{JrhzLE&qIH$@UPMbe8nld9&Bc#J8p@9wYpC$YvF;dv+NG$WdS_VJa?J;`oFYa&scd5kxZKkrSWw>qD~pdSLhs!ty;2U5VDR#MmW!9M@|z2qF%-_e^Z^ss<*6+WvyBOH_&V);bH;nN4X}un3y&2`{@jhh|T",
  "AT4tASxU8fySO^$D2k|p<jAyloTbBo90-gkYgG@W*aj+?)xxseb<JE(hIO<4I<U3Hod9Hg`e(~G0zDn`P+c*+v^L272WesRxg&AsOb9&2)kJC{t=HYeS#ERMsZ0DosF8|R6t@L2jQA*hxUcMwWbOHtVulKu-e^!Q-W0L7@HAoJM$x!2`}JdTac49~R7D#F?D-J@oI^e>;lt}&xRDWZ_2!JX>hK;@!*#~V_S&*Tk{>XkU=#<$<If?dD~}{pE)2r4HM{b5{t--!|8B_kE}hxfD}%+N7*wtW`1~&;KggYQS0=75)=)V=x;_Xiuho;zTa*sj2h$%|$bc@;O4qNu=s@mfb<s9j{xu?|%O|52(v^qO?h2U5!4SV#u)wI{JC*KXTC_jY4sRT?k&u+|fWlFE<Ql^`VSxJpSF|RNfRQYJcpO+BSlvRrZ(99;q_=t>k=9ZEYwoS#AaiEgoal;F4*&w_4b^tm-jbr7xaIwzj7Jd7a6PS)(o$=Z*f<aJ3bsvB(n5CsStNQ?NvClw`2_!F3zZ1?#O@>URtc4<JO",
  "UQd2)UT7ue*<CXTovNv*RKsLb;+s1k&!j<rM9m8he=q?0za@`RcL!MTa_?s4YC+>uTAGM(p%ZGeCK*2oL=5b+WC7u|Y~4Yqd_YyHa`sMicuf0vYF?WJlG*n|t6saUm6l)=q|tLzJ5zgc6Q!%K+HJoHD>u+)6Zb8zlVRgxVfGgKK;zK#=1K%6&EQz$=llgQ&Z@mLFyAhBcbdU`sG%bUA3?f%n0G;F6vT5eJE1o`6dV0|V$!G|M2i>ooB<`%ggR3DKoTrE_>V%rqKf!ESoj})t8leuYn)(ydH4Z~pm90i(@6v3v*o*Y!<qId*nr~fW-x;F&XLD2vPuDLusxZVM}k_OAotG{4_O=vpO;FoSaa^LD0I(T{E5-V1fd)&sa6A9Qqzj3o}9!U9NMM%dX6Nh*BSp~Tsp}-S26PL#h2D2!3*Z|ih~ww1f*f-t|&l8dbk#atG*F0RuTP9*uGrfPgMOcxe54IDf!Ifpuz6U9mg0B$<gUL9w=DVRzN-SHGs(*%>@$`!W3KCnyd",
  "Ojh7h5(Repq1HMe7W#M*TZFQY7;$gC>6z~tvSM|IqcJynSAt$Nc;dSG7%DJC)ap&}#aFMRa^xDi&>rPSx}j9F)LgICHPnFZSHsw5R>cB0-2+Y&RUs6|$d-S&w9qc2xp0G#Xsi4L@xzU1doc?t5Iv!!)umJP%vjs^z@#=ukZyFBilX7IMK%3i8sL6USZJi|{3*e3(YuSjvgTt>@dg#pA8_EzwKu}yMGl1k@#cfu9UegckzXVf{6VgH;Y@xolQ+sla(>xoi(9{Jk7BxIbuSnU-}c5My!=(fxOg}_*Ez;4QbVsegMuV_`9#43blzE5T?>0#=G@86yxlG#+rj)x(Qs~@@i_NJ*WR?dUUM>44Va{anT!EBSAMyM7lO$ri}m4}-RHceH!sKkOycKvHw;^u<~8_dH#RQFi)6~PX5$CGI(Ch+Y?XQoS=>7BwB*I<0_K_hS?GadKW5+5!Q@$!($4d%s5nSFp@q+k?y~wqD`5f&0I8Y!XI(qMg=S$`;4qIv66I!~<cI1(dg?vQen%|gX9S&VY8hDhxqN#hWD`|-dOU2X&EaMi!Kea%KL(Y1jgDLd@zPr$thgB_Oj$(p-%e*8^5GB"
]
""")

# -- Helpers --
import textwrap as _tw

_LINE_WIDTH = 70

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()


def _secret_for_snippet(index):
    secret = _open_secret(SNIPPET_SECRETS[index - 1], "cstring-vs-stdstring:snippet:" + str(index))
    if secret is None:
        print("  [error] Could not open snippet quiz-data -- contact your instructor.")
        sys.exit(1)
    return secret

def _find_compiler():
    for c in ("g++", "clang++"):
        if subprocess.run(["which", c], capture_output=True).returncode == 0:
            return c
    return None


def _compile_run(code, compiler, tmpdir, idx):
    src = os.path.join(tmpdir, f"s{idx:02}.cpp")
    exe = os.path.join(tmpdir, f"s{idx:02}")
    with open(src, "w") as f:
        f.write(code)
    r = subprocess.run([compiler, "-std=c++17", "-o", exe, src],
                       capture_output=True, text=True)
    if r.returncode != 0:
        return None, r.stderr
    r = subprocess.run([exe], capture_output=True, text=True, timeout=5)
    return r.stdout.strip(), None


def _ask(s, actual, index, total):
    secret = _secret_for_snippet(index)
    print(f"\n{' ':=<70}")
    print(f"  Snippet {index:02}/{total:02}: {s['title']}")
    print(f"{' ':=<70}")
    if s.get("platform_note"):
        print(f"  Platform: {s['platform_note']}")
    print()
    for line in s["code"].splitlines():
        print(f"    {line}")
    print()
    print(f"  {s['prompt']}")
    attempts = 0
    while True:
        n_lines = actual.count("\n") + 1
        if n_lines > 1:
            print(f"  (Output has {n_lines} lines -- enter each line, then press Enter.)")
            parts = []
            for ln_i in range(1, n_lines + 1):
                parts.append(input(f"  Line {ln_i}: ").rstrip("\r"))
            raw = "\n".join(parts)
        else:
            raw = input("  Your prediction: ").strip()
            if not raw:
                continue
        if raw == actual:
            if attempts > 0:
                for ln in _wrap(secret.get("explanation", "")):
                    print(ln)
            else:
                print(f"  Correct.  {secret.get('explanation', '')}")
            return raw
        attempts += 1
        _show_wrong(raw, secret.get("wrong") or {})
        print(f"  Actual output: {actual!r}   Type it exactly to continue.")


def main():
    _banner("Activity: C-String vs std::string")
    print()
    input("  Press Enter to begin...")

    compiler = _find_compiler()
    if not compiler:
        print("error: no C++ compiler found (need g++ or clang++)", file=sys.stderr)
        sys.exit(1)

    print(f"\n  Compiling snippets with {compiler}...", end=" ", flush=True)
    actuals = []
    with tempfile.TemporaryDirectory(prefix="cstring-vs-stdstring-") as tmpdir:
        for s in SNIPPETS:
            out, err = _compile_run(s["code"], compiler, tmpdir, s["id"])
            if out is None:
                print(f"FAILED\nerror: snippet {s['id']} compile error:\n{err}",
                      file=sys.stderr)
                sys.exit(1)
            actuals.append(out)
    print("done")

    answers = []
    for i, (s, actual) in enumerate(zip(SNIPPETS, actuals), 1):
        answers.append(_ask(s, actual, i, len(SNIPPETS)))

    print()
    print("  All correct.")
    passphrase = _decrypt(_BLOB, answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
