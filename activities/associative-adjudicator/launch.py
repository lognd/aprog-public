#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/associative-adjudicator.json and re-run the generator.
"""Associative Adjudicator

Requirement scenarios go on trial: for each one, decide which associative container -- std::map, std::unordered_map, std::set, or std::unordered_set -- fits best, and why. No code -- pure reasoning about hash tables versus balanced trees, keys versus values, and when a plain std::vector of pairs quietly beats them all.
"""
import json, sys


# -- Crypto / obfuscation --
_KEY_A = "84ce287972a5b7bd672a7ecc86c9e879459b91d44451c9dab268731650956d86"
_KEY_B = "9fb4d4e57993c922957b47ee46468600b939de1c2c8a918f14da2e181e3dc196"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None
_BLOB = "06bf2931f9d297d655688d554ce774bc7ded3069c7e8de5046356376bf0136647aaf1b55056383ea47d3490ca577ec37dfdb9a06bb121a0635a1d5"

# -- Public questions and encrypted answer/explanation material --
QUESTIONS = json.loads(r"""
[
  {
    "prompt": "First, two definitions from scratch. A HASH TABLE stores key-value pairs by running each key through a hash function (a function that turns a key into a number) to pick a \"bucket\" to store it in directly, giving average O(1) insert/lookup/erase but NO ordering between keys. A BALANCED TREE (like a red-black tree) stores key-value pairs in a tree structure kept balanced so no path is much longer than any other, giving O(log n) insert/lookup/erase but keeps keys in SORTED order when you iterate. Which of the four associative containers in this activity is backed by a hash table? Type exactly one of: std::map and std::set / std::unordered_map and std::unordered_set / all four / none of them",
    "hint": "The container name itself tells you which two: the word 'unordered' is the standard library's way of saying 'this one does not promise sorted order' -- which is the tradeoff a hash table makes to get O(1) average lookup instead of O(log n)."
  },
  {
    "prompt": "A program needs to print a dictionary of word counts in ALPHABETICAL order, and does relatively few lookups compared to how often it iterates the whole thing in order. Which container fits best? Type exactly one of: std::map / std::unordered_map / std::set / std::unordered_set",
    "hint": "This needs BOTH a key mapped to a value (the word AND its count) AND sorted iteration. Which of the four gives you both?"
  },
  {
    "prompt": "A cache needs to answer \"have I seen this exact API key before?\" as fast as possible, millions of times per second, with no need to ever iterate over all the keys or care about any ordering among them. Which container fits best? Type exactly one of: std::map / std::unordered_map / std::set / std::unordered_set",
    "hint": "This is a pure membership check (yes/no, no associated value needed) with speed as the only priority and zero need for order. Which container gives the fastest average lookup, and which of the two hash-table-backed options matches 'just a yes/no check, no value'?"
  },
  {
    "prompt": "A program needs to deduplicate a huge stream of incoming sensor IDs (removing repeats) as fast as possible, and the final list of unique IDs is only ever printed once at the very end, in NO particular required order. Which container fits best? Type exactly one of: std::map / std::unordered_map / std::set / std::unordered_set",
    "hint": "Deduplication needs only keys (no associated value), and speed matters more than order here since the prompt explicitly says order doesn't matter."
  },
  {
    "prompt": "The exact same deduplication task as the previous question, EXCEPT this time the final list of unique IDs must be printed in SORTED order. Which container fits best now? Type exactly one of: std::map / std::unordered_map / std::set / std::unordered_set",
    "hint": "Only one requirement changed from the previous question: order now matters. Which of the two set variants gives sorted iteration?"
  },
  {
    "prompt": "A student writes: \"I need to look up a value by key, so I should always reach for std::map or std::unordered_map, and never a plain std::vector of key-value pairs.\" For a collection that holds only 5 configuration entries, checked a handful of times at program startup and never again, is this student's blanket rule correct? Type exactly one of: yes, always use a map / no, a vector of pairs is often better for tiny n",
    "hint": "Both std::map (O(log n) per lookup plus per-node heap allocations) and std::unordered_map (hash computation plus heap-allocated buckets) have real per-operation overhead that only pays off once n is large enough. What does a linear scan over 5 elements actually cost in practice, compared to that overhead?"
  },
  {
    "prompt": "A dictionary application looks up a word's definition by the word itself, needs the fastest possible average lookup time (millions of lookups per second), and never needs to iterate through all the words in any particular order. Which container fits best? Type exactly one of: std::map / std::unordered_map / std::set / std::unordered_set",
    "hint": "This needs a key mapped to a VALUE (the word's definition) with speed as the only stated priority and explicitly no ordering need."
  },
  {
    "prompt": "std::unordered_map guarantees average O(1) lookup, but its WORST-CASE lookup can degrade to O(n) in rare, specific circumstances. What causes that worst case? Type exactly one of: many keys hashing to the same bucket / the map growing too large to fit in cache / running out of heap memory / iterating in the wrong order",
    "hint": "A hash table's speed comes from spreading keys across many separate buckets so each bucket holds only a few keys. What happens to lookup speed if, for some reason (a poorly chosen hash function, or an adversarial set of keys), many different keys all land in the SAME bucket?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "2vHQN3oOc}D9F9SW48QrscnLH@R~V~U!VydsowNKmwtEn%B3VfnVc{o@ne4dwZ0yg7@#J>BcXY3+UdFqPUv3EDe$kI7FpGV*w!M!^sUqb;)K_^8gggk$p6Ech{Wp*zB>86JkS3ARXSF^$wcUD*XWK5>gXJA){pIs)UnXtP@!Y{quNqbEh}+Fl*%8^KZRyyyaO2w8_O~2F3&ZJ{YFZi!4VEqp<3<b)tm9`jg%Yx%HF90oV3|NRS|m<D>*B(8kogQ?~j=KM_|8dW>_$4a9>TitqnsSw*R=R*uLQf!M(xMm$ljCu<Q4F_2M|DQbKq$(_aTIQ-_q_%}Mexrs*BMksR0VINv&Lsa6uuQeKN?teWjAYMIH%QKIOcy6}<zmMJfSd1bFzaKm8AR({B>G>x@`7T_T<Z8gHtsTn1O-Fm7FG_qh7O~>~xER2m5J^raQ)LIeMH8^*qq9a>NJEI5r-~r&Km?T<_x`Lj8K;lq<T1K919_tZa)EgaA>#*LD=^@XRX%3TWFHk|C7#2E0rH5HSm=Ts1d611)9Odc%hk&1F-GhC30SyM_KWbG4m{ft+_#EzZX9L0W-2sGWY3~=Q-(#uxBlHH4ZUU};tnH{3qmGhSHx;6qg=@MwbIPK-PHYF?aEO*2kC)%RpG$7iBr5crJkDasQ|FGk0yhbg>kpg9tdCQe?Y!D<K4$$ap<a|R?=x_g(6g=I5x!3~?&&Z%Z>d%QkD=N)j^w8Gb0F#XFp;#;A^k#!!y*4iBPPvFJu7qL`*8rE{tm;6&5>Rh83R#@<6A<WVkMQvgAmWsHwM=766MOxIZ^_`3e&($E@_j83WcqOlFh{1gwy&8-{+U8dK!GqU>W?uoik29auTcM_mFD7Kkf&9{IYEw05rWO4p>azMmm^PA2i{e<_ot?@Yg!KJB0",
  "9#-^FB-$XD6#`Cy<!m`isc2Tx5`H9s9M7f=KCF(13F0i$Z>zDDQ%>cFKhtTPR7o*V!*@G@CbY~B)t#;cHJ*kSHwSp6zGMyfD8nrT?%Eb8>Zu-+3UA%|M!z}rpI9;N+S3uR+kZY)=@#;wG5Lns=q$5jzWcCCDQ@{-4KKr#K^n0IAI6?fHl?W7H;txo-VKcbt?NirngclU*qWq?p|0^mgq28x>x0ij8cOImVr!j>3-Fic)kfc&o=>&(HL|gw)!8_z5Dnk$;5urH2ETK(oTBmsIx<wQS3{o*I_GH+^ocF>QWJuqs{5ky4{NI?i&2^5BNdc1uxA<kDcYX4l9o>4AlF){djz_P6!*$uhbNK7K8eqVj>tlr`(L?l7^>}VDNZM`zJw;g1uyhy_r9hF5(s8(qelBZpT1ofh55|KyQ|-9U|J1`HncCjuggv2b3}Zam(cY`qdvjWw{nuWE_FooPcV)w?&cfF0r!z{4&VkF!|QpvYMX~tS&hBl|0COuHj?jt_gK>ms*N-4v}v?hKZwG7^}!8tke_?(#Sh`9Uo@uU5DUM-Jtl#sjRu<k$v~^U-teiHZ-jZQ=<;cqFr=T6=&P7qrQ-0sday1A!zqtJ`f6bkB$yAof#?jo;+B>t8Q`2X|NAIq?chnITGj?+h=1;shFpzN7wj4JOms-gojF7@3fLZhMwGG9)2C;7HzRH2j)z(T2-x{;!6va)D}yk;v`%=BVJwodV~SAZ2re2CR8J=kEbgbojll1t5$>uHr=ZUtfQUE#1)BE}0{ns3@B?>pNZ5<T32zRG^X(Slh2)JBzT5~kgWp6ZN`N+Ld_C~>O*^Y5zB9wAwanI~#jU!8Cm4--OEO3RmW^O0@+XldZ^0MxEw|)%eDn6)3YfCHmAxR1IbkW6=cwV2#~hfAGyLXwi^b+y;Vc9eE~Nmqxs&P#OSq!+=InZ@#vD{*Smu#Skmw|^wJC#`(+c!(No9lq)4BdUjVt*qzlBy<n)1%o3sf;C*vQAsnyfbj^_!g3^P+}wiJ9!5NDxG#R{",
  "8lR?V4uE8?aQf@6a;y3?D)J;au5t>kvL4cmR<GQf0JzjRB+zXGLK1%YEI``LEHQ#kOfFOd1n$AvTDox(w0)gCPel!za?<RYl?+E;#R9Cl%;db^@zS$^Udf?dpr>elSecUhN<tyM-ORFvjF&(L%Uozmq&$Gv)+`P>hnej5V2B$QpThe8LR+MEN%gnEjKPC^=`Q>t1@O+@+TcZ?e=-s0ui<ElTFlx|PSYRX2TYyG?s<dmGUEuzs${GZ!Lhx+QpXiLkbdKgPyQ2XVRQUmpYr^yrak=QmGT?C&vVAHeoeLJ=IcM*47H9T-&dWy)qB=~RMPmo)M|>im%PDt8xBq%hj49MJWYlT_?*hSo~J&5Z8ja%vOXXp0BGM9-f4J4Rk9zrGjyX#>dQ<~ylOX$LUu22RF<i<JH4!}Srq>p)2L2-vDp{?%D~-~^=2Z;v8a{$=!d*U6`w91WG$;l5ZSuoRqhj=8}bP3b@hXtfMMPw3E+EW2XTnTFq#eXgNu=j*goaHRa&m$;Dq&Rcl$r{y${_FAo+HXxD<oL>+ZCc*@zsFAjHEMS{G$6h2saZ>)Tp_3Tdvo!J+wjM~scW^1r{qumiH9SY5{wUhr~YSQq;5<B+s`?Y+fN7<SVqA?#dN1bc<QGB?i!z|AyQ9iZQe@s^RC>Lljrg&-CsLcMTrlHC-CQL&Br$ZQU`Hyx;#vn7?K2McFq^3M-JsbNDbgK?aCUmzwma1O=DYmeITx!g6@+HWL21w(|EN$1&n-PI@QYf7vGy*hB^N>n^Oqd8`2*?ms&S5~z)FSsutxemFI911qXPBml;%a(eu8~&9*V62>YpyuRmlz4Ta!hJmGI|iN06LH8l5-Ld1q-tGL3qm;pZalBA_TFfk9N!>+wyEX&>|))R7QUupkgcKAd17x8v#bk33Kf-d_o}8{WTEiX3!`h3<J_4c-Hu6J)Z@2qtIe~;B$|5Pf1!ANKvFR",
  "RPWOZD*u)&kwGVl<%SAgp)X!}!OXF^V;9yX&<%VB%oId5%|k+uV2t&Nx)zs?oMexZSDn#=t&${H>@^fSq6?X@eek#t??3py3D0_#K9zOIiB|(5niCUtFs7y<7##@KyW?cZVviAJmYsY&#z_-;-1f!8ks|VcJbxOrPAUnRCq~fAzmAdlUBi6W>3dQs3`GFNa{7hDc7E)bY1&J=BldY$>p&Q0I=Y~^inGvOy_=wjw{}Ihm1Iv^Y^0AD9+Y3Pp3cJzuRmiO{1d?<ic&#WS3{f&^EO25rQ#xAtCcFWp=8yH_(t!`DM2%fuvY{YBO#nq;^nz%g#0p(l-zMBz2DeR<GITWl7|>Q3^(me?bh(2l^We)f)Ul6h@?^;6{oR{77u&Nn_L9UIIvAM-#I2s(U_sup7&O)O8C=p(^OC-&~6+Gk}4+#uE)dH4J|=(7~d=;kOII%xarF+GvC4Sm^WeAd&w{nGU}&~`rS6R`kW3=jIkJS)%=Ssc)bs8iz46n3$-|T>2hRLQe1)tTJRT)eWO|(w-bFU5#Fjkpo;3NL^Jz|-xEWtg&RW*tWgcApE{hFAM10mNf!L-JnhALV&r|LLiH7^GVSGcO1C9#d2Q|CIU|#1&#Sg`FBv{=qOb}~g#pYm^p?fQIYmIf4Oi-^ppH=1qn1t-eYJ$Mn(`s$GYZ~zMD;OJxogVu^Lw-LTt`O)+^;en(NQ1+rd;OHpmVO%QM(c?%o4Yh6~s?&>m^7F`In>&Xvv@jkS=c<;*8v(nZ~Pz<EADt4_cA?mR$;tQSUFgetMo6x#ujfr5MHa3U`<@e782xD3+s-)^K_6n_K0^Oqx=r1Cgb?AAt<l#0IMto~2!vJ!%IM{BcIR0;4<@MJZJ2HE`Nc{r%G>(Re`6{T$u}ee{Qy|6v46!UH9%oSH8sGp1n&7Jb>^pF^R9aWOyC2)r;o9ny7mCCQSJnVz_S;ffNi6^4KQHPr$E_3&O*-w``lp2U2bO$b3)X?J<&JJiLpQ<&POaE6B;QU",
  "R4@=bj-^d~#8Hx5h|Wl^u#%{9ODT<dHwTnxdcpKqjCc_eSzr0Oo-P9Hx&zwK(1-2XiWJ=D5>_XY8QEL*As=XhD`z61`V-Ks%(0Lry9mh?e!*OsR>OJmcYQAeP6(J~L#c9r<Z;`_UO)$l&vnV*xpw!!g}Fl;xPhf)$}BbLP0+Oo;p9R`jC(gnA|XrkMDQKos!|7}K+X#0T6CwEP4T7l+#oj{Gr2@Y^vAlPwxOZi7$oe_)W->F(n>C?2_Cx+zIKOGwt=wyMpB$KRI54$;}8!$1!hq*2aYQTBG4#e$D8w0vO0(oa;Zl!P|MpmhUYZLpCsG9x4=*20f7d3Loh}VdnSpblZY0QTt3L1r;*Ccn{|7s?SH@vnrT2k6uD+@`$`HDwl!!j<my@NO;a`5^Le%VRa`b|q3Vu<R-YbK2*ocP>^Ur=(e>+aF){uzl)4W3679LU*|`|NAlQtJ1BW3aoV7N~89CUwEw0R<Om*C4^5KUN`ma6C2MEBOYjrMAz65ivYd!tWDVDh~zriG-lm#|Z;i&HZ)Em0p@D%ZXk}=EZt-C+sE&0rfsl|R_7Wb(;Y^m(AHd+A(n*%Bs#MXD`L=GCDm4|j2!S5W4C?pCt6PL%{oL_KIB{(K7#-pp$23DK$19L2hc2}GoIzp2%6-)4bWP%;4kmMR%`KAqJB6YxUf%`MaOF(g7UG(uHn^gN0^IP<&)2kY2p7IyUoXGp=M!!gubbt#yQ$*60CVL5G%tZg_1~3kiX3r$sj+=4@%}02{E~#onkobaFt{kSx@YG}nD>57ned1X<7>o3aKdg_XYmh0%f>D9wi9Tfyr2{yZ0{SodcZ^?u`+~yF(s|<?%I85H7(&4NDKKTK%3LfYfEOF^W0x^Gn>r3fQNBZxciES1TOS7#QDCVJt@URqzvC^?&GUfRwF{;Rdpy!DR0}q%G2tvXGHM1i9)KWRs&kjC+rE5tmU#7yvq*ul7xGj-5UMrxGjown<OfV_+r^M^pER?+p#C-rEo==;-X+3@<#dBR4jr7}Fu7z?lYoFIFxL=^G-$C3WY7E`X+BZ^d)YpD^UEinOWOGmHlNfhrSqDRO1oh7s=^v<$VOV0;u#%pmoE@*CzL7YcE1h",
  "kIF&N(jhYZukBQ;Od`s+M;Dq<A8X~K-oIA6M}lI%&BFAeJ}GTBCleaBuHZA`ZmKu+=o;nQXkNb~6tykxJ=`}QCAV=0Wk6GHVcL7cB}|S}(T4p>Zb6BA?E%gB+>C~uS&<y+myb4z@N4BCNlVQj{X59q8bJ6>bl#K%0Xxt)&yEbh;LM4zLp+Ag>Y7)%Q<t#+_rL^;pu!(@+fmh<Oc}=#hz0PC8ZIcv#jgAPT_+XMZWeXyJTa-Ho)Yg&0J&0%{hxH7K}ao-gDVJ61Rf;`^wAymuS(oE6~kq?ydoO4zs-mZlv+bbC0)0ug^Ra#1y_$50nN5=&fLVSquzdxO&7e|V4nXZAdugM(CTxY>vad#K!}oy4C1^-L!!cFD=Z-hROgD)9N9(hY>!k$veqG<DEM3r#4L|M=yDb~T|nfSCq=t-*+)@M9RM?5Q7kul8p|U6qqK&A!0ZNM(_nM-T9jw_uxU%C8WeN5;KB~3J)m<8hnc`0k{##)SsbZC(wCj<zwDae-;sc2&D!2-V!*MWk1}45x&3%QbxtUL|2C)-CYq~Ah|9BA1*cI~M@Z@V1E67aq%j*Qh?eBAP2oqo>g!<4^B_KTKxiur5nx(ANOQAefNvd$OSOhgGkyvJfMBZaLbZ8T!Q~IoQQKteN*j-JqFt0Dc`v(P5Z`*UEdR7c9hn(NEw7`@2T~|;902V#x;~}PE$Vq=13})wYo@%_Rq%;?avthFfEs<|P^vtS)~?N+cD})J8a=jQ?C*a^B{i(^!<<LmWnb2t=oc-7>qy{<U;JUl4ywdEAt0SuP6bcKc9{tW23c&j>MIIU`WdjahToU+J$1S+GK7mAsnk11Uu11)dqP2XvBFbYz*`|Y-zn5jam3N6dSK&XP?+*RbIZFy@s}>Y=Nzsa1GpZr;{s?rwfwsCUA7E;&lh2AhLQ*J4=0Y-bm?=LHdp~sn$MCCf#phY80@M~I;~@sqs<x>qkyTOlpvK7S?aSts0RF?;A<+js%@w=8l7hG%I|fblQu+^W42-KKX@o5ND|0|_2%<dNQzYfSKik*J;<#dt=qEe7iJw16R(jQ!LnsGfO}-4tKQEEOTOIlS^q5^)pcBy#!&H;CTDcSXXo0i-&!P&C)s4@{y9p|bmIfyZ2|f}X^Lmcz`k%TY{HK8cHt+F*0}7HfbmR<c0}azMnXgAi4rpBi<|<y<zoq$#YYfsyu$1ipx4pCs`2t=<h|3KyROZNf%%a1gg9IGXr3wB-zaVQp}DlvO}}^pwTRRfY$8&jTIRB>U2MqLdO^(MAXeA#Wxo",
  "%w~XQZfIuXqz?J`XSrv1Oi2;=&GJs20{|sSeq4kZi`FM(i6FG!OGk2jIfWu+KnCZa8BSi@2>C`;NExF7e)#yAD#@yqhg5Y<DE9ceQ6h=0OOe?OPIrfh$qd3EYx$k|ItP=v1Z{EKFpEQFy#DPcb^b#d&){ldJ^q+t9Llx(0FPBM9?|1{ja_M$$WqfU5Jx{l><Q!Vgp3ziNflZsR?-QD8eJUgS)>od!@<}9T7wobdq^`W57DO^hP|fy&BgG@XV{7OE-vW68mx>LtC}MET_p|ZF%`k2jbVj5%(nEg%U-z4L%JXdmdnv3pF1*pTXXH%H7k8OFfuxRSCa=KSc20hppRHT1U4(Iwe&*<D?8det6Hrh{WwK>ZeMXcvp%Xg^a^rf=bX6EsI=W273nW?*92q#q$cc~x+@TZFhif4)2jfOs}$CL`nvZ~X~5jA;jfJJ@Zu{gllBlpps+aUc)o7woQ_y&>^W02J=s)+yPB9daIYE4f??hvqo(3t0b1p0p43%%7h@fjB@1AAcfHMw=9D`D-NpIukOQkY3DwO@NgeW>x!r3?2waS-U2W$mBOlZm(dZ|<F2@Nj4s$Z*K%G1@1{V=o8#QPO+WDGb<cFAT`YQB`Cgp^S(OQQO%;bZE`Wvg|+F=KUsZ8YbZ(7mH6`~RTQT@<tq>?zb0#!cAR+l1f9C=d2CQL&CCKbILGv39+wKl31^^SH-^N{kD7)5#jC9f(OrN8^Ip|OldR{4tDhg<=M43ex4$@&x+R@z-b2<``>-$dfDGt<>8avXEVW`7r-+xw*q7&sgagyZ?A%ck}8X(LGa",
  "nKfTwYbMSHLDO2*V|^p7;4PSu;e!0f3%r1BdWJc|7RC8hBvCDAIuwDWJ7_xYI{WFP9Dp|6<LUcpi|+xc*sM3{!u)v%2ytahXNYKq(@TUopnMmI<Sal@$qZb!S(KpFhuVVTUeu3w5yqALge(BFj=5-o&39cj?CTE0t#2eEDwmq3KO}J<`ND6Gt7+S)91ml`I-Cmo$hL#tvy=cyQ3SAkT`yXHKu)-!@?)fLWvpoLc$5*dM><jZnVGt2xUUu)sBD)!$QSJ>`N@f6&-y^daO$4rs~XJz1aouYzjZtm#^+N~ExJ&OHh54DP~p~2*Yo8rDL%`Jo|h+<2+<>O_3)ne2G8d;WbQ4O-xNowFu7T(eo!$uFGbPl&pBtBcUE_C`bkrV5|1|Ldgm0wt1W<XxCWd@SrU(<>ApwFpX#ML=gPM5Eal&B=1`9fSiD4x4>vG%JT<1uEIZk+naZn1y2MJ|ZEPs!7?j>20x%vERik^k0=N^`uW<B0jo}uplMlq6VSuA!tsyqI84Jq`DH5uFwf*sv)Fxb`@5|Z2kQhWw9;pOsl?bmIPm#X5&n2^kE*AE0-&#`SH<+T1Y+@pyje*oaT+#DaeY8K5f+fdIitQ{j0q;doGZE`>S3PsgS#4cs)P%`8G(%$Yl&(=heQCl+(|*57&Clp<#Tdy~tE5@f(d;;=f!xVE<5X?d#A5e^=g*0r*$at(ipFsF&e~L@%7Ke_MhmG5;`iCD<uWrKGX@^Q19Gzp0%l=(!^f`E_Q-a@i02Wx)i{ZJcsI2!QwlX&f1Hk&g{W%)D{HI{pu3cO<sN@n&0tVOjETov2XmUd+XrB#C{3emVxW})RYzrlvr~0j$X$H>9BAhu6X)%qa<56lO2_G$sj7FZvv4~1YgyPdRgpz8cFzFP`Y8pdum@=Wv4iG_RFl$32I^hU)f`;bKj<#kJRNxYAc<xNv|{~oS#*ca&gZrB$|JnKL1CbFMHyzaC0Jb8ffGSC4OO$<r$8e#OR^QgVU!G@3!U+`lF0gW?vlbkzFN7Eg}k;uMkV+Rq}ofDX_20B*B;o74}ETwK-X>BC3u86&;C6)ZCN^8Dqal<`F)6grm8vEXALW)b@eqJ=Cg%Ii8CaAxMqtFnJ+J@_gY|!<9QVrZ6KC-6WuP|*S1Y`OkCWz5`m7I;T=!(EnpR7ib?=0SYpKVj@u-2eIVRPeNUZ>s%E-@kKP};;{>V2(dOO`P5cSpNnkk5>*FSSUjO^ct$IhIGv=92?a)AOL1nv#<X8Ivw4QG*<wX982`AA96?Qwf{x}pNfDvXsNlxxdOldhv-pV4U)EZ{=dzW-Nl1PvD2U$R$e3e!-tr*s%??di^yw03vaP$"
]
""")

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)

def _secret_for_item(index):
    """Open the encrypted answer/explanation payload for a 1-based item index."""
    secret = _open_secret(ITEM_SECRETS[index - 1], "associative-adjudicator:question:" + str(index))
    if secret is None:
        print("  [error] Could not open item data -- contact your instructor.")
        sys.exit(1)
    return secret


class ActivityEngine:
    """Core check/passphrase logic for Associative Adjudicator; importable without a TTY."""

    slug = "associative-adjudicator"
    title = "Associative Adjudicator"
    description = 'Requirement scenarios go on trial: for each one, decide which associative container -- std::map, std::unordered_map, std::set, or std::unordered_set -- fits best, and why. No code -- pure reasoning about hash tables versus balanced trees, keys versus values, and when a plain std::vector of pairs quietly beats them all.'

    def items(self):
        """Return the public (non-secret) item dicts, in display order."""
        return QUESTIONS

    def _options(self, index):
        """Shuffled display options (answer + distractors) for a choices item.

        Order is deterministic per item so the correct answer is not always in
        the same position, but is stable across runs.
        """
        secret = _secret_for_item(index)
        opts = [secret["answer"]] + list((secret.get("wrong") or {}).keys())
        _random.Random(self.slug + ":" + str(index)).shuffle(opts)
        return opts

    def check(self, index, raw):
        """Check a 1-based item's raw answer; return {correct, feedback, explanation, canonical}.

        For a concept "choices" item the raw input is matched against the
        displayed options with small-typo tolerance, and `canonical` carries the
        exact answer string to decrypt with; for other items an exact match is
        required and `canonical` is the raw input.
        """
        secret = _secret_for_item(index)
        answer = secret["answer"]
        wrong = secret.get("wrong") or {}
        item = self.items()[index - 1]
        if item.get("choices"):
            matched = _best_match(raw, self._options(index))
            if matched is None:
                return {"correct": False, "feedback": None, "explanation": None, "canonical": raw}
            correct = matched == answer
            return {
                "correct": correct,
                "feedback": None if correct else wrong.get(matched),
                "explanation": secret.get("explanation", "") if correct else None,
                "canonical": answer if correct else matched,
            }
        correct = raw == answer
        return {
            "correct": correct,
            "feedback": None if correct else wrong.get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
            "canonical": raw,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all answers are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()
def _ask(item, index, total):
    print(f"\n  Q{index:02}/{total:02}  {item['prompt']}")
    print(f"           Hint: {item['hint']}")
    if item.get("choices"):
        _show_choices(ENGINE._options(index))
    while True:
        raw = input("  Your answer: ").strip()
        if not raw:
            continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return result.get("canonical", raw)
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print("           Trace through the function step by step and try again.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    answers = []
    items = ENGINE.items()
    total = len(items)
    for i, item in enumerate(items, 1):
        answers.append(_ask(item, i, total))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
