#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/iterator-walk.json and re-run the generator.
"""Iterator Walk

An ITERATOR is a movable cursor object -- a little value that points at one element of a container and knows how to move to the next one, without exposing the container's internal layout. Every snippet here traces the core iterator mental model by hand: begin()/end(), dereferencing with *it, advancing with ++it, the fence-post picture of end() as one-past-the-last-element, reverse iteration, map iterators' ->first/->second, string iterators, and the safe-erase idiom -- with every printed output fully deterministic.
"""
import json, os, subprocess, sys, tempfile

# -- Crypto / obfuscation --
_KEY_A = "06fd56214c8868fea196650e8666b3effbcb02d7e88537660d1fa65990145399"
_KEY_B = "2687c7d479013ae4e9dc1925d27b88bb0aa732c4e63a4c1af0ea70016af93b3a"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "5a2117e318176facd92e5601d08383cad2d094f8a9998bb0119f8e8a1d1fc13df43a07a61f26f8a4785470e45db984e44dd3c0ab51"

# -- Public snippets and encrypted answer/explanation material --
SNIPPETS = json.loads(r"""
[
  {
    "id": 1,
    "title": "begin() gives an iterator to the first element; *it dereferences it; ++it advances",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <vector>\n\nint main() {\n    std::vector<int> v = {10, 20, 30};\n    auto it = v.begin();   // an ITERATOR: a cursor pointing at the first element\n    printf(\"%d \", *it);    // *it dereferences the cursor -- reads the element it points at\n    ++it;                  // advance the cursor to the next element\n    printf(\"%d\\n\", *it);\n}\n",
    "prompt": "v.begin() returns an iterator -- a cursor object pointing at the first element of the vector. *it reads the element the cursor currently points at (dereferencing), and ++it moves the cursor forward one element. What single line does the program print?"
  },
  {
    "id": 2,
    "title": "random-access iterators support it + n directly, and std::distance measures the gap",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <vector>\n#include <iterator>\n\nint main() {\n    std::vector<int> v = {10, 20, 30, 40, 50};\n    auto it = v.begin();\n    auto it2 = it + 2;          // random-access iterators support jumping by n directly\n    printf(\"%d\\n\", *it2);\n    printf(\"%ld\\n\", std::distance(it, it2));   // how many steps apart the two cursors are\n}\n",
    "prompt": "v.begin() gives a cursor pointing at the first element. std::vector's iterators are RANDOM-ACCESS iterators, meaning it + n jumps the cursor forward n elements directly (no need to call ++it n times in a loop). it2 = it + 2 jumps two elements forward. std::distance(a, b) reports how many steps apart two iterators are. What two lines does the program print?"
  },
  {
    "id": 3,
    "title": "end() is one PAST the last element -- the fence-post picture",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <vector>\n\nint main() {\n    std::vector<int> v = {1, 2, 3};\n    int count = 0;\n    for (auto it = v.begin(); it != v.end(); ++it) {\n        ++count;\n    }\n    printf(\"%d\\n\", count);\n}\n",
    "prompt": "v.end() does NOT point at the last element -- it points at the position ONE PAST the last element (a 'fence post' one step beyond the final element, never meant to be dereferenced). The loop counts how many times it advances before it == v.end(). For a 3-element vector, what single integer does the program print?"
  },
  {
    "id": 4,
    "title": "rbegin()/rend() walk the container backward",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <vector>\n\nint main() {\n    std::vector<int> v = {1, 2, 3, 4};\n    for (auto it = v.rbegin(); it != v.rend(); ++it) {\n        printf(\"%d \", *it);\n    }\n    printf(\"\\n\");\n}\n",
    "prompt": "rbegin() returns a REVERSE iterator pointing at the LAST element, and ++ on a reverse iterator moves it backward through the container (toward the front); rend() marks one position before the first element (the reverse fence post). The loop prints every element from rbegin() to rend(). What single line does the program print?"
  },
  {
    "id": 5,
    "title": "map iterators dereference to a pair -- ->first is the key, ->second is the value",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<std::string, int> m;\n    m[\"apple\"] = 1;\n    m[\"banana\"] = 2;\n\n    auto it = m.begin();\n    printf(\"%s %d\\n\", it->first.c_str(), it->second);\n    ++it;\n    printf(\"%s %d\\n\", it->first.c_str(), it->second);\n}\n",
    "prompt": "A std::map iterator points at a key-value pair; it->first is the key and it->second is the value (map iteration visits pairs in sorted key order, as covered in bracket-trap). m.begin() points at the first pair in sorted order. What two lines does the program print?"
  },
  {
    "id": 6,
    "title": "std::string has iterators too -- begin()/end() walk character by character",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <string>\n\nint main() {\n    std::string s = \"cat\";\n    for (auto it = s.begin(); it != s.end(); ++it) {\n        printf(\"%c-\", *it);\n    }\n    printf(\"\\n\");\n}\n",
    "prompt": "std::string supports the exact same begin()/end()/*it/++it iterator interface as std::vector -- a string is really just a container of chars. The loop prints each character followed by a dash. What single line does the program print for \"cat\"?"
  },
  {
    "id": 7,
    "title": "vector::erase(it) returns the iterator to the NEXT valid element -- the safe-erase idiom",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <vector>\n\nint main() {\n    std::vector<int> v = {1, 2, 3, 4, 5};\n    auto it = v.begin() + 2;   // points at 3 (index 2)\n    it = v.erase(it);          // erase(it) removes the element AND returns the next valid iterator\n    printf(\"%d\\n\", *it);\n}\n",
    "prompt": "it points at the element 3 (index 2) in {1, 2, 3, 4, 5}. v.erase(it) removes that element AND returns a fresh iterator to whatever element now occupies that same position (the element that used to be right after it, since everything after the erased element shifts down by one). The code reassigns it to erase()'s return value. What single integer does the program print?"
  },
  {
    "id": 8,
    "title": "const_iterator walks the same path as iterator -- it just forbids writing through *it",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n#include <vector>\n\nint main() {\n    std::vector<int> v = {5, 6, 7};\n    std::vector<int>::const_iterator it = v.cbegin();   // cbegin() gives a READ-ONLY cursor\n    int total = 0;\n    for (; it != v.cend(); ++it) {\n        total += *it;    // reading through *it is fine; writing (*it = ...) would not compile\n    }\n    printf(\"%d\\n\", total);\n}\n",
    "prompt": "cbegin()/cend() give const_iterators -- cursors that walk the container exactly like a regular iterator (same *it, ++it, != comparisons), but the compiler forbids writing through them (*it = ...  would be a compile error, not something observable at runtime). The loop sums every element via *it. What single integer does the program print?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  ">L7#L4Y2R%7<gZJ-#{-W0K{*|*8;%!+{ChM(1yVYIBDduz)xS<atDUc`Yf~et+45GZ8vz3tok#Mg{C$8Ww#XL%_YRt)zaD~SinP>p%8fAOhjuBo&pGn0UzTa#T<x|m?8a&k&gm#%r+7PpCA;N^n7m=iaE0P3-#Zjbsnu9*t<uAL`2U!ob^wDlo_uoTR)#tg7E<uUcXT4E(vcYCMx(ZB{fXWR2a|?>+M+dxw(-Xw+7Q9ZXF2Kxa%HOgyV}N*_fI&gBM$C?u-VztEog!`=vzdF^#{19fZG-^we+8=(HnoZA*c;k}hF8?TyZ2k^-*c`kXZ4#0n05uesLyy4SZCBCv*+nG6RES3-G)bDtNF>Ee~w%m5om4F{;RI&Y>I#&%Hm>=XyU8}ov!{;X3d>mtI}44sAt*UEASvRkiCd^U8>Goo!0F1Yo_ox&fuoaFZ2G4nks)o!@&9~4O8`C8r%W$g)tYPjzUt-qDMNXl-n;BnsELfZCF7XtMLm;#LT*z+16k?3imGXTRmgu#&kzWX?t6J^)wUF}bEP=m2&-E<~@PC+^W<_=<z`atd7rnQ~B`U2cRr(a13w%=^5htrzxl!*X)NQ4>ZJK%OVwrG(??+nn{$$hbhu1UqTOvM30#U=*A)MR#WsE&6?Mon5mKIu;Ma2|oPm`FSlcL9y)S(VaK&-eT}*JLPH`?>p5X*VNDCb;31YnlrQ2I5RH26osusd90L@_5R}C1(1SKT*Ew;-<&GkZjhtl!-}IY}ty?^Qn?Au_~}seIs$79#K4Ple~sLLCVK=^YQS<PfUbtXx#Eq{9bo}e65DEC0P|Yl#3E}QBqk0#Tgnlhmhq!",
  "0fAq`yG6tj(z+~)n06o9hvpaO08cp>;B9@}RH=!=lp>i;4V_>8*`hg2o$4qsSF=g0wOvG9>0HoLdv&d{5)O8R*lVaKVwJSfq{hHqG<Fsnr*Mpv#PF?&58{IkhJxJHp$I)Pa>|K9>7S$ahqrTR9khm!XOL~#Uzf|Y_HHD)$~Ra?Bjcsk%YlZs|HpRpBD>+!%qN9k(+P<ln#IR8=v^~dg9|G?Ir5xoY$6xLnB3;^u>#4dN_A7#OR?!uJ*LDND@tA|YOUFU=dmjLarYx<X%E$jhJ%|fnUH#=wKca7H17E^n{a7q=WndV#;?GJ8klxB)UUHDHidw+a>8l(R;XP__T=Di*l6_lXX?o8q8KnCkB<a3h=s6&X(#}o5l617%?Sewy*iM|P}GzR0wCk77-2M_n3It(<dH!ia5VZaB=tH~b}M;fU;WAfE~Ec!_!1WL@uJy>gqO0+D%`h7w-as^6Yy?=SDXxepn(8^Qr{b$(xp6xKTlAivV-}j{oonta<SmsH_m`lNByjt3ed{ep`83bwXZ<L5j?(Z5k>-E@{j{+>1rh-pxKcy4x@tn7}CGO`I!IqsVfqJOe9#uO}c9GNUA<Qw6zKSmzV)}h8B7?$}vTf1XJ^k`5R>~j$<>b#OWoKZvA_Ig4Li5-$q3MKHBq`Bzt3^H(qs!aH{<uhz!ZlarSvEX?00VM!(v)##GWszC6fDjFVs7)b2&=^u7mwZaT6)!-9OtoL=LA@(Z5{H>ViKZIu#U49jH;G>b-WS%lh-p!XmtQ>VA_w9{)4-uNI8j)H0ab-OZ^w(6NL_s+Ur$693Pz3fNHV^d_k_0ajVNwWtGYi8xwnT_x`ud!zDQ;L<&)gZ8Wp8eP2BWSAo@~jDW+!0^)^d53JGTGvSJ#?KF&m+thN0yZ4C#27S7N7iEE2jpdxM!xNo3Bk`@u@Fc3iE&",
  "6lHYtEf(ra*U883-N#6BK$BBwO<`0Kbk#Bd9xRhGo18dTwLfj3C0XL6&niC&<eIX2>}XA_R9Dk}d12QU|5zRr_qG9-Q&UyoS<Xpxfljw9?8nhU`Mhx$k%hjV9>Bh5-G+tUSXH^bZWeCJUbD%NLTqQk*_hi%J6Oypol|=OAKXT#;`;|>^3j<pf<nAnvyJtovkrx&T?J0&<?Pdn62s`zV>f5=8w~KI>bmbgjQ(DJCp>vMy4juTQ0+wU76GBU>wUUp8uP*R9uGI0_=SO!M>JaaQizud*gZ4R17*e=1aBf*6$3>&ijTSQ;(}|?*6K0D!)884<+<t%ct<=Z{(mAA07!sCWJ&(&6z>A%a#XU&OSw#Up##x{-sdTMxW(6+y|^|o+163s5XtzkTPt#;BOMsj`u=waq2Yn%JEuh%<NCFrSb6{gdv=hl<`(v}RFTl(U3oW>%Vu4#(?eP~AkKsq9sL}A=YAh$)wmc@DehaJxzJ)f^bH2ZtPnXw9_|GyR8oGsh@TySH{C+9BHK;IQw#lfud!yL6384}P!2Du!cf}`5_SR7Yoo9JEU)~f3!2$i&j6rW2LDol+X_PL`Ep(kQ|3KM2{po=5QkImC6Mc9c_4JpOyx-5E7C^v795YIJaLIj-~KbL954A7x8qS24Q!cA=9F9=FGgEfBy?eF?58-tLoL(-jfT@Edy}J^vbW=H4zNrg?2nfTm+;@?E*}s!iYV}A-73#C;lh2wFvtnhUx<B({Zip-8TIXoO72jG8*!W$dS_HiB-L)Q|McAF<1IMGj$=zit()^<{WFGY=zVXI3ef)>=3x287cxyGluQkOnXT%JEe7vX%Fn9qSGGJrT0&*U7rT4vY>xCDK~hbCp(i#Oalq@x32|gy5h)UkB7efgJ3(i}Prl-Bt4ElsndbQdv7n*U2-2{?sa$1WVvQ*&%ZDdplN0Xj8y^JgkDWtqeuP;VF(V!nyLzz}w`DsfG!(<uL4+)~^-O;Tgy8vdQ+VzhZz?sLPMG{^ZU",
  "mUtW<AW}-)Skrx3&YyC!tmW>M9uM$=4NieUix=%V0i6xr)bDouAt1T*JnMC`&Ic5JX!VXcJIsS!9D~8-aQrWX32cNJy-K3ys80;$xxU+!`<gRIU05tW_fv4MwpB7+4*c5ge7b5b;jAw*;^_I4BV*U=5d|HcXcm6TqVvmb#3&8t3Z#G@64YqBa#|YvwY1$?<XRsEqB|C*$v#EM13z?P(F&2OOD>P%NUuYiy#rRyoDS6rZC})-YBI5Q1*ILZ!pHHE^&yFuuwZ>q`A;$k05BR}`*YC2TmtGubq7;+htm)GSLR+HCbb<D_newWgS|txeqI#9X~R-zFG(k{#oa9gAW+{5gDdf7PJVg7Y}ZrQQr@E^^n%_UsbMjD{g=<!9@z-2R7eRJ^DpjQKxT@^p-dg`RV>6Z+t8ERI&8G=Ibs~~V_n<`ThzlMYC-1ve8g&n86!3R;40D!O|VL?VCLE{_sP|);dol{D;bFnznb>s-;dY4oJtpADTAMJFBHxj1jdNu1-YeC{mo5!fU0EaoYvaKGZ=5CBZfyV<V%uu8rX+gaRSsry9V~oRu>CgWm)trM|EjvWJchvrWXA*KDH<6C5t3X6%JYo$mRuqtLia#P07n?^a46y-SEmf9cAekV4L8Txjh_`^kltGt2me0ap?KGfs2_BRuuBVrSp0$FVhYnuMv~s*<DQ1pe{n(k|xNkOQV;{jo-Ergo)kVqW`E_pMbK)`@Vb!J>$s|Ri>A;Om@#c^V@n0N_!nPyXaVZq~L_Mo-<ra^<8nxN3IKemZ2y{dQt#F+tG}x?Gze0Mb08j;#YL?@DlpFvR^sxO*|4%r7DD^c9Cfr;K@FyU|h3W`a=fbQ1Sy~?2LH&La&tbedlJ^Pte5r@18&f$RB+rvHWUlJFj2ytnbW*SEKdt2i8{~_@;$ma7L2-e;~G|cA04Mz0_+{4WC>r",
  "KxD0{<E;iWk=!`%j{J}#wtKVbJhoSdb1+#U$E)Dc7~yHM`ht`*Y)4!%qZe1yv2O>q(6ns(v(LsLhD|j-j7B}&lB2iGt4ueqJAV-mX-{uWAj@Ek5R6Nt(;9>)MY#{{NRPcPSGx&W7+wL|7$t(oWzN+VWW>YtHJsR^S4rt~Cc8bVr2^X9Bh&-*sp0_vY`2n4G5{GE0nab4F3!y-GKY<V@No=h!pxUuz0tmMlIV^hhKUlOHKH-QA@&^lSSA;IDOS_|ik;9XCy_U|adwv=JiV}`9?99u0|w%hI-dWLK1o`D9BRvLZ>AhzR*Mxj9I+WP#UAeVJpgqH1t-}=wIJ`-25cw3I#c)OKJ`g!i9Sjn(-jT<40hTB>3paQQB7B(PWp@)(*xwms}fsX9kntRK6)Ox@(2?o#x<>TChp4yd(9$C=TD{qQV#LfxyMu3xyzMF8WJ9m!G<|yO-zDeem7i5mKd-L5zja^@Cuv6NvjeTS3yE~qDNIESK^QEG3D(@D6Wre;;C~s{8TbP%7McwUe7KVOjV#H|JuYj%;^MPHV?a&Rm8?(XetZ{OEy-UipmZK^toe)?8Zo+!nhD@ec{C-!di1Tc2Hr!C;6V`wO8tR{Fx_6D8tS069UiFCB^q$27zM8MH#98_{h$Ts_;mJ|G=*d9k!Tt8|xXjIsllH<8FxXc--Q5P21DU^?P2YbxunGB^vC-^s|$S0*U%<N?effn=8ndkPbs@HQhy);8-5~P;hIxU(CCpoKV1S_5JQ_qQBiBdg$Y*v1$2lBohkAL3p}0m@4EnvcSuC<Fapcf3*Pge>hE!2uk8gTrn4=6T93S`plsB280;5(6BBV2f?T)C%u$>;ti?P#~BI@6B>N9tS_?mNLQ7f!yGdNmYYgUV3`?=o8(@iMgq?}|0bjrk9W3Uev!cGC1y?svIceIK!&1H^(E+d?2<rtY;fNOwXCGdvSrFH|5QqJiPx)s1Kdqe?GC_~&Pd|9<=SR-#&$VV-o>RR%a}bwpluK",
  "52WMZ`+s8u=54Z_Dc18u8aGq9&kSm}2^%iWV2ljzSbq)tX^He?=Im-l;jV-38=wn~%5QE<o5m(ix~CmS<kCE(+aTlr<r4welMMDfg<b?hJ(}dxNHpaEfxa5^bA7Pu(A|TwlVtVK(TG!Zl0~*#+V3s6fx)~b!~I7U1CNP=7%R`Fb%Qy|uJOz3PB;2+<lz+5$rfMr(hDGcs@`PR&<&<6X7v|iStIy@oIAf@jwK#nP{3+_Kzu_^Ou5OENQ0t&HWP7X*=v8yF+sQv;$+-BbU!8K%|am<Om_Kx%3(HX73PE0u-xmWdu%OII=JbetM4IF(MCb^*?WNGuQUAuKRg1R6#s8VyqGjXjOr|%lN9xxQo=SvMjWnI{*uYtW=jjO{T_N>{Hb0apSxCb^s`KXm~>9-g2K=<7s>|^&WzZilEqkWEWHa^D1PMWpbF|Pjjx~hkX8tIMzw`cKD2mbzpY@x`L_3k(IC5xMQdxn*PU+ffU69x2NfwoGWN$>atc;z7!pGo^h;x}_~QODR>h2l9AY}3f2Fk!()o`zNaJm*@q<U5GCz7YCPsN4ZWKS!(Ti8}?WSMaqC@VHJix!CB%o^A*Sn&lI7Vn3Tg%Th3zU%`?j}3K17=M2uTJhQcS-rA$Lg;YlwGqdYqIV&s&PrQI0!Ev>f8c%sGC<GV9xtY#fjQkJ@z}qO1}~T&rb`cAZpNU@7HyZ4<OEdZDMmT#Qdw=w{TmjS>;rrWHU%HXI(BD?Mv62Xn*}?{l!%vUl#KrIOwWS^V#v<l84~hFHL(HNW6JDr}q-0*d3Y>U|KFAX@KZa%_>vsA=~t!2m`6L1i8Pl&A2Xf#y}D|8odFzwQaH7IY)~7$TdhO<~!X%%2U+r_!z6c_^DfBL_Sat3>o$WPlo+x<gwK-Wnpcamqu0oJ5FUJy@4;Y;fH(pPo)%o",
  "Za*QNRLa1^lM{6&!D1juZ8+?TODs)j&m8!X>$JEVB{>c?#4LuZYRHm;w>&OxBmb39mRB$rA8WCI860FV$VX`o<fAOa3I&09a)X0;ae_J)m9jHtzV7uyUw7Srje8rPCM6evC@y#qTGycMWRc4X+80@KMD!xh8VNz{8;pqs$v&dl{+u6}TVcX0HFbtaCpZa)F$(1DE7|93w6@Z}!$<pBF)~kQD805)5jL758r*0j$8)%n)u}k{kR`~!O-B)ZFM=(;I%$ja3hpUwC#82Vi9XM*)mGpbaZdZpfqB>8s?APf<4sGI$)OGGQ}gD@Zwo<P=rW~vS+!?+Iew0}7KxM4;Ixo0km}IH_pG*q><~WDfxz=e<N5lbPwSL_aJ3M5+S*r@1X+Q7-oN>J07D4((ks0@A|v41N3=n{xI{^%X|-?yBAd~i#50?WAV6OhnkQ(?rnQ6jhL@T9XpguyhLSg~IvHD^pDG2O<T2_#(<L4v@LByu<DG`gaeUnX!uXrN2rgykX<|WE(CpjFJMKS;P#xyVtKL;%!e|Z}O?>;Jl7nRuMZr|lD(MpeZ1mTDLeke@wgUYAK}TJo1Kf$ra7e{^V>=(u(amyBHz+W?u8ad*Hs>+}od+Wk86N1WrI#zt3a0Jcku}W?In~sZ(&rUCI(=Y>ElIvP-EU~)uYG@STeFZg?P?Yk3G3zTXE3*!3yaIWgN)S=aooo57~*|rXJ~~gJOm&asUVlI=vy7^Suc8Z3d172X?`QrV(&l2=XX|aO21W%bGrMrUgG)0wCom8=Bh(6kg_##ytLv78{3bw!vuKxysp|6*M|uPbKI^*lO%s2(%?4NJ$#M0b@)+h!GzYD3dRD-o_XS-06wCVzEwf0m1gjT{`fF{@#a+WWoKpuLEtfr(_ReHWzc@1D=Kc|sXyYCFTU2Kcp5knK1GCW@<7m+Q`dk&(TzsYd0hG@_|_!YZ*y}tK@pNtd||kRDSDd-BMO-DaL108#Ty7S1sda?Mdx0apBnMSFnRquSo2xRvsKLjTQrzSV$J4}3N!D_Tp-2<9(MOJjF!FS7?nYhYY%)sfHejs(}l<fs^h>JYa>ZK_<(>)!DrXy(%>-JZcLDP>;",
  "!^Hq{=)F}_zAR3MTe7m;%8%yqf)geF3b1WrIqb3qM|E#;M=mv~2(r3<YvRNkyzI|f*6%Fu+R%WFV>HEMDqFigZE_bUY3cJ#2LrxG>{a7NxD+U0ZG_{U+!NDY_^-#BzvRKlq?iv=vu4NS!h6t!_4)JH=$`=BU%KBw9%|w1X{1l3b9&@kK4wBjukH>yTBFuAxap4WdCg)dnMt+*xTYYX6yZ9NFyQAo&P-x}h4FB&AxSv91SdHwO&?;Rz{`Wpg4_rq)9)tzEf8|G3+-%|6gg1YH~4$7vcEyU2wZ)pKdc0sYhI*v15H7$1;8m2gC+oPB+Y7)Hy4hml(fwo@v0v`qN|(%$yvU9iECgFO?m|W+D4C`uenf)RLj;EZx_K#*X8#35cfkj9=~eVN6$jF`ER{;p=d`jgsqgo?jN%BsVRBRaFFvVTMbh8ygm13p4TYE0i>~=0QLvbkh@PA&hX6Vv&r77rEwn8C02L^UBr7A^$lH1LT4-3gP{MHArncQXK-j9LY^FN#n(rF%f-cb#(a;Rl#Y7UV&mAAHl<~T8jx+f0@#wV4yGRE1XisNaSm6>LzdhXdbv%CbhwK$xCXLL?(Se(AUW!V2e9j9FVUEk#C+OIV29+LJSd%I5A#`U&UL|hCbndat=I{gbLpZ$4fA|-cS3f_Y$OW>#0|d*<Rx$wDR1@`FhoQud3t*^5k)Y~AxL*v5Ka1%xxQdh?QW{vn1uMCLwvrmirf#}mMZ41M{tXqxftUOcs!6iR%@FG&fS4k44-=tvP+4+K;a42v`Z4sgFbl9aGN^++bG9cN0&IV+go(2BB2@!$fMz^dR*DQRzOy!Mg^<?T)B<`SGUmGDolaftuRIVt<ICP0U4pBQ%?-M`ArG<G{K6e!dv<gN0Ad4=i}*l2;1=Qvy@jBc0X^d9Fv75Fz|#WA6qVQv4au59Hrn}^0<FR$^Yoe<WumdP~vl{!af!xt7#RRiy|>hXya%"
]
""")

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    secret = _open_secret(ITEM_SECRETS[index - 1], "iterator-walk:snippet:" + str(index))
    if secret is None:
        print("  [error] Could not open snippet data -- contact your instructor.")
        sys.exit(1)
    return secret

def _find_compiler():
    for c in ("g++", "clang++"):
        if subprocess.run(["which", c], capture_output=True).returncode == 0:
            return c
    return None


def _compile_snippet(code, compiler, tmpdir, idx):
    src = os.path.join(tmpdir, f"s{idx:02}.cpp")
    exe = os.path.join(tmpdir, f"s{idx:02}")
    with open(src, "w") as f:
        f.write(code)
    r = subprocess.run([compiler, "-std=c++17", "-o", exe, src],
                       capture_output=True, text=True)
    if r.returncode != 0:
        return None, r.stderr
    r = subprocess.run([exe], capture_output=True, text=True, timeout=5)
    return r.stdout.strip(), None


class ActivityEngine:
    """Core check/passphrase logic for Iterator Walk; importable without a TTY."""

    slug = "iterator-walk"
    title = "Iterator Walk"
    description = "An ITERATOR is a movable cursor object -- a little value that points at one element of a container and knows how to move to the next one, without exposing the container's internal layout. Every snippet here traces the core iterator mental model by hand: begin()/end(), dereferencing with *it, advancing with ++it, the fence-post picture of end() as one-past-the-last-element, reverse iteration, map iterators' ->first/->second, string iterators, and the safe-erase idiom -- with every printed output fully deterministic."

    def __init__(self):
        self._actuals = None

    def items(self):
        """Return the public snippet dicts, in display order."""
        return SNIPPETS

    def compile_all(self, compiler, tmpdir):
        """Compile and run every snippet once; cache actual stdout per snippet id."""
        actuals = []
        for s in SNIPPETS:
            out, err = _compile_snippet(s["code"], compiler, tmpdir, s["id"])
            if out is None:
                return None, s["id"], err
            actuals.append(out)
        self._actuals = actuals
        return actuals, None, None

    def check(self, index, raw):
        """Check a 1-based snippet's predicted output against its actual compiled output."""
        secret = _secret_for_item(index)
        actual = self._actuals[index - 1]
        correct = raw == actual
        return {
            "correct": correct,
            "actual": actual,
            "feedback": None if correct else (secret.get("wrong") or {}).get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all predictions are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(s, index, total):
    print(f"\n{' ':=<70}")
    print(f"  Snippet {index:02}/{total:02}: {s['title']}")
    print(f"{' ':=<70}")
    if s.get("platform_note"):
        print(f"  Platform: {s['platform_note']}")
    print()
    for line in s["code"].splitlines():
        print(f"    {line}")
    print()
    print(f"  {s['prompt']}")
    result = None
    while True:
        n_lines = ENGINE._actuals[index - 1].count("\n") + 1
        if n_lines > 1:
            print(f"  (Output has {n_lines} lines -- enter each line, then press Enter.)")
            parts = []
            for ln_i in range(1, n_lines + 1):
                parts.append(input(f"  Line {ln_i}: ").rstrip("\r"))
            raw = "\n".join(parts)
        else:
            raw = input("  Your prediction: ").strip()
            if not raw:
                continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return result.get("canonical", raw)
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print(f"  Actual output: {result['actual']!r}   Type it exactly to continue.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    compiler = _find_compiler()
    if not compiler:
        print("error: no C++ compiler found (need g++ or clang++)", file=sys.stderr)
        sys.exit(1)

    print(f"\n  Compiling snippets with {compiler}...", end=" ", flush=True)
    with __import__("tempfile").TemporaryDirectory(prefix="iterator-walk-") as tmpdir:
        actuals, failed_id, err = ENGINE.compile_all(compiler, tmpdir)
        if actuals is None:
            print(f"FAILED\nerror: snippet {failed_id} compile error:\n{err}",
                  file=sys.stderr)
            sys.exit(1)
    print("done")

    answers = []
    items = ENGINE.items()
    for i, s in enumerate(items, 1):
        answers.append(_ask(s, i, len(items)))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
