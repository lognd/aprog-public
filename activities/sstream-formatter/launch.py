#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/sstream-formatter.json and re-run the generator.
"""Activity: String Stream Formatter


"""
import json, os, subprocess, sys, tempfile

# -- Crypto / obfuscation --
_KEY_A = "455e76dde7ad0012a76aa935ed689ba6db6b8557b769c4ee933884028e745d56"
_KEY_B = "ad7f7061415946d1f4899c7baf23bb759e02a7a44045291a6109cc52d6c2f790"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "e52c6037635c341ce1c4592c4b0ae6895b7c4dc8a20139327c8bd5f8dcd1e0fb2e79bcd1ebeedfa8190d7d802bebb34b8bd95e69d83dd0a693"

# -- Public snippets and encrypted answer/explanation material --
SNIPPETS = json.loads(r"""
[
  {
    "id": 1,
    "title": "ostringstream builds a string in memory",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <sstream>\nint main() {\n    std::ostringstream oss;\n    oss << \"score: \" << 42;\n    std::cout << oss.str() << \"\\n\";\n}",
    "prompt": "What does this print?"
  },
  {
    "id": 2,
    "title": "setw is one-shot -- it resets after each use",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <iomanip>\nint main() {\n    std::cout << \"X:\" << std::setw(4) << 1 << \" Y:\" << std::setw(4) << 2 << \"\\n\";\n    std::cout << \"X:\" << std::setw(4) << 3 << \" Y:\" << 4 << \"\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  },
  {
    "id": 3,
    "title": "setfill is sticky -- it persists across insertions",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <iomanip>\nint main() {\n    std::cout << std::setfill('0') << std::setw(4) << 7 << \"\\n\";\n    std::cout << std::setw(4) << 42 << \"\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  },
  {
    "id": 4,
    "title": "std::left shifts padding to the right side",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <iomanip>\nint main() {\n    std::cout << std::left;\n    std::cout << std::setw(8) << \"hi\" << \"|\\n\";\n    std::cout << std::setw(8) << \"world\" << \"|\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  },
  {
    "id": 5,
    "title": "fixed and setprecision are both sticky",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <iomanip>\nint main() {\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << 3.14159 << \"\\n\";\n    std::cout << 2.7 << \"\\n\";\n    std::cout << 100.0 << \"\\n\";\n}",
    "prompt": "What does this print? (three lines)"
  },
  {
    "id": 6,
    "title": "hex and showbase -- and the zero exception",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <iomanip>\nint main() {\n    std::cout << std::hex << std::showbase;\n    std::cout << 255 << \"\\n\";\n    std::cout << 16 << \"\\n\";\n    std::cout << 0 << \"\\n\";\n}",
    "prompt": "What does this print? (three lines)"
  },
  {
    "id": 7,
    "title": "combining ostringstream, setfill, setw, and string length",
    "platform_note": "any platform",
    "code": "#include <iostream>\n#include <iomanip>\n#include <sstream>\nint main() {\n    std::ostringstream oss;\n    oss << std::setfill('*') << std::setw(10) << std::right << \"hi\";\n    std::string s = oss.str();\n    std::cout << s << \"\\n\";\n    std::cout << s.length() << \"\\n\";\n}",
    "prompt": "What does this print? (two lines)"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "YOE4@&R1QwWf^u)<`)rO%x$YV{eG34%Oat2@hT@QU%>~Z#p$TmJqs>-B}dDRpeF1qoP9yd4Vl=>+$ZK*;$&(<Snk}>P4mV)y`;!&uYdGVZKv<%fHINJHzZXF03V1@ACpQM26eJYVEmG{&@Y8_vBa!=d`#hfiu0qNcBQ=5$ICDeL8BTy)e{261xCQPMSGF0*83Q|!W|y#fL?vGq?RAAhA`EoJ}_q7T@oM&)Js{jqmb`#-Wbhrfd#R)|3`)?nyR$9VlH5fcPc1^3-7;y`_wX#jVOzEMT&5!cE*RvK2NK|#f0SGYNFXho~ljwRUTkr;y5VG0@t>PLKYDggiL(UT+WdcW$y",
  "95|E5px+ixgzaRg8)?GdVA+zMpZb8fvKCH8P5KaUlZg*qUKc#MEAo~)-iAOc&Ppn5%#(E6*?CKlLzCaUK^VyCJd~y9+=e$=kKO9A6D1&XX~e5yroE&MUD0v|h&%jHY(NL#TT0b9Z~{elZEX0DyE+IbH_%;{2YsX{RXwT)+RsGG_(cA;9(%xt4Nl5n+d1wwT<EM1pb&9sgtAazk8{efH0MVt&#9S@(`6(zfe*OcorYvlc|04|w{ZqBj=|!`mlGp3mp3uh{86HjFrn2eI=ov?f0eYvFhK=ffgW6$=2CG{n@vq<j(PUvoi&no>5!1VG)v6KWfpvVA7XobS1S|0<}kh~W;mKdq@wG3Q!~)5K}Ye?-0f;TQ><y*)*o7EB#|1{I~+FLTLrI?_7#(j`tH$qEPeYdwI_QL7L<Z6S_YnoB!>i1B@?(T2#r%{^Fl&h@bm19{G4HKBA)zc&T0j=DrH7m)4J$%;|9-zikH&kRZd`mh<~<1Y=qT@bI<",
  "{PI&!r54$%x#(2<kt_I12!beeiCWN51r&G=kVFj}DZtn%3Khs}^&LV0alu;briVx*?B$7KZt)&6X6L}0o?MAUo*Ii-`_Bv!uT&iACNDU<SiU=5)}Fy2#(as~FbQ$`Wx?^<!fAYedjjBUPjW@Dy)k5z)R89%kBpiNBF{_yy|TE!fPyA348=AA*I)|4ZR(%$I1Pd#8!*ooiuYwM4+1<TWrHc|0vF{0T|yHK_UN1s+7?w)%Z9-%P>MYwcm%e60mlhZ3gDH<{e9u8=<>pVe&WBIZ(U)2o$kY*4s>^7ut#V(<s@~3XP^|(R~tY>U5#VU>!0DpP+$X>F~r3i3Ayv7DG|NN)2c?m23h57Fn0RCC|LgkrnS}qqKjAE9!EL>7Vin0fhml3MO5x7K01XZM|#I{-Lq&(`%hDaV27tV7Yo4(CV`y^{R?8Q9$_*;$R;#_ad<gjnz6Y",
  "es*$!Txy)PdNMM%o6#op9I0wQf<n0rkx^8@TMlUlTx@oGE{{hUE7tdl_ZP5uCSAA-{qo(n65`vx&zhIa-)NsjSry{KcmNmsD=Vb8mIU806y&LE|4x$e_ZR=cTW~-=R~Ai`zjNZw`vF`eA=Ka_sNLiZz4@^5{Wy(+EToCd0~kLe!cVnjbR7?L@}87<IlgdI+$B?mq5smSrXdqn?z1FE&ic>;rDki|Q#?%liO00ZWQmum8}L;0A(6Fu*2Z<pC>=QP8x9(4H%x)zY+JGT`u1OnYP-dmEFaZ^<T^z^gBI{YYz)k;Fbim6vhELqv1^Cizaj~x6q$J(ewE>OIt>on@So9aVEkoEww={ZUIVj4i*U)mXyTj$IMv#J81?spnb@>&P$iKaIagaRCBcl=Gya$U^uz!=faC*eotCbV4&8`@D)zn}XtW%CUo_+r?FhP^",
  "eV&I_6=P0!2}9a@0#Sibsz->!IaBXX&<SU|-(R5@*^NWfFr@G<8&#Vd?!b13*Z&rOP(J=ak7(cRQKGS28L)Oeu-w&K$D|Pm4&YgecLcH@p%?Iytx~HVBS@r;n9{Y$&jJtSxZQ%`wlrY(Zn*Vl!@Qup)0PyIRNFpT?xwZqnVsB7vEvo14J#ujfEU2(?PSF>I+K3?ymftnk5lKl&EV14Mh_Pj#TPQ%dMVj9nBD}lZG$M#jGiZi_P+A5K{c~=SyGl(2d0v2s1UY~k}zlK^Fx_c6PL=f8?(lv>aMlAii8OHZtRZg%s-rj{a_5$$wq{jSSTyn3lV<fSy6y+IYt$v7V?v5@d8Mr)|73HS|!dXmgwS5)$$!__bTwV(2YJtU>xqWF}Z2+rK3HOAVJ1P(FXktPu<lYfl_{=S_Ij_#$zrK(r^",
  "HpT@sn5IoSTylub*3ndte0)24+Zi=D%_piN{liWC=ZyRQL=;m9ZD)*$VX(@n<saHGt}akY3jQ1}X*(!9;3%v74ucuRQ?tVYz^ejcw#ZBtz91vt_WmIx-82J)HO?YkKsFc9g_g2Uc;ve_J!*|TZ@$46kdI@+Wa4mhg+`WUG=lq<z=^fACN{&*GUbAd91qdxmf3a^Hmkxs@mzy9ussXP{`MGmrchfK8s#CbANm7!*TX%piT+ra7+z>Jk?R0sW2Yi+d})z@ry1dw8@PX5?2PaQl<fMr#C@}<?AVJBZA=OOK&t2Ax3t-LfV3HkVwXI*O=B3e@h-GjSxMpomwBq`mAU(Bf2L8y5xk2{51a^8#QuM5USgyK?uDhlZQYwHZgJ<eyRBWcCR-^#a~!w{k^a@T6?Wy%$HBwb0@Ve-OZgQLC?&BHmIbz1F#gAH!*GT",
  "slpab;Fj6c6j4J9|03b6{NP#$u1+guO*4NaR#gAw)5Oj_+}GM+gP?n@Z#EWrd~?)hG2`>%$>za|6U$cW!MA?CcI<=(IKu?c(>i{@OB)=Ts)m|3OEXBoG}<0DwsSrRpa;}Et6<#K#@g9-ZEPRVxwYZd#zTG-07(xUKHFtJy`v)Iq`a-&MC;XGCGHPUNh&(fZzoOEa1u0V8J*d1+1OnB$F8c{Dh1+M*Yizepbcahtdi2-Jak8x_vkhN+ni0SOhww~@cD-Dtn^oSPTM!rJ$W{^oR8k6YnM@?3vrlkFT&rmiEzvJJ=1nWwS0(V8_P3vX8y)>XC6I4QATZurdt!zbk_7_JOpM<XD=*SA#ok5kZ@LBb%O<Xq-yShUx`vx+HQ*l`6tf|3FK0GGDQu?q9A96JIsLq4fO28(>6wVI^XbjxqtgxEzb4Jk795Rtx$FHeGtaD6>!|OO58o~{B7`nM{Tsd=sBt{5mMNjrA(b9vkn&"
]
""")

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    secret = _open_secret(ITEM_SECRETS[index - 1], "sstream-formatter:snippet:" + str(index))
    if secret is None:
        print("  [error] Could not open snippet data -- contact your instructor.")
        sys.exit(1)
    return secret

def _find_compiler():
    for c in ("g++", "clang++"):
        if subprocess.run(["which", c], capture_output=True).returncode == 0:
            return c
    return None


def _compile_snippet(code, compiler, tmpdir, idx):
    src = os.path.join(tmpdir, f"s{idx:02}.cpp")
    exe = os.path.join(tmpdir, f"s{idx:02}")
    with open(src, "w") as f:
        f.write(code)
    r = subprocess.run([compiler, "-std=c++17", "-o", exe, src],
                       capture_output=True, text=True)
    if r.returncode != 0:
        return None, r.stderr
    r = subprocess.run([exe], capture_output=True, text=True, timeout=5)
    return r.stdout.strip(), None


class ActivityEngine:
    """Core check/passphrase logic for Activity: String Stream Formatter; importable without a TTY."""

    slug = "sstream-formatter"
    title = "Activity: String Stream Formatter"
    description = ''

    def __init__(self):
        self._actuals = None

    def items(self):
        """Return the public snippet dicts, in display order."""
        return SNIPPETS

    def compile_all(self, compiler, tmpdir):
        """Compile and run every snippet once; cache actual stdout per snippet id."""
        actuals = []
        for s in SNIPPETS:
            out, err = _compile_snippet(s["code"], compiler, tmpdir, s["id"])
            if out is None:
                return None, s["id"], err
            actuals.append(out)
        self._actuals = actuals
        return actuals, None, None

    def check(self, index, raw):
        """Check a 1-based snippet's predicted output against its actual compiled output."""
        secret = _secret_for_item(index)
        actual = self._actuals[index - 1]
        correct = raw == actual
        return {
            "correct": correct,
            "actual": actual,
            "feedback": None if correct else (secret.get("wrong") or {}).get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all predictions are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(s, index, total):
    print(f"\n{' ':=<70}")
    print(f"  Snippet {index:02}/{total:02}: {s['title']}")
    print(f"{' ':=<70}")
    if s.get("platform_note"):
        print(f"  Platform: {s['platform_note']}")
    print()
    for line in s["code"].splitlines():
        print(f"    {line}")
    print()
    print(f"  {s['prompt']}")
    result = None
    while True:
        n_lines = ENGINE._actuals[index - 1].count("\n") + 1
        if n_lines > 1:
            print(f"  (Output has {n_lines} lines -- enter each line, then press Enter.)")
            parts = []
            for ln_i in range(1, n_lines + 1):
                parts.append(input(f"  Line {ln_i}: ").rstrip("\r"))
            raw = "\n".join(parts)
        else:
            raw = input("  Your prediction: ").strip()
            if not raw:
                continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return result.get("canonical", raw)
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print(f"  Actual output: {result['actual']!r}   Type it exactly to continue.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    compiler = _find_compiler()
    if not compiler:
        print("error: no C++ compiler found (need g++ or clang++)", file=sys.stderr)
        sys.exit(1)

    print(f"\n  Compiling snippets with {compiler}...", end=" ", flush=True)
    with __import__("tempfile").TemporaryDirectory(prefix="sstream-formatter-") as tmpdir:
        actuals, failed_id, err = ENGINE.compile_all(compiler, tmpdir)
        if actuals is None:
            print(f"FAILED\nerror: snippet {failed_id} compile error:\n{err}",
                  file=sys.stderr)
            sys.exit(1)
    print("done")

    answers = []
    items = ENGINE.items()
    for i, s in enumerate(items, 1):
        answers.append(_ask(s, i, len(items)))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
