#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/link-tracer.json and re-run the generator.
"""Link Tracer

Minimal singly linked list programs built from a raw Node struct (int data; Node* next) manipulated directly with pointers -- no smart pointers, no std::list. Predict the exact output of each classic manipulation (push_front, insert-after, delete-middle with relink, reverse, find-middle, and a broken relink that loses a node) to build a precise mental model of pointer chasing.
"""
import json, os, subprocess, sys, tempfile

# -- Crypto / obfuscation --
_KEY_A = "27751f9e7c89f09c7c858589c450d2c91259b51414dcc3db44f7f85f821c7ef0"
_KEY_B = "e6b0e91592eecbb5affc223132fd0e539e93c9d9f8d6f46932c5e44a40ecf9c1"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "e60ba1839b897af5acd8487685293c937dfccd0a4cdc7c9f9655e9e0b01265100810349262f921fc7f8a5fa6e651464d5f53e78541192a"

# -- Public snippets and encrypted answer/explanation material --
SNIPPETS = json.loads(r"""
[
  {
    "id": 1,
    "title": "push_front builds a list from the front",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n\nstruct Node {\n    int data;\n    Node* next;\n};\n\nvoid push_front(Node*& head, int value) {\n    Node* n = new Node{value, head};\n    head = n;\n}\n\nvoid print_list(Node* head) {\n    for (Node* cur = head; cur != nullptr; cur = cur->next) {\n        printf(\"%d \", cur->data);\n    }\n    printf(\"\\n\");\n}\n\nvoid free_list(Node* head) {\n    Node* cur = head;\n    while (cur != nullptr) {\n        Node* next = cur->next;\n        delete cur;\n        cur = next;\n    }\n}\n\nint main() {\n    Node* head = nullptr;\n    push_front(head, 30);\n    push_front(head, 20);\n    push_front(head, 10);\n    print_list(head);\n    free_list(head);\n}\n",
    "prompt": "push_front is called with 30, then 20, then 10, on an initially empty list. What does print_list(head) print?"
  },
  {
    "id": 2,
    "title": "insert_after links a new node into the middle",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n\nstruct Node {\n    int data;\n    Node* next;\n};\n\nNode* make_node(int value) {\n    return new Node{value, nullptr};\n}\n\nvoid insert_after(Node* prior, int value) {\n    Node* n = new Node{value, prior->next};\n    prior->next = n;\n}\n\nvoid print_list(Node* head) {\n    for (Node* cur = head; cur != nullptr; cur = cur->next) {\n        printf(\"%d \", cur->data);\n    }\n    printf(\"\\n\");\n}\n\nvoid free_list(Node* head) {\n    Node* cur = head;\n    while (cur != nullptr) {\n        Node* next = cur->next;\n        delete cur;\n        cur = next;\n    }\n}\n\nint main() {\n    Node* head = make_node(10);\n    head->next = make_node(20);\n    head->next->next = make_node(40);\n\n    insert_after(head->next, 30);\n\n    print_list(head);\n    free_list(head);\n}\n",
    "prompt": "The list is built as 10 -> 20 -> 40, then insert_after(head->next, 30) is called. What does print_list(head) print?"
  },
  {
    "id": 3,
    "title": "delete_middle relinks around a removed node",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n\nstruct Node {\n    int data;\n    Node* next;\n};\n\nNode* make_node(int value) {\n    return new Node{value, nullptr};\n}\n\nvoid delete_middle(Node* head, int target) {\n    Node* prev = head;\n    Node* cur = head->next;\n    while (cur != nullptr) {\n        if (cur->data == target) {\n            prev->next = cur->next;\n            delete cur;\n            return;\n        }\n        prev = cur;\n        cur = cur->next;\n    }\n}\n\nvoid print_list(Node* head) {\n    for (Node* cur = head; cur != nullptr; cur = cur->next) {\n        printf(\"%d \", cur->data);\n    }\n    printf(\"\\n\");\n}\n\nvoid free_list(Node* head) {\n    Node* cur = head;\n    while (cur != nullptr) {\n        Node* next = cur->next;\n        delete cur;\n        cur = next;\n    }\n}\n\nint main() {\n    Node* head = make_node(10);\n    head->next = make_node(20);\n    head->next->next = make_node(30);\n    head->next->next->next = make_node(40);\n\n    delete_middle(head, 20);\n    delete_middle(head, 30);\n\n    print_list(head);\n    free_list(head);\n}\n",
    "prompt": "The list is built as 10 -> 20 -> 30 -> 40. delete_middle(head, 20) is called, then delete_middle(head, 30). What does print_list(head) print?"
  },
  {
    "id": 4,
    "title": "reverse rebuilds the chain with three pointers",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n\nstruct Node {\n    int data;\n    Node* next;\n};\n\nNode* make_node(int value) {\n    return new Node{value, nullptr};\n}\n\nNode* reverse(Node* head) {\n    Node* prev = nullptr;\n    Node* cur = head;\n    while (cur != nullptr) {\n        Node* next = cur->next;\n        cur->next = prev;\n        prev = cur;\n        cur = next;\n    }\n    return prev;\n}\n\nvoid print_list(Node* head) {\n    for (Node* cur = head; cur != nullptr; cur = cur->next) {\n        printf(\"%d \", cur->data);\n    }\n    printf(\"\\n\");\n}\n\nvoid free_list(Node* head) {\n    Node* cur = head;\n    while (cur != nullptr) {\n        Node* next = cur->next;\n        delete cur;\n        cur = next;\n    }\n}\n\nint main() {\n    Node* head = make_node(1);\n    head->next = make_node(2);\n    head->next->next = make_node(3);\n    head->next->next->next = make_node(4);\n\n    print_list(head);\n    head = reverse(head);\n    print_list(head);\n\n    free_list(head);\n}\n",
    "prompt": "The list is built as 1 -> 2 -> 3 -> 4. It is printed, reversed with reverse(head), and printed again (using the returned new head). What are the two printed lines?"
  },
  {
    "id": 5,
    "title": "find_middle with a slow and fast pointer",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n\nstruct Node {\n    int data;\n    Node* next;\n};\n\nNode* make_node(int value) {\n    return new Node{value, nullptr};\n}\n\nNode* find_middle(Node* head) {\n    Node* slow = head;\n    Node* fast = head;\n    while (fast != nullptr && fast->next != nullptr) {\n        slow = slow->next;\n        fast = fast->next->next;\n    }\n    return slow;\n}\n\nvoid free_list(Node* head) {\n    Node* cur = head;\n    while (cur != nullptr) {\n        Node* next = cur->next;\n        delete cur;\n        cur = next;\n    }\n}\n\nint main() {\n    Node* head = make_node(10);\n    head->next = make_node(20);\n    head->next->next = make_node(30);\n    head->next->next->next = make_node(40);\n    head->next->next->next->next = make_node(50);\n\n    Node* mid = find_middle(head);\n    printf(\"middle: %d\\n\", mid->data);\n\n    free_list(head);\n}\n",
    "prompt": "The list holds 10 -> 20 -> 30 -> 40 -> 50 (5 nodes). find_middle uses a slow pointer (advances one node per step) and a fast pointer (advances two nodes per step) until fast falls off the end. What does the program print?"
  },
  {
    "id": 6,
    "title": "a broken relink order loses a node",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <cstdio>\n\nstruct Node {\n    int data;\n    Node* next;\n};\n\nNode* make_node(int value) {\n    return new Node{value, nullptr};\n}\n\nint main() {\n    Node* head = make_node(10);\n    head->next = make_node(20);\n    head->next->next = make_node(30);\n\n    // Insert 99 between 10 and 20 -- but relink in the WRONG order.\n    Node* n = make_node(99);\n    head->next = n;         // BUG: overwrites head->next (10 -> 20) before\n                             // saving it -- the old link to 20 is gone.\n    n->next = head->next;   // reads the pointer we just overwrote (which is\n                             // now n itself), so n->next ends up pointing at n.\n\n    // Print only what is still reachable from head, capped at 5 hops, so a\n    // cycle in the broken list cannot loop forever.\n    Node* cur = head;\n    for (int i = 0; i < 5 && cur != nullptr; ++i) {\n        printf(\"%d \", cur->data);\n        cur = cur->next;\n    }\n    printf(\"\\n\");\n}\n",
    "prompt": "This program tries to insert 99 between 10 and 20 in the list 10 -> 20 -> 30, but relinks the pointers in the wrong order. It prints up to 5 reachable node values (to avoid looping forever if a cycle formed). What does it print, and what happened to the nodes holding 20 and 30?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "A9ialPsJjk4RCm5fh5Em3CP$s4(5Ja$t#-817S#y5LCz{q4Uci0pg45u6(2w=OCa^5)J&1+^<L0_YpXTMU!SBEOBRks!xhx*C-#D9mGwCY&IC-N_)C6ju0|q{74wxdRvw}9X`5!SH2A~)hvd#FDaNuqhAc5pyocqMORlJ2`iL<a%y<L&{kak9QT`6`As=YmXe+)ov82+=0Z>yF_?1^97%m@zTZS`{ODESY_QljCNUVoK+Z$G-*g3!q8L)rv^@R{-4C@|QX771LiweKc^Q@H%|DqJANX4e0WL0rRU0RF<q<`y!2K1oLsv$iSmHqe=D{dX8zXa>Nq+?f1`6nIOvFA^DJtD!j^ZB|zOP*vdq-so+TXtr+Ibgeaz>Y1$!;}TuHml_M%Et>C2Aa8<+Fg1oEI&@ZR+%DgfMV9<wS)-9Nkl|Ovc?gJj+d`Ftg-^oN&Hb*fVN#A#(vw?5<-w#Ktp)IR02#TxJ>CaMu3$-TIfF=?gsH-=g?nTpLxkKFvtH-2ywk!A#l|&Wjbz{FFD$j8L`gOp%;-kJ7~EfRuSiHWHR_(?;7<YV$%(1g^a?TeHb;)tEINYnj9lINvsM&ofZemL+kyOY;Zzbmcf9`*>a!zL$tmB|eMI)00IML1BLc(xYu*Dhy(i#)^=pR&6Fp->2hC(^fnhth;b4#ESFv=3a(t?&OIz&AqY|7Suk78%Isw2)AR)>-OZi4tS>4#+7{V$uRecsfyG_YxtQ0H>la$u8oF>)0p}oGx^@Q=@#1nTM}zzkHnxKnY?kDqiT6|rxfU-6aXuzrz@zu=GksnQ%Qy9X=q8Yc2yB~vzLE{9UbrO?vc(-bm`-~s2BI4",
  "FQO6FU__IF7CXTsH&Q$`5^Lzem<O;G;)a;MBCqg}8$-49=>n*N$*y?KDolF&{DM_7_0^CMxH3n!<G?%D$zz~(aN6HNG#7P^bb?d)M9kO8e##4Ol@tb)q2x?bpT(=SvLBeOEo%o-(2crniv~amhSt+_%C%wDqx2}JlxU?QoKf?|&I{mFoE>z^y{2*n*O?2VLw}Eqzw%+En0Xn@`(N*2k}p6nT6<f1J8Wl6C~N2fohgY(ye9ggDTK`MqAV^b;;4gwlh2edpH96HOQyt@y+M8~8dTHnW)vq(V)yUfoMmzNTvTgl+uJo}t67Q`D4>}~$rbk!4RrALRjyi80a%^-QF`8@x~JylaC0~O&vD^?0s2v6h9P)|n4FJFpzeh`%OoJt^-Ego#e!yEQ~X!VrS?Zm_&-yAox%Hb+osdz+%DJCI7o;QbCkmn7C*HYNvUxuGG%>&BZtRu`Axn=A#j1@WdBr?G_5rePqoNvIMun9$S^1yt&On7z=(CM74NgG3?24%$TUh22g%M%s({JRCczU`1*w%4PG(J#SNwPs?wGTDp<`N=",
  "HhnahqW>M}Ta03ymXx-<3dyMAO00v)edDb>ome82qPC$@$XT-42ts5|H*h?5WviET`#T*32ApiVQC#~3Csq_=!(vss%3U6K<PdFFLbAqM<;nr-Juij>^(bxEn_MzIE>yF(mra-N^&x?o%Vp%cBxwW-6h75w0Hf?m2shQ<Ts6yXCpDjG43!r1X`Y(91_t;zt$wHbzf;;s#~2z#vc3T)rWw*Rp0pKmitdE}t@?wIeeMm>MT#jUH90n(8oyU!1?6fGY;EkMj<P>@-!-rl`gEb*nz?mV*D2{)rU0liF7c}K@TQW;ps`rha?8^7a$!YxRT00hVZZ<NP*tLLElBc=Yroe0nkBdU8QjL?Xof+k!Tqbm5Pudi^<8w^Iln1<BZ8U6ZA>Ied=g21@JvSFeN_B%ASOp<BW5Ve*qprFQm?>f$gFOjZ%oaZGr{`<?co?b7%m>mkjHiksU>Bx;e!fG?;NzxVM?qLs%14zaGkC{UWO$H6~l*!Ns0cyxh_VT0{aZuQG{T_Bev1FieeKi(=S-+A5Z}mwQ!1IzL#FaLi$C1os}X8EmiZoEUjEXgHWmxhBEsVUj$OEqh4ugC(@j1Yg82s)3T{$%>#t5M$5Z?5ds2@?N~2C57;-KdO{;B*j(`WyP@_ue-=T}pX7iwDn9<!ZS8F-8|MIRM;Z7Lt>xb2JK8OQ_RuJFZ<huiCNyMmqZiutMwVSE;SKP+hr?~MI;t~UOq4Kw>=2SmFA@nr>1)HQOKe#",
  "XU><NB5S2yo;uZ7g=Sj#-VMQbGqp#%XR!xbp5HE+7+*>P)a+FzJUO~h$D%jwUi20!y#zVj&TXsYoIx+x&ym=)7B=`ibfO)$k2X<ccDw+6;T^G0`nZ%11HC0}IK0#$hzF#KJ#*}4Gf=D^ajac4@Ii>*siFf7V{}~Vl)w(_$pZDI7rIyWwzz3fE-XEb+#{LNb)1*~5ky0KlzV~!!(HalrI=wi58bN)%>;C@&s!zuESoQNHHn53MVrgZ55A6~w>&}3D3aouu(Cqip$<>RFOvLwZ@KGqd=wAi-`7vbvYsABdc%h6qP?sdv!YuygEpH1+%@1U-_tk`*-MXv0BT*z$D`eT$>TIH<RF2=GRht|YC8&{JsZZ)KgMg6MzcjCcVKWPF6XotRg{2|I%}VC_#DN-59nKbqLi<@hUSWoxf4n^a5R0j@L{5v+33KwB%_1OcX&i<J0~VZkW?J-(ETT;9nU?I>KSBoNyG6n<|5{`S6C&8E)~!Ps}ns<VB9&?{2fuE()@a6ql%G!R)z7<di$u^Co$uDg|D8k&V_nsei?mjdR^+sqrQLe3Y|d7-fY~Bwthd4NN%CI?^u3BZyay;pMT|Cq|IQy-3tly(4IpUwd|B?IV$YSOKE@3qP&Z511;+;rMteUP<`lHRd=BNQRFK)*!{TX7zR>c%!9S4{%Gi3URUdkel{M{8Kd58u55}&yncbQ<BaFne*}~||8_x3#d@{e|8^*GEO{7fuL{@j$w~{maH;akQHe(Y?NcFC_y2&L*XcCbhMVzun-}!Vww0sJ1}t$cb=CL=_XdrSUu8(4HpVHnn&XM-VTziybgA0+KK5w}PTKt",
  "CQ#2>a|)g?RprsBNK$x_(Vu*u>-|ohc`Vtr>0GCN5be=+7E-^5%H{X`KH;D@KZah^@EURQ@PW=fTo5Gd>g>=8uYXqh0eCC}VS?xF&2mb}j3U9qV*^=5JhW06ONNre?DH^mj7{%{uHhz+WboZtZv@aIbvE>?X{0mxe9n}bXo5*V<5-F7ktE8z0ti7vwq#QxjRT97<BQ$&4^e`8@!+tmD!GmA3a688a9R{JFc>Z)RuPivu`rNRwO;70hJ_5$J{g9GQ%Zd*Qg0d{C-a*r^KMe5cb=t}`(_0Y*}%JB&LR4LM{^i4o+U!I)i~Jv|7}k7r#&mayssJ|TIx?=zqe`K3h#FmM@!k_<}Tz~WjrszgS)HYF8%u+0YmoGy(VTM0Zut1pu}^GFa-I$eLcK3sBHsrPeCcTLvBX5eHMkp^ayji8Q(sB0p=k$r+R~VqYg4F0&Su0-dq&5wN(<8pyemm^V0C8VERol-uu=mmNIYSr0@$)i&JeNaT_$dl5SX9YwfwdXLq-MZvPD-q_qsKH-0n?p*Z6Ll%ri&cvvnXstJ+!QZ2`OZuAR8oACPrnrrKXQk}GZ&3Zx*U7ER46z)toJ7PfnI#l^!@7sjuK)7MF{i5CS{KKbADEHo4mdrEP-FHkKhoAVQI#O)XNw6y>ybj$+?mcsaq2W2e=<LLIjr=f-ZJ|M-bL9gb^%smk<$TU~tmuFIMT}ud@r1_Z0-xo0v4XwfSgVovEu6i;L@xu<sK_aT>;PoZp;QP!QcEZZ*MqWms<<(;!RQ78H?li)7Tijr-Q?C!rTe50fq&=wBclRR0oo}Luc-^i8N{EP(buO;>GrFSJr9TVw)uc9tu?y",
  "Ha*~EL6!Ny+fW}~A@%R}_5_k$gB3J*c!u<<r}JS=NQ9<bpU;&8oYGKHDus9K3V|$a%8Vq7jm_nRt6~Lkpt^NrAZqCH83Jw8De#kvLNCC6H0__RkHed1MtXCa>KsBqi0{_+lHRUo^swQh{Tfm!HNse7DFch<OH}V>0NZBb19uC?tM1wG@dtp{qJXa~S)>Gk%!++!jCPRAtV?`ngf8c0C6(#n))5ItTl0wiRb{$3I?!ZL;-2%?b}3AxcZexs*>0sR2{PCcLymt3pbxi`8>S0xjmKU4<+T>qnS>y~bo3&CsP#utXgXJ#>ABu%T3sM-mGh{43z?NvMvp#sogctMtH~nrg0ZGv4RV62r&$2=h8WJzG$~9bDFg`EhkydWG>bz|!G^x1Z81N0SZr)AKPUu7$W<$j(JHpw==Eygk!ssra1_V$+rBC7FE%JSJkv61iXL)g=ZA5>23ygrXMfsL6zIX}M`-W>NQQkNK~i1e#ODoA^d1!(ra=ALDts5OULI;5k=Cg;h(5>FPb1tt?2y4G3eb+aiy#M4v`ig0Ib14Ap;P!j97BJE=(t}G&BaNMyN$0Juks{%M@ttWh<yedNCx<SUN}S`18yu6PL)TyCum;ew{S9CPt1n$QfItOem+Dmv?V}`AHcDhi|}=4Npv>$@qY)K06z>JkN1tp&a^=~YY)J!{*6+cXFh&fOx~DiE4y-EnBxPO^9tgJ^p;W&dP`xwFyt(jq1_o5=(&lvQ#00iAkLUe8?&a%dOeKaX10I!gXIO9?JL3T)S(P#Hbw@pdi@9FHPL@?d>p34z~8H5?R$8dO_4W=S6A8#%mSOwM-#V;E5AB4EDB>fuucV-7O(5V9y+B8T!G7&L&q&m!h2To=%0``uavI-kI);VJa!;0<!!MrOx90`#mzk~_{Qb&Az;ryw#4lrN{o7ZH9I7)QFE;v%316S8z$hj#Agv`72X|i#$=xH+fCudTf{)xw5s!8d5$V^=W35Rtd;T--=4J?0*^&M+58pd&y@SJr&F`q!~p^sATaTkO<gW5lpb*ING~*rDX%DZ_LbWm<srs=@SD{tX&i<"
]
""")

# -- Helpers --
import textwrap as _tw

_LINE_WIDTH = 70

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    secret = _open_secret(ITEM_SECRETS[index - 1], "link-tracer:snippet:" + str(index))
    if secret is None:
        print("  [error] Could not open snippet data -- contact your instructor.")
        sys.exit(1)
    return secret

def _find_compiler():
    for c in ("g++", "clang++"):
        if subprocess.run(["which", c], capture_output=True).returncode == 0:
            return c
    return None


def _compile_snippet(code, compiler, tmpdir, idx):
    src = os.path.join(tmpdir, f"s{idx:02}.cpp")
    exe = os.path.join(tmpdir, f"s{idx:02}")
    with open(src, "w") as f:
        f.write(code)
    r = subprocess.run([compiler, "-std=c++17", "-o", exe, src],
                       capture_output=True, text=True)
    if r.returncode != 0:
        return None, r.stderr
    r = subprocess.run([exe], capture_output=True, text=True, timeout=5)
    return r.stdout.strip(), None


class ActivityEngine:
    """Core check/passphrase logic for Link Tracer; importable without a TTY."""

    slug = "link-tracer"
    title = "Link Tracer"
    description = 'Minimal singly linked list programs built from a raw Node struct (int data; Node* next) manipulated directly with pointers -- no smart pointers, no std::list. Predict the exact output of each classic manipulation (push_front, insert-after, delete-middle with relink, reverse, find-middle, and a broken relink that loses a node) to build a precise mental model of pointer chasing.'

    def __init__(self):
        self._actuals = None

    def items(self):
        """Return the public snippet dicts, in display order."""
        return SNIPPETS

    def compile_all(self, compiler, tmpdir):
        """Compile and run every snippet once; cache actual stdout per snippet id."""
        actuals = []
        for s in SNIPPETS:
            out, err = _compile_snippet(s["code"], compiler, tmpdir, s["id"])
            if out is None:
                return None, s["id"], err
            actuals.append(out)
        self._actuals = actuals
        return actuals, None, None

    def check(self, index, raw):
        """Check a 1-based snippet's predicted output against its actual compiled output."""
        secret = _secret_for_item(index)
        actual = self._actuals[index - 1]
        correct = raw == actual
        return {
            "correct": correct,
            "actual": actual,
            "feedback": None if correct else (secret.get("wrong") or {}).get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all predictions are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(s, index, total):
    print(f"\n{' ':=<70}")
    print(f"  Snippet {index:02}/{total:02}: {s['title']}")
    print(f"{' ':=<70}")
    if s.get("platform_note"):
        print(f"  Platform: {s['platform_note']}")
    print()
    for line in s["code"].splitlines():
        print(f"    {line}")
    print()
    print(f"  {s['prompt']}")
    result = None
    while True:
        n_lines = ENGINE._actuals[index - 1].count("\n") + 1
        if n_lines > 1:
            print(f"  (Output has {n_lines} lines -- enter each line, then press Enter.)")
            parts = []
            for ln_i in range(1, n_lines + 1):
                parts.append(input(f"  Line {ln_i}: ").rstrip("\r"))
            raw = "\n".join(parts)
        else:
            raw = input("  Your prediction: ").strip()
            if not raw:
                continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return raw
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print(f"  Actual output: {result['actual']!r}   Type it exactly to continue.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    compiler = _find_compiler()
    if not compiler:
        print("error: no C++ compiler found (need g++ or clang++)", file=sys.stderr)
        sys.exit(1)

    print(f"\n  Compiling snippets with {compiler}...", end=" ", flush=True)
    with __import__("tempfile").TemporaryDirectory(prefix="link-tracer-") as tmpdir:
        actuals, failed_id, err = ENGINE.compile_all(compiler, tmpdir)
        if actuals is None:
            print(f"FAILED\nerror: snippet {failed_id} compile error:\n{err}",
                  file=sys.stderr)
            sys.exit(1)
    print("done")

    answers = []
    items = ENGINE.items()
    for i, s in enumerate(items, 1):
        answers.append(_ask(s, i, len(items)))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
