#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/hiding-hunt.json and re-run the generator.
"""Hiding Hunt

Compilable C++ snippets that distinguish overriding a virtual function from accidentally hiding it. Predict the exact output to build intuition for static type vs. dynamic type.
"""
import json, os, subprocess, sys, tempfile

# -- Crypto / obfuscation --
_KEY_A = "2c5b307e3c69e39508d2a03fda2f6f8fc9ce5dfeb2936bfefa41bf6368f348a5"
_KEY_B = "83aa0d9a431f41cd8cd0ff991f3c035a50e1faaa46bb862bfbfb5778e0a687d5"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "a04a6a774f9c3446bfb3241862cf6e11eace0f2376bbe16df513e38c5f7e6edc7eac8713cbb4325fb80be6f4d250b5ae7bcbb17300c36a56"

# -- Public snippets and encrypted answer/explanation material --
SNIPPETS = json.loads(r"""
[
  {
    "id": 1,
    "title": "A virtual function called through a base reference dispatches to the override",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <iostream>\nstruct Base {\n    virtual void speak() const { std::cout << \"Base speak\\n\"; }\n};\nstruct Derived : Base {\n    void speak() const override { std::cout << \"Derived speak\\n\"; }\n};\nint main() {\n    Derived d;\n    Base& ref = d;\n    ref.speak();\n}\n",
    "prompt": "What does this program print?"
  },
  {
    "id": 2,
    "title": "A non-virtual function is resolved by static type: hiding, not overriding",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <iostream>\nstruct Base {\n    void speak() const { std::cout << \"Base speak\\n\"; }\n};\nstruct Derived : Base {\n    void speak() const { std::cout << \"Derived speak\\n\"; }\n};\nint main() {\n    Derived d;\n    Base& ref = d;\n    ref.speak();\n}\n",
    "prompt": "What does this program print?"
  },
  {
    "id": 3,
    "title": "A signature mismatch (missing const) silently hides instead of overriding",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <iostream>\nstruct Base {\n    virtual void speak() const { std::cout << \"Base speak\\n\"; }\n};\nstruct Derived : Base {\n    virtual void speak() { std::cout << \"Derived speak\\n\"; }\n};\nint main() {\n    Derived d;\n    Base& ref = d;\n    ref.speak();\n}\n",
    "prompt": "What does this program print? (Note: Derived::speak is missing 'const' compared to Base::speak.)"
  },
  {
    "id": 4,
    "title": "A base pointer to a derived object still dispatches virtually",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <iostream>\nstruct Base {\n    virtual void speak() const { std::cout << \"Base speak\\n\"; }\n};\nstruct Derived : Base {\n    void speak() const override { std::cout << \"Derived speak\\n\"; }\n};\nvoid announce(Base* b) {\n    b->speak();\n}\nint main() {\n    Derived d;\n    announce(&d);\n}\n",
    "prompt": "What does this program print?"
  },
  {
    "id": 5,
    "title": "Calling a virtual function inside a base class constructor uses the base version",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <iostream>\nstruct Base {\n    Base() { std::cout << \"Base ctor calling speak: \"; speak(); }\n    virtual void speak() const { std::cout << \"Base speak\\n\"; }\n};\nstruct Derived : Base {\n    void speak() const override { std::cout << \"Derived speak\\n\"; }\n};\nint main() {\n    Derived d;\n}\n",
    "prompt": "What does this program print? (Only a Derived object 'd' is created.)"
  },
  {
    "id": 6,
    "title": "Protected members are accessible from a derived class's own methods",
    "platform_note": "any platform, g++ -std=c++17",
    "code": "#include <iostream>\nstruct Base {\nprotected:\n    int secret_ = 42;\npublic:\n    virtual void reveal() const { std::cout << \"Base secret is \" << secret_ << \"\\n\"; }\n};\nstruct Derived : Base {\n    void reveal() const override { std::cout << \"Derived secret is \" << secret_ << \"\\n\"; }\n};\nint main() {\n    Derived d;\n    Base& ref = d;\n    ref.reveal();\n}\n",
    "prompt": "What does this program print?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "aI?qp<|KqyFN=$+CD{U>8Wt$x!@&?oa0T1qz=^lQ@5s;#czNw$H{BeZ_b^4IpZjs0GD+!0&_x!8`6&Wta)h}Xz24@hP%9nG)ElGM+bMJlLgtb9u^uNyQW;OJlC+N=(ueimu}TYpQN!Ipr&qZjqsy@;;$XOw#4C@=ctZNv9kV`4oDk}xL~scYfp0ZlsI!JKy2cg1EEyY3f#f2>dGvIKyBzkwg}+wM9e1af3A?{tDf!r*o&`tn@lx~`&FL`_KFG1S)r-a>ki*VX-o1r6FbZ??5C_%@l`J{`Ab%9g3m0P(e~ZK=AuVpoSmfvrN-lrLQ=<(jmvk~C1+B<rDttP^pAJV==uo0V`gDw{e)ABr_+FtPFF2-03liy1LmNfnzj6_`O@WZ(&}ItTt}***utKYcKUUU@S`Ul~jbK{Uug)OpX7PkvUGn0uS1okfonmh04BU4>q4;Iub3YonKO!BRIof)K<df7dB(ZkKJ}ou!WbdHU_lPM|DX63y{+y&0q<AC>-5;xOM8#@TV|hnk;r2+lED!qlnV!?B5h^+;A&@~uc6U+T_MRp>xB5TT7vhpEY`X4xdWpgn%CcK(p6|P;$-wISF%^5Wl8cp{1IaS^)vbr`!P;a@i2",
  "CTo~Okp*?KIdM&kY@_8jF)4rXxwT*7z|%>66zJFTkU>vp1hrgD9qWd5Hd&GW(*bssu9HQ}N$y2ai5J9MR$paF)km9ye9!}6XjbF*vD2FxHTMEyfD-6Egbp&Ke}U_G3+M=V{7NWVjbtDaA6Hcpuq1eUc7-Jg>lo{`<J4o8&+D8K)fMKwk)-%*j2e3x`Mo^mr||r>tyUt)F^~f6vuZmh`^m?~@84%wP)KLFbQ=86-A~}_n}1GcY}2t3oa-GXHx_P#2@-twa3(}qW3^O#g4PV5@&C!Iq7AsbAXxQ!N>UE_zb8)rP9wA4qB15|qSG#y$sue^j$GY|G9z5_*;Vc+WQl>cnc~QR*$>ulA>L?u8iMU>U?viKPkhz|JAl*8UU{p}Gr(Lsm_Vh`e6z_m)wmJnn{CYOO9!ud_y%lw6Xem=?i@mMsfiN;&Q|POEr5j}^I|-VeupmY57lt)HM6@$N7e(*@j^}n;dzQq)co+m9O~;;F*&&aqNff!i_&p71&*TOej_AiWE3TOOg<GPy)$zB=hwes%=^ko=H(QvOCX@n3jbi4tcw4fp`=0qAiv&wv0`3XxLQIj7?xAwc^9OQCA?W3zQPfV%pP;>Qq~%#MUkx+>XD7sAA=V#b~Oy5sxkbe17wvX0E|ZxJD6}#d&#fQCn`fvpq&NCFGzPQ{3~cRDx}EA%K",
  "=X;ggpaF!x?lQnXJ&5u|jt`nvO}P$|@fHEa)vOfd<t%5OFb)DJmO4ITQE>U5!dpg34COzj61V%`byc-S3iZ7@FZp}cVG?waN#LmCV8s3E1v#Yi%mHoQZ9Yc*sG1^!k0zuX7_|P0w2{fDeq<L%V&T?L_;@<6BWr~QS+YgRh>~_Y!z&)k%K03COb>~Z!pZXSzMXhh-cjb)n$v0kCI(sy96ejn7vt6nG#w*#&Ry7mKvcj!+i|C5l)7F%!s|L<Fs*(^tq8Hu$6t}G)rIH|s?cJSBl(2}@^!Vl;2^iNCkv;T!K+XsoAob-@c}P2EWbo}0(qn0N8vOGL~Ni%bt)!7Xc=HMUEMVo8q9Rp3Wn@|bM0pQnvpgO+jTyzC$7EPW9J5Lr~B_F;0UKcib>k_>Qy27O2)p1S{IiV0Z54$D8<pg`0ch=jDef>ea*pq&`W6+X=<OIf+b(KI8AbwJ@6~OF1O-mcQ@Y)yZWT)mL3WtDUhCDjU!11sLkZ~qtKVb4L{4sDdK+WfzPjgn5JV{GcThH_kjTW-s3RnAB>oCRPWU+Kq@MrPqq($#~w1a-SK+OtkTt*T`acgHf|Udw-a|rolRK)K$+Bdu26}EZaffH><S|JuQsd`Ncz|E+gUIJherT$75jSIjq30y6S;&UT|OmFEp1rn`9hO#=^WAuO5r-ocrX+FG-lU1%n+G3XqNado#J#RdQOXZ02{0Nl@TD;JiE)nqjVfIG)z<IV&h4mUqm_|Yq!zfnnCMoRZ%I#(z>@1CJ*4>g=ALiPG-x+WQV)KU#THs{|(d6holRPgu_*w%4Gsb`YfG64F%vb<C+Cd!Rol~KhR>&7%!f6*&cl8Vn<SgFa_!pK>7S#;;T*;i!N($Xa!LBI{M92>Ssz_b8p4*b3fdt#3*)RFY9?InH5@HVtwbWv=@o*jTVC5o`@I|w2i0M`1<qQe2@tjH4L%_<QT$cH6ibWZ0Q>Q8r8m(r&Csy`<Fv5%|a7IFVP()gFY1IXONc}7HbTp_|C#CfGrp^c*d8Ouiw(Yw0B2U46;>q2m",
  "bYeAaxlnJ*wuzxtd>MaVw8K!4N*o6ZG&XLT-L2=UQmBgPeO9zL;LQX%VTf!9>(qycb=1G|Wi`LnnWP;RK}sGqo1?*c)lp&blzFhQ>zzGHezPD5P+Lyx0~d6Xa0wq40l)`<(^6ZT#IwAOZFJ&q#t9X5)C$H_&b4G@0N03zqYy<KwFD4n;xZKvg|ZzfI`!T<J4b-T(h#=FE=CKY{Pr)7kdt692-;%uZOn?`YNO^VeKm4dbpLPs@z1QXv%;U;?^~9nN+NEV>otUL&FV9|+N((hqk3I(Fg9KtF+5C*qJYtfaf3rONyi3g`>`&GAlY1(n$4)lRgwtLdrFBJpW+x0hTE?$V=T{JPf@CyVJdB4F7#0qKqnImnGv(T2oOiS8uL|9EAs|m5*ah*B_3x;u_98;305x8u4PI;JWivQ-{X-Ullzm(-5-d_yk(ryB_VHpWs9HGOgrJFIg@zbuP=^OPDDZy(zd6N<c;gc7L>`x6UBoy(|Uo!C42HQd#C-(I5=Fg{V=;FS8UE0%FzvHlk_p%D=X4uRtAfxN0K%Lg#GC87}en+-A$AFAhsb%>vKH}6YLVSx#82*KU5}Z2J;+iO5sQE9Vk%!E<O5pw^+(xWfcop&zA`_7ek;36BNf%K0v{Z$gnC@K#_Z1Ur2R{Lb8sk(|q)#opB9ov6s$7H8_YW{!MdB<sr5",
  ">2tG98A|=N!~{he72PlK<ry9u^E_W6=dUct?5~zZdJDPfnpiCOHIp1Pe8!s{Nlgim(3j;>Aw2wDE3;P&qO9`-WUf|<!CoA1Msa)2VfMSgr%q_|%{dEXZPg1zNS}78mF$2vxvG5yl}R47i%Uy7h_>${(*bxX3-@S$mpLAQoBgY>Tn4~Ed_^noQ_HiF2P)Y*pUTK#an~_VkgaS#lM|_-$fcn+!NT(O#}V>z;U>nB9Ar{&e~eLUEWGJC-yT{cml1$|9-NPy*UEnlwK^U9K_7U!-AF(~qD0~2rXlR|co*q>n@(D$nUN4*ykIbYKeu%*aXQt0DmvgQ4Y}DNF4}5u2SS-;u1(Y^Vi0!wXN;wRl_iJv*7&yZl9%JQy4*lXed5=9Ag~FyZuVuBpM;^|OrY`Y=x+Sw-_fiC)N<dkz07W|4TR9V3-Vas`SRv|6I2(<+r$mh7Yk8K4h2viT(Y9Hh$mo*pIM6_DDTdX`IC#d6{|lhPDyJ8tkix)a}JelAR`K@;>-~b()~dCEeQnW!2x-~u93$`oZ5$lO`rIQ=s^&p1Z!rR7~vw;GM_c1IPu0Jul!f_bR|NbmgaRa+aE~<?am5rFbi6}AqcS+N@N?PCu8ltyDH)WW0$n#Ka;BeM^C`_o9*L$J?&Cb*qNtbJ%APkE6>yjc^QI^oVg1U^+`|(B1!)0%F}^r#@SOw!yrQT$({`*O9$_?&6q8_zc0~1GJU~A=ljYOFf|$J+R!&9l_`8#`0^!77}nSJPzU#tEEYq<b$eS_sU2pgG2>#v98vYW`n>_R14T7O<0Bv!y?*pan1~BmP&L<=*_-qWp=?$^SOBe48N+I?CEV6ooyKX2K1Lb-h=5=)%IvH1dYk%0rCK@+m)plbLf&i9`8PW*4r(b7z?|I~_}&-3w{VEopRQlEmJWox-BrBl{D~HMvK(H|hZRH0=ddw#u!c2ve;5(NhSdhSeX7Cqil?mlrN2x2!;b+-MG?TVY&!OYjgoX0Z>`b3mnfiV`3~(F`K%YHog^MLJF=&ThOh#>7Oi{Q^$5z})qR5La4Ds$LFQ|n)MZ(;WJAi^491kkz%NKb$UB44id6",
  "Y1;=OG9ypr*zpM>(-BqDR@}_`T}2r>WX#WqcX^I7(4?CxJ$PzF8RTBbriWDuj2@?u^KnR=uP8^%2c0)>AeaW4F~*V$lHj>_$|i<4Ww~(mH$^6g_4fuGsi(Xnk@;I@9%0$6{F3efW4s6=hHXR7OV%)j^L>|}H^|M3fdQcw$=Xojs-{lz%EM1bZqVheep^vLoJMrriJR+Ep4If;8nwu^29^}tN4r+6O!kQ}Oc0RDliOiJx<G)3%8``SmO`pHYFbd&HUCQKz_{9$6c*F*{TjA1G_E&T)C@Q<j%r3fb8f~0k9)JOD&kHRE-Snw;?R_4IOL%4;Ck%C(MMTJ1=YzRcf+Pc;lFwmz3e=$soh7DAmkcBJUVSVQIAYUE?9BlSj|ZYE_N1F_-IW#((CfeL{0~2`a8$V=sva!5H<T)F2-K&zBrU680h%67(_vs6$$U0pZ0b`_FdgH*Yf%WctE!|czge+Y&y+JgVGydQl?1%eZ}`<ePu*~JTMgAgj(JU0OAZe2bUUlFol<V2f%ZrAg*Zq<@U3cO_AQHu&~XiS|<YkS+o{Gt0Yz&G&LupXF?lb<V8gRg3B4~eQnp{6x88|?)>I7k)9GiZ3M^+WRk=azh<{KP<SIjQeC5vS2D!2G+y*wnK9CGx0Q>&OR&yYNt3N;NCw}#N?G|B#Y7?Gb}np{4gDTY<txze3(sC7)#DxyGGcvm3@#R5WSHzzNLEVT#_3kaXBw`<PpjSW4Gksu6wb5=N7cV3Vf3nXXG%=mEAgYYU1^0WM#yUEfs~^g4U7"
]
""")

# -- Helpers --
import difflib as _difflib
import random as _random
import textwrap as _tw

_LINE_WIDTH = 70


def _norm_answer(s):
    # Fold away the differences that should not matter when matching a typed
    # answer against an option: surrounding whitespace, internal run-length,
    # letter case, and a trailing period.
    return " ".join(s.strip().lower().split()).strip(" .")


def _best_match(raw, options, cutoff=0.82):
    # Resolve a typed answer to one of `options`, forgiving tiny typos. Returns
    # the exact option string (so the caller can decrypt with the canonical
    # value), or None if nothing is close enough.
    norm = {}
    for opt in options:
        norm.setdefault(_norm_answer(opt), opt)
    key = _norm_answer(raw)
    if key in norm:
        return norm[key]
    close = _difflib.get_close_matches(key, list(norm.keys()), n=1, cutoff=cutoff)
    return norm[close[0]] if close else None


def _show_choices(options):
    print("  Choose one (type or paste the exact phrase -- small typos are ok):")
    for opt in options:
        for ln in _tw.wrap(opt, width=_LINE_WIDTH - 6,
                           initial_indent="    - ", subsequent_indent="      "):
            print(ln)

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    secret = _open_secret(ITEM_SECRETS[index - 1], "hiding-hunt:snippet:" + str(index))
    if secret is None:
        print("  [error] Could not open snippet data -- contact your instructor.")
        sys.exit(1)
    return secret

def _find_compiler():
    for c in ("g++", "clang++"):
        if subprocess.run(["which", c], capture_output=True).returncode == 0:
            return c
    return None


def _compile_snippet(code, compiler, tmpdir, idx):
    src = os.path.join(tmpdir, f"s{idx:02}.cpp")
    exe = os.path.join(tmpdir, f"s{idx:02}")
    with open(src, "w") as f:
        f.write(code)
    r = subprocess.run([compiler, "-std=c++17", "-o", exe, src],
                       capture_output=True, text=True)
    if r.returncode != 0:
        return None, r.stderr
    r = subprocess.run([exe], capture_output=True, text=True, timeout=5)
    return r.stdout.strip(), None


class ActivityEngine:
    """Core check/passphrase logic for Hiding Hunt; importable without a TTY."""

    slug = "hiding-hunt"
    title = "Hiding Hunt"
    description = 'Compilable C++ snippets that distinguish overriding a virtual function from accidentally hiding it. Predict the exact output to build intuition for static type vs. dynamic type.'

    def __init__(self):
        self._actuals = None

    def items(self):
        """Return the public snippet dicts, in display order."""
        return SNIPPETS

    def compile_all(self, compiler, tmpdir):
        """Compile and run every snippet once; cache actual stdout per snippet id."""
        actuals = []
        for s in SNIPPETS:
            out, err = _compile_snippet(s["code"], compiler, tmpdir, s["id"])
            if out is None:
                return None, s["id"], err
            actuals.append(out)
        self._actuals = actuals
        return actuals, None, None

    def check(self, index, raw):
        """Check a 1-based snippet's predicted output against its actual compiled output."""
        secret = _secret_for_item(index)
        actual = self._actuals[index - 1]
        correct = raw == actual
        return {
            "correct": correct,
            "actual": actual,
            "feedback": None if correct else (secret.get("wrong") or {}).get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all predictions are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(s, index, total):
    print(f"\n{' ':=<70}")
    print(f"  Snippet {index:02}/{total:02}: {s['title']}")
    print(f"{' ':=<70}")
    if s.get("platform_note"):
        print(f"  Platform: {s['platform_note']}")
    print()
    for line in s["code"].splitlines():
        print(f"    {line}")
    print()
    print(f"  {s['prompt']}")
    result = None
    while True:
        n_lines = ENGINE._actuals[index - 1].count("\n") + 1
        if n_lines > 1:
            print(f"  (Output has {n_lines} lines -- enter each line, then press Enter.)")
            parts = []
            for ln_i in range(1, n_lines + 1):
                parts.append(input(f"  Line {ln_i}: ").rstrip("\r"))
            raw = "\n".join(parts)
        else:
            raw = input("  Your prediction: ").strip()
            if not raw:
                continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return result.get("canonical", raw)
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print(f"  Actual output: {result['actual']!r}   Type it exactly to continue.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    compiler = _find_compiler()
    if not compiler:
        print("error: no C++ compiler found (need g++ or clang++)", file=sys.stderr)
        sys.exit(1)

    print(f"\n  Compiling snippets with {compiler}...", end=" ", flush=True)
    with __import__("tempfile").TemporaryDirectory(prefix="hiding-hunt-") as tmpdir:
        actuals, failed_id, err = ENGINE.compile_all(compiler, tmpdir)
        if actuals is None:
            print(f"FAILED\nerror: snippet {failed_id} compile error:\n{err}",
                  file=sys.stderr)
            sys.exit(1)
    print("done")

    answers = []
    items = ENGINE.items()
    for i, s in enumerate(items, 1):
        answers.append(_ask(s, i, len(items)))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
