#!/usr/bin/env python3
# Generated by scripts/generate_activities.py
# To update: edit quiz-data/char-by-char.json and re-run the generator.
"""Activity: Char by Char


"""
import json, os, subprocess, sys, tempfile

# -- Crypto / obfuscation --
_KEY_A = "f9ec4f693c6f7dd44582e35df729a47b5a7ce91b68d744029513eedb7edc7ff0"
_KEY_B = "b53fb0a274b93d07a29541e847e6374628af641baddd631d3e31dda9b8893fd6"
import base64 as _b64, hashlib as _hl, hmac as _hm, json as _js, zlib as _zl

_SALT      = bytes.fromhex("a3f1b2c4d5e6f7a8b9c0d1e2f3a4b5c6")
_KDF_ITERS = 100000

# Split runtime key. This is obfuscation only; do not treat it as a secret.
def _xor_bytes(a, b):
    return bytes(x ^ y for x, y in zip(a, b))

def _runtime_key():
    return _xor_bytes(bytes.fromhex(_KEY_A), bytes.fromhex(_KEY_B))

def _derive_key(answers):
    return _hl.pbkdf2_hmac("sha256", "|".join(answers).encode(), _SALT, _KDF_ITERS)

def _stream(key, length):
    ks, i = b"", 0
    while len(ks) < length:
        ks += _hl.sha256(key + i.to_bytes(4, "little")).digest()
        i += 1
    return ks[:length]

def _decrypt(blob_hex, answers):
    blob    = bytes.fromhex(blob_hex)
    key     = _derive_key(answers)
    ct, mac = blob[:-32], blob[-32:]
    if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
        return None
    return bytes(a ^ b for a, b in zip(ct, _stream(key, len(ct)))).decode()

def _item_key(label):
    return _hm.new(_runtime_key(), label.encode(), _hl.sha256).digest()

def _open_secret(blob_text, label):
    try:
        blob = _b64.b85decode(blob_text.encode())
        key = _item_key(label)
        ct, mac = blob[:-32], blob[-32:]
        if not _hm.compare_digest(mac, _hm.new(key, ct, _hl.sha256).digest()):
            return None
        pt = _xor_bytes(ct, _stream(key, len(ct)))
        data = _js.loads(_zl.decompress(pt).decode())
        return data if isinstance(data, dict) else None
    except Exception:
        return None

_BLOB = "2671c43f8be44ebd06955f551aff31f7b3606d45cf25992eb9bf4dcdff426479cdab70ca798fc698604029a6b99d68c14946729aec7b"

# -- Public snippets and encrypted answer/explanation material --
SNIPPETS = json.loads(r"""
[
  {
    "id": 1,
    "title": "counting characters the hard way",
    "platform_note": "const char* string literal, any platform",
    "code": "#include <iostream>\nint main() {\n    const char* s = \"hello\";\n    int count = 0;\n    while (s[count] != '\\0') ++count;\n    std::cout << count << \"\\n\";\n}",
    "prompt": "What does this print?"
  },
  {
    "id": 2,
    "title": "pointer difference after walking to the end",
    "platform_note": "const char* pointer arithmetic, any platform",
    "code": "#include <iostream>\nint main() {\n    const char* s = \"abc\";\n    const char* p = s;\n    while (*p) ++p;\n    std::cout << (p - s) << \"\\n\";\n}",
    "prompt": "What does this print?"
  },
  {
    "id": 3,
    "title": "the *p++ pattern",
    "platform_note": "postfix ++ on pointer, standard evaluation order",
    "code": "#include <iostream>\nint main() {\n    const char* s = \"hello\";\n    const char* p = s;\n    char c;\n    while ((c = *p++) != '\\0') {\n        if (c == 'l') std::cout << \"L\";\n    }\n    std::cout << \"\\n\";\n}",
    "prompt": "What does this print?"
  },
  {
    "id": 4,
    "title": "in-place mutation while walking",
    "platform_note": "char array (writable), toupper from <cctype>",
    "code": "#include <cctype>\n#include <iostream>\nint main() {\n    char s[] = \"hello World\";\n    char* p = s;\n    while (*p) {\n        *p = (char)toupper((unsigned char)*p);\n        ++p;\n    }\n    std::cout << s << \"\\n\";\n}",
    "prompt": "What does this print?"
  },
  {
    "id": 5,
    "title": "two pointers crossing -- the classic reverse",
    "platform_note": "char array, pointer comparison, any platform",
    "code": "#include <iostream>\nint main() {\n    char s[] = \"abcde\";\n    char* left = s;\n    char* right = s;\n    while (*right) ++right;\n    --right;\n    while (left < right) {\n        char tmp = *left;\n        *left++ = *right;\n        *right-- = tmp;\n    }\n    std::cout << s << \"\\n\";\n}",
    "prompt": "What does this print?"
  },
  {
    "id": 6,
    "title": "word counting -- combining the sentinel loop with state",
    "platform_note": "const char* with space as delimiter, any platform",
    "code": "#include <iostream>\nint main() {\n    const char* s = \"  foo  bar baz  \";\n    int words = 0;\n    bool in_word = false;\n    while (*s) {\n        if (*s != ' ') {\n            if (!in_word) { ++words; in_word = true; }\n        } else {\n            in_word = false;\n        }\n        ++s;\n    }\n    std::cout << words << \"\\n\";\n}",
    "prompt": "What does this print?"
  }
]
""")
ITEM_SECRETS = json.loads(r"""
[
  "K#Ee|gHsP;Mm})0uX#1CoKNdMveDvdoj;pg$*lI%DoP-KA5yG18CV-KZqDi#Qh2t|6$sSYw7;L)$zaF|#1c~XWJC^p_{v-~VtGH5j4!*vGq8K?Sp&$Y*uEU@x>wX<Vd*TQt$e<?nEW?D^}6h+6wn6%>4E})b>ldR#<(iHalDWPPS&1}b1a^$sZ=-O${S{xQH<aQPg_dI)hFcYht`rZ!XgI)k0N5iAf!BcDQY1=Rei6UCxYqCb|=~iM&{|L!t6hK4jld;g8S9}n(}2)0?<oXC&+_-Ctg;niLSA_tr7?B3b)2r9IR(B)+vU6#4d=8<+Hu=yYP+!C$0dQAxsOoytASUOf>@fM44)X8ldnEeL(bXJb%W08YbzP4hZa*oKO&@oaAZ65z9OGq!@Cyaulf&_8T@P(SnyOa->bN($)M*0S(xQmT$p4!YP33Naz",
  ")eNNO3BknUXZbfq7;yKed;Ld5bw88h`s0kb#-CnQUBIOVS}V_1^YEV$r$SYAcv!I8Hs0!G4oY?%x|+-WjQoQZgd`L{1XHceOi}P=K{kJ+kT*(@*$Uup9*8q<ZFBxkPA~Rn=(?46iP=5s4YxwA%|(-l1{QK3O@jyeU=mie-e&+KKP*rwzMFspZ_0jI3v$$|Ki-Wd0{@z=P2~#zky^(m{KCc)x918buv&?2o!eRZHpo!>=t(g}W`39i1rc3^H5fEB6@$m5GMOQ1H=y5xVvV0DGAWk>;ZD4%9Zw!yi_z!E?LP&nUdy5;s$!tau|%H<9sY^!=&9-LiL^o_#5LAa_%9>;e6cP!z);_--i)f4+~8F6s3qhxBK6^i7dF<6(z6GD{PmvLJB{h!_}N#H<{r>P5IaAEmR_w?z6q)ftkau&JHL8TyalL59pirUoZC*J#k^auzp<?2nP*vr$+U4Ib`6{o0YHv$w133ho3*4Qg1ivCb{u++WB",
  "OHdK=xwMJ_-=4A&D8YaW1`ZK)QyTJ|Ckj#^=mVJ9q9KrPkzHo_rpG-Fs>i_xNjzvMiGDOB^h8YJR*hWC>|{7q8vj%7aXL-{Rhe7Hq*tLVG@tN)OFI@#obd#vsFpBSex_=nUqEM2XF;JW5JIuo^uHiIoJ0erB@a7?M(74@yh6PL&8R&CNwwpE&Lqoj7%1&=$gIvV(swGN9Vpag*uA!h)obDMV?JBU!Jx<?U?13Hw<%Y0HHv$ORAkv;-T$9o8VGK00@rp5_9LIsi=R38=t6w3(@+SnzSe2!ltW1`dqRq^^kM3+a~a3QV4&?-g~2nc84e;G`+o%)fDGSTam~1+!CUdaaEPNjcx)_(g|pEjhQ5kbvYG0X-d_sP5F1>WW0mknu9K1uuQKVe5kJJUj7+Xw%7COTs>?-e4+`-Jjy6knlqpzj@POe6p_e7_jVw~oJd^GPz)=Kn2H<EJ?~)ie9{qe>?qRiGihc_UdDEv`(PlRonE",
  "xZBW1djq1nTeI&PJ;P$*4fyE|$yz=d|JtwVILWDy6-sN}e0Hg-)yy~SDgN3PEB-OZx0)N}>#ig(n&>Gm%Z$)IpXBlqk)TQY%iAR6_K!8c{r+zsP9W6}X9c%sXO;L%UUg;kSLT*@T_5yLgg>ltEfyU?TZ|)Nhu@xA;6^e9^kM+*M&28XPB9ysZe7w%oxi?+FAk>ddR|;vBib|qi<!9np~~K^SiOs4+|3DSL89PjuY&!k=g`+i{Mp-onx+X$(eCzg?p-6@tr~B#cFuO8pA_vPOX+_cbscgkOgXP?vB>z(HLhz+sVU0ZU8uVwh2)<H8wZt5?<}JPLt~EOoO1U`ziitoCKNFJu@i+u!*t9wv*SWcF=dBAFuN8x;lfLshk5FgC{{NXtiKdQSaCocP5b)&ChxXO;9}=%;YD;H6fi*loB{u*N51T%$sAq^(+ds?<s&e>M$6fzvl^*_?H$XimQ+MPnVimJSm+nG>G5`OF6!;52m0HnjUHFg1catxV$D@ce=lbd7Ub^g?6*RRRf!_sahIsiHI`ZE<Eq5I^$|eyny^VoS3BYLAGX1!g?nX!YIOjlR3*$F",
  ">lt5Etm?uhvbO<B!G(P61H+#w*)Fi{)iR!~l@gok%|(Xm$E(nUAcKX@i5+0m{D9qQ-6H>+7j=ub-GVa<9(q=TNs<<0g=-HNZzy1zpHMbNc`^<kGHEqrnBqapO-krE(QCP32Svw~OXl~J*z?uKlo|N1ObYQU!7%MUcuELW!nh>@E-J!`#rC2IV#w-k1KA&zqgm1PKNV7vWzd>#%-kHqQrRFvmLH6`7(i;Bs`8{SJbZG!rT3n*PAWwBRhJL_JR(D+iDe7{@J^2}#UmPkaR_Owa4*J25&(9>=l0gtM&B|S>Je2qE=VFgvut0Wr3^8%ykff(yIT|ERc|!A1DINYH^KG%8ejxXokaav=nS&D0TL!fJj$g7!0l;b@!vg((+@uha;k744Rwt6qDB9ksFYG39WczFMhU(jA@vvp7>HURv4Z)bQ}7-`Zro-9Y~v*gfOTv;Qhcslv384g!M3>N;rPr0RnP~^eX6IxcI`awC@H<tXv3O`c)P;H)YCWnFBlOblg1ZFc^dtJ>|z`~vWM8`=z!FtLoeoEz0f2lyH;(~m81-b*eBVBS>^H&z)4)fJN~M*ecy8",
  "p)?YkF-944;Kbd#@ZyMa6+9c+YU1jcr+AME6bUcJN2)Nb1(_2bpFdY2dGUiqz$h8>e$o#ebN$IRboa!SC#2@jU)xvEQj4T^e~PFN(WV@(5vzub%;=&MxEXzx_#ode&m6ZLTKUarY}p}fq~{;5Kf}g5@#jtp?g8==m(0j<#-<j)8Y;v$m2cI4tEZM<k=SXFbL!qfPJox@(!v!q4DB0#`zI12XJ7|iwsmj1TwP)H7rtKerB8ZEk0gR;yKAy;7EvIXsg1X9r3L7RQ-LG+>fk9wL112G&>-k<nsnxUMfklQMN*dByf|xAK(a+uu$1q#lkfD-*moyT{#+t&&BrvHqHMWE!g!cBFY&oh9#)-X@8c6o(J>;JbX9U^g1wKx&>Bl9tUvU_*Mq5=>CulbKr$>JcCL0@ZFUNQH%vIzo`~wip=-#;GMWx1HDG`YK1y+?jDy>}pxBG$)PD<252~UxsEO-O2rW@bN0cT4^6N*zr2C|f;p9z+Fs8^FAliy6Qs)G+*69bBmZ_WqlA~zZ0b=Z|t`HZO!JUUICplZ-bE@6A?+82sH}K6Rk>K9|i*{lF^T>R`Rf0Tyx!ZC;dh+BrYI_Qf=;k>|Lv4K}e#%xFMC^8vtq1D7?TASZuYIXfGUinjE-5D<s}v`ynwlnae=~gR8&vJ=0R{"
]
""")

# -- Helpers --
import textwrap as _tw

_LINE_WIDTH = 70

def _banner(title):
    print("=" * _LINE_WIDTH)
    pad = (_LINE_WIDTH - len(title) - 2) // 2
    print(" " * pad + " " + title + " " + " " * pad)
    print("=" * _LINE_WIDTH)

def _hr():
    print("-" * _LINE_WIDTH)

def _wrap(text):
    return _tw.wrap(text, width=_LINE_WIDTH - 4,
                    initial_indent="  ", subsequent_indent="    ")

def _show_wrong(raw, wrong_dict):
    exp = wrong_dict.get(raw)
    if exp:
        print()
        for ln in _wrap(f"[wrong] {exp}"):
            print(ln)
    else:
        print(f"  [wrong] {raw!r} is not the correct answer.")

def _show_passphrase(passphrase):
    print()
    _hr()
    print(f"  Passphrase: {passphrase}")
    _hr()
    print()

def die(msg):
    print(f"error: {msg}", file=sys.stderr)
    sys.exit(1)


def _secret_for_item(index):
    secret = _open_secret(ITEM_SECRETS[index - 1], "char-by-char:snippet:" + str(index))
    if secret is None:
        print("  [error] Could not open snippet data -- contact your instructor.")
        sys.exit(1)
    return secret

def _find_compiler():
    for c in ("g++", "clang++"):
        if subprocess.run(["which", c], capture_output=True).returncode == 0:
            return c
    return None


def _compile_snippet(code, compiler, tmpdir, idx):
    src = os.path.join(tmpdir, f"s{idx:02}.cpp")
    exe = os.path.join(tmpdir, f"s{idx:02}")
    with open(src, "w") as f:
        f.write(code)
    r = subprocess.run([compiler, "-std=c++17", "-o", exe, src],
                       capture_output=True, text=True)
    if r.returncode != 0:
        return None, r.stderr
    r = subprocess.run([exe], capture_output=True, text=True, timeout=5)
    return r.stdout.strip(), None


class ActivityEngine:
    """Core check/passphrase logic for Activity: Char by Char; importable without a TTY."""

    slug = "char-by-char"
    title = "Activity: Char by Char"
    description = ''

    def __init__(self):
        self._actuals = None

    def items(self):
        """Return the public snippet dicts, in display order."""
        return SNIPPETS

    def compile_all(self, compiler, tmpdir):
        """Compile and run every snippet once; cache actual stdout per snippet id."""
        actuals = []
        for s in SNIPPETS:
            out, err = _compile_snippet(s["code"], compiler, tmpdir, s["id"])
            if out is None:
                return None, s["id"], err
            actuals.append(out)
        self._actuals = actuals
        return actuals, None, None

    def check(self, index, raw):
        """Check a 1-based snippet's predicted output against its actual compiled output."""
        secret = _secret_for_item(index)
        actual = self._actuals[index - 1]
        correct = raw == actual
        return {
            "correct": correct,
            "actual": actual,
            "feedback": None if correct else (secret.get("wrong") or {}).get(raw),
            "explanation": secret.get("explanation", "") if correct else None,
        }

    def passphrase(self, answers):
        """Return the unlocked passphrase once all predictions are correct, else None."""
        return _decrypt(_BLOB, answers)


ENGINE = ActivityEngine()


def _ask(s, index, total):
    print(f"\n{' ':=<70}")
    print(f"  Snippet {index:02}/{total:02}: {s['title']}")
    print(f"{' ':=<70}")
    if s.get("platform_note"):
        print(f"  Platform: {s['platform_note']}")
    print()
    for line in s["code"].splitlines():
        print(f"    {line}")
    print()
    print(f"  {s['prompt']}")
    result = None
    while True:
        n_lines = ENGINE._actuals[index - 1].count("\n") + 1
        if n_lines > 1:
            print(f"  (Output has {n_lines} lines -- enter each line, then press Enter.)")
            parts = []
            for ln_i in range(1, n_lines + 1):
                parts.append(input(f"  Line {ln_i}: ").rstrip("\r"))
            raw = "\n".join(parts)
        else:
            raw = input("  Your prediction: ").strip()
            if not raw:
                continue
        result = ENGINE.check(index, raw)
        if result["correct"]:
            if result.get("explanation"):
                for ln in _wrap(result["explanation"]):
                    print(ln)
            return raw
        _show_wrong(raw, {raw: result["feedback"]} if result.get("feedback") else {})
        print(f"  Actual output: {result['actual']!r}   Type it exactly to continue.")


def main():
    _banner(ENGINE.title)
    print()
    input("  Press Enter to begin...")

    compiler = _find_compiler()
    if not compiler:
        print("error: no C++ compiler found (need g++ or clang++)", file=sys.stderr)
        sys.exit(1)

    print(f"\n  Compiling snippets with {compiler}...", end=" ", flush=True)
    with __import__("tempfile").TemporaryDirectory(prefix="char-by-char-") as tmpdir:
        actuals, failed_id, err = ENGINE.compile_all(compiler, tmpdir)
        if actuals is None:
            print(f"FAILED\nerror: snippet {failed_id} compile error:\n{err}",
                  file=sys.stderr)
            sys.exit(1)
    print("done")

    answers = []
    items = ENGINE.items()
    for i, s in enumerate(items, 1):
        answers.append(_ask(s, i, len(items)))

    print()
    print("  All correct.")
    passphrase = ENGINE.passphrase(answers)
    if passphrase is None:
        print("  [error] Decryption failed -- contact your instructor.")
        sys.exit(1)
    _show_passphrase(passphrase)


if __name__ == "__main__":
    main()
